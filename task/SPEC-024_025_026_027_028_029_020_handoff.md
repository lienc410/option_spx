# SPEC-024_025_026_027_028_029_020 Handoff

## 修改文件
- `strategy/selector.py:49` — `StrategyParams` 新增 `initial_equity`、shock/overlay、ATR trend 与 persistence 字段
- `signals/trend.py:72` — 新增 ATR14 计算、ATR-normalized trend 分类与 `get_trend_history(use_atr=...)`
- `backtest/portfolio.py:11` — 新增 `DailyPortfolioRow` 与 `PortfolioTracker`
- `backtest/metrics_portfolio.py:19` — 新增 daily portfolio metrics，包含 `pnl_per_bp_day`
- `backtest/registry.py:1` — 新增 experiment id / config hash 工具
- `backtest/shock_engine.py:50` — 新增 8-scenario shock repricing 与 `run_shock_check()`
- `signals/overlay.py:20` — 新增 overlay state machine 与 `compute_overlay_signals()`
- `backtest/engine.py:152` — 新增 `BacktestResult`，集成 portfolio rows、shock reports、overlay/shock gate、ATR trend、persistence exit
- `backtest/attribution.py:42` — 新增 strategy / regime attribution
- `backtest/run_shock_analysis.py:81` — 新增 Phase A shock analysis runner，并修正 hit-rate 逻辑不依赖 `approved`
- `backtest/run_oos_validation.py:35` — 新增 IS/OOS split 与 5 报表入口
- `backtest/run_trend_ablation.py:13` — 新增 4-way trend ablation runner
- `tests/test_spec_batch_024_029_020.py:17` — 新增本批 spec 的 focused regression tests

## 收尾
- 缓存清除：否　Web 重启：否

## 验收结果
- 通过：SPEC-025#1,#4,#5；SPEC-026#1；SPEC-027#1；SPEC-020(追加)#1,#3,#6；结果对象三元解包兼容、portfolio rows / shock reports smoke 已通过
- 未通过：SPEC-026#9 → 未执行全历史 `EXP-full` overlay 回测 vs 目标 `Sharpe≈0.86 / MaxDD≈-12.22% / trades≈348`
- 未通过：SPEC-029#3,#4,#5,#6,#7 → 未执行全历史 IS/OOS 验证打印 vs 目标 OOS AC 全部可量化
- 未通过：SPEC-020(追加)#7 → 未执行全历史 `EXP-full` vs `EXP-baseline` OOS Sharpe 方向性验证

## 阻塞/备注
- 本轮已完成结构实现、单测与短窗 smoke：`venv/bin/python -m unittest discover -s tests -v` 27/27 通过；`run_backtest(2024-01-01→2024-03-31)` 成功产出 `trades/signals/portfolio_rows/shock_reports/portfolio_metrics`
- 尚未跑 full-history / OOS 长窗验证，因此与 Spec 中阈值绑定的 AC 仍需下一轮实测确认
