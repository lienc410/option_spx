# SPEC-107 — 2nd Quant Review Packet (Round 2 — Implementation Verification)

**Date**: 2026-05-26
**Prepared by**: Quant Researcher
**Audience**: 2nd Quant Reviewer
**Stage**: Verify R1-R7 incorporation from round-1 review before PM approval
**Reviewer response**: `task/SPEC-107_2nd_quant_review_packet_round2_2026-05-26_Review.md`

---

## 0. TL;DR

Round-1 review (`SPEC-107_2nd_quant_review_packet_2026-05-26_Review.md`) issued PASS WITH MINOR REVISIONS — 7 required revisions R1-R7. This packet is **focused implementation verification**:

> **不重审 strategic direction。只验证 R1-R7 在 SPEC-107.md 上是否落地准确，文字是否精确反映 reviewer intent。**

请 reviewer 逐条对照 §1-§7，确认每条 R 都被 implemented correctly，or flag specific text that needs further edit。

---

## 1. R1 — Explicit Priority Order

### Reviewer required (round 1)
> Add explicit priority order: hard-risk layer > SPEC-107 governance > raw selector.
> AC3 must include unit test: SPEC-107 says OPEN/HOLD + SPEC-103 R6 says BLOCK/CLOSE → final = BLOCK/CLOSE.

### Where implemented in SPEC-107
- **New §F "Priority Order"** (lines 182-211): 7-layer stack with table
- **AC3 updated** (lines 234-252): added "Mandatory priority test" with explicit assertion
- §A line ~50 references new §F instead of inline priority logic

### Key SPEC text (verbatim from §F)
```
| Priority | Layer | Source |
|---|---|---|
| 1 | Manual PM override                              | governance manage CLI / web |
| 2 | Broker/system stop-loss + lifecycle exits       | trade lifecycle |
| 3 | SPEC-103 hard-risk daemon                       | R5 / R6 |
| 4 | EXTREME_VOL / EXTREME_VIX                       | selector hard exit |
| 5 | SPEC-107 scheduled actionable decision          | sched bar 10:30 / 15:30 ET |
| 6 | SPEC-107 hysteresis state                       | A2a state machine |
| 7 | Raw selector recommendation                     | passthrough |
```

Plus statement of principle:
> Hard-risk layer always overrides intraday governance.
> SPEC-107 may delay soft selector churn.
> SPEC-107 may never delay hard-risk exits.

### Question for reviewer
- Layer ordering correct?
- Is "Broker/system stop-loss + lifecycle exits" one layer (2) or should they be separate?
- AC3 mandatory test wording adequate?

---

## 2. R2 — Bypass List Expansion (4 → 7 classes)

### Reviewer required (round 1)
> Expand bypass list to include lifecycle exits / hard_exit metadata / stale-data failsafe.
> Trend only bypass if hard_exit flag.
> DD Overlay armed NOT bypass. Roll = lifecycle bypass, not selector bypass.

### Where implemented in SPEC-107
- **§C rewritten** (lines 85-110): 7-class numbered list + explicit NOT-bypass list + bypass_type enum

### Key SPEC text (verbatim from §C)
7 bypass classes:
1. Manual PM override
2. Broker / system stop-loss
3. Profit-taking / lifecycle exit / roll / expiration management
4. SPEC-103 daemon hard rules (R5/R6)
5. EXTREME_VOL / EXTREME_VIX
6. Selector verdict explicitly marked `hard_exit=True`
7. Emergency data-quality / stale-data failsafe

**显式 NOT bypass**:
- Trend BULLISH↔BEARISH 翻转 (unless metadata flag)
- DD Overlay armed state change
- Aftermath gate open/close
- Any strategy-level signal change without `hard_exit` flag

### Question for reviewer
- 7-class enumeration matches your intent?
- "Selector verdict explicitly marked `hard_exit=True`" — is this acceptable as `hard_exit` metadata pattern? Or do you want a different mechanism (e.g., separate `RiskSignal` enum)?
- Bypass_type enum string values OK? (`manual_override`, `broker_stop_loss`, `lifecycle_exit`, `spec_103_r5`, `spec_103_r6`, `extreme_vol`, `selector_hard_exit`, `stale_data_failsafe`)

---

## 3. R3 — AC7 Tolerance Tightening

### Reviewer required (round 1)
> Tighten AC7: EOD ≥92%, ≤3h ≤4, flips 93±5 OK.

### Where implemented in SPEC-107
- **AC7 updated** (lines 272-281)

### Diff
| Metric | Before | After |
|---|---|---|
| `intraday_flips` | 93 ± 5 | **93 ± 5** (unchanged) |
| `episodes_le_3h` | 3 ± 1 | **≤ 4** (changed to upper bound) |
| `eod_agreement_pct` | ≥ 90% | **≥ 92%** (tightened) |
| `round_trips` | 18 ± 2 | **18 ± 2** (unchanged) |

### Question for reviewer
- Tightening matches your intent?
- Note `episodes_le_3h` 从 "3 ± 1" 改为 "≤ 4" — 含义类似但表达不同 (前者允许 2 也通过, 后者允许 0-4 都过). Acceptable?

---

## 4. R4 — AC8 Strengthening (Two-Layer Test)

### Reviewer required (round 1)
> Strengthen AC8 with synthetic HIGH_VOL/STRESS fuzz test.
> Real subset + synthetic/fuzz architectural invariant.

### Where implemented in SPEC-107
- **AC8 expanded into AC8a + AC8b** (lines 282-303)

### Key SPEC text
**AC8a (real subset)**:
- HIGH_VOL bars (n=203) + STRESS bars (n=10) from 12mo P3
- Assert: 0 BPS episodes generated by hysteresis

**AC8b (synthetic fuzz)**:
```
VIX regime = HIGH_VOL or STRESS (e.g., VIX 23, 27, 32)
IVP values around boundaries: 33, 35, 37, 42, 45, 50, 53, 57, 60
prior governance state = "Bull Put Spread"
raw selector verdict = non-BPS
```
Assert: governance MUST NOT convert non-BPS verdict to BPS hold/open regardless of IVP or prior state in HIGH_VOL/STRESS.

### Question for reviewer
- AC8b fuzz input space (VIX × IVP × prior_state × verdict) covers your architectural invariant intent?
- "Architecture invariant" language matches your spec phrasing?
- Does AC8 need a quantitative test count target (e.g., 10K fuzz samples)?

---

## 5. R5 — Decision Log Field Additions (15 → 19 fields)

### Reviewer required (round 1)
> Add immediately (avoid JSONL schema drift):
>   last_actionable_decision_at, next_actionable_decision_at, final_priority_layer, bypass_type

### Where implemented in SPEC-107
- **AC6 updated** (lines 263-271): explicitly lists 4 new fields + notes "from day-1"
- §E (lines 142-181) shows updated 19-field JSONL sample (verify §E body explicit shows all 19)

### Key SPEC text (AC6 new fields)
```
- last_actionable_decision_at — last sched-bar or bypass actionable decision timestamp
- next_actionable_decision_at — next sched-bar or null
- final_priority_layer       — int 1-7, which §F layer won
- bypass_type                — enum per §C, or null
```

### Question for reviewer
- 4 new field names + types match your intent?
- Should `next_actionable_decision_at` ever be null (e.g., at EOD after 15:30)? Or always populated with next trading day's 10:30?
- `final_priority_layer` as int 1-7 OK, or should it be string enum (e.g., "spec_103_r6")? Both? Currently SPEC has int + bypass_type string.

---

## 6. R6 — Forward-Compat Config Flag

### Reviewer required (round 1)
> Add config flag: hysteresis_lower_force_close=true default; future Q077 can change.
> Upper-bound (>57) close always active.
> Changing flag requires Q077 approval / separate SPEC.

### Where implemented in SPEC-107
- **§A line ~57**: lists `INTRADAY_HYS_LOWER_FORCE_CLOSE = True` in configurable parameters
- **§A close trigger line ~46**: re-worded — IVP > 57 ALWAYS close, IVP < 35 only if flag True
- **AC9 new** (lines 304-313): full AC for the flag with semantics + protect clause

### Key SPEC text (AC9)
```
- When True: IVP < 35 forces BPS close (current A2a production semantics)
- When False: IVP < 35 does NOT force close existing BPS; remains entry-only filter
- Upper-bound (IVP > 57) close always active, never gated by this flag
- SPEC-107 ships with default True
- Changing this flag in production requires Q077 approval / separate SPEC
- Unit tests: cover True 和 False two behaviors
```

### Question for reviewer
- Flag name `INTRADAY_HYS_LOWER_FORCE_CLOSE` clear?
- "Changing this flag in production requires Q077 approval / separate SPEC" — is documentation in config comment + AC9 enough? Or do you want a runtime check (e.g., flag-change emits Telegram alert)?

---

## 7. R7 — State Persistence + Timezone/Calendar

### Reviewer required (round 1)
> Hysteresis state must persist across process restart.
> State key must include account / underlying / strategy / position id.
> If no active position exists, state resets to WAIT.
>
> Scheduled times are America/New_York.
> Half-days / market holidays should not generate false scheduled decisions.

### Where implemented in SPEC-107
- **§A "State persistence (R7)" block** (lines 60-66): atomic write to `data/intraday_governance_state.json` + state key composition + reset rule
- **§B "Timezone & Market Calendar (R7)" block** (lines 75-83): NY tz + half-day handling + holiday list source
- **AC1 updated** (lines 214-222): persistence requirements + unit test for restart-recovery
- **AC2 updated** (lines 224-232): TZ + half-day + holiday handling + unit tests for those cases

### Question for reviewer
- State persistence file path `data/intraday_governance_state.json` OK?
- Half-day rule "early-close skips 15:30 sched, last actionable at 12:30" — is this the right behaviour, or should early-close days have no sched eval at all?
- For market holidays: SPEC says "do NOT generate false scheduled decisions" but doesn't specify holiday data source explicitly. Acceptable, or do you want `pandas_market_calendars` named explicitly?

---

## 8. Out-of-Round-1 changes (please verify these too)

### 8.1 New §F Priority Order section
Not explicitly in your R1-R7 list but you said "explicit priority order" in R1. Added as a top-level scope section (alongside A-E) for visibility. **Question**: §F belongs under Scope or as its own top-level? Currently it's inside Scope between E and AC.

### 8.2 New AC9
For R6 config flag. **Question**: AC9 belongs as standalone AC or should it be merged into AC1?

### 8.3 "不在范围内" expanded
Added two new exclusions:
- Trade lifecycle operations explicitly out-of-scope
- Hysteresis band sensitivity test deferred to Deferred 3

### 8.4 Scope clarification in §不在范围
Per R1 addendum from your round-1 review:
> SPEC-107 governs recommendation/actionability only.
> It does not govern trade lifecycle operations such as profit-taking, stop-loss, roll, expiration management, or manual close.

Implemented as bullet 1 of §不在范围内.

### Question for reviewer
- All 4 out-of-round-1 additions OK as-is?

---

## 9. Verification Questions Summary (please answer)

| # | Question |
|---|---|
| V1 | §F 7-layer priority order ordering correct? Stop-loss + lifecycle one layer (2) or separate? |
| V2 | §C 7 bypass classes complete? `hard_exit=True` metadata mechanism acceptable? Bypass_type enum values OK? |
| V3 | AC7 tolerance (EOD ≥92%, ≤3h ≤4) correctly tightened? |
| V4 | AC8b fuzz input space (VIX × IVP × prior × verdict) covers architectural invariant? Test count target needed? |
| V5 | 4 new decision log fields (R5) correctly named/typed? `next_actionable_decision_at` null semantics OK? |
| V6 | AC9 config flag name OK? Does flag-change need runtime alert beyond doc/config comment? |
| V7 | R7 state persistence file path + half-day rule + holiday source acceptable? |
| V8 | §F at top-level (between E and AC) OK, or move elsewhere? |
| V9 | AC9 standalone (vs merging into AC1) OK? |
| V10 | Any R1-R7 requirement still not addressed correctly that you want re-spec? |

---

## 10. Not in scope (this packet)

- Strategic direction (covered in 4 prior reviews)
- Q076 P1-P3 research conclusions
- A2b alternative (cut by PM)
- Q077 low-IVP semantics (independent track)
- Implementation details below SPEC level (Developer responsibility)

---

## 11. References

```
task/SPEC-107.md                                                ← SPEC (current revision)
task/SPEC-107_2nd_quant_review_packet_2026-05-26.md             ← round 1 packet
task/SPEC-107_2nd_quant_review_packet_2026-05-26_Review.md      ← round 1 review with R1-R7
research/intraday/q076_p3_findings_2026-05-26.md                ← AC7 reference numbers
research/intraday/q076_p3_metrics_by_regime.csv                 ← AC8a reference
```
