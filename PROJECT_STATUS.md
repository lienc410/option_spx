# PROJECT_STATUS

Last Updated: 2026-05-13 (Q064 Phase 9 CLOSED APPROVE α — spell reset sensitivity 12 variants engine replay。仅采纳 P8（max_trades_per_spell 2→3）；P9 延后；hysteresis/no-high-reset/combo 全拒。SPEC-100 P8 待起草，commit 277ba9b)
Owner: Planner or PM

## Current Phase

- `stable` + `research-driven`
- Core trading system is stable; current work is mainly research, validation, and selective Spec-driven implementation.

## Current System Snapshot

- Primary project objective has now been reset at the PM level: **reasonably maximize account-level ROE**, not merely maximize `PnL / Sharpe / MaxDD` or `PnL/BP-day` in isolation
- “Reasonably” currently means: preserve explicit attention to drawdown control, margin stress / forced-liquidation risk, hidden concentration, and the opportunity cost of deploying scarce BP
- Recommended production posture: preserve current strategy matrix and add only research-backed, low-regret changes.
- Latest full strategy status doc: `doc/strategy_status_2026-04-20.md`
- Latest system status doc: `doc/system_status_2026-04-20.md`
- Live runtime host has moved to `old Air`; canonical running services now live there, not on the main machine
- Canonical runtime services on old Air:
  - `com.spxstrat.bot`
  - `com.spxstrat.web`
  - `com.spxstrat.cloudflared`
- Default compute host for heavy jobs remains the main machine; old Air is runtime-first, not compute-first
- New operational role exists: Codex `Server Maintainer` on old Air, responsible for runtime health, logs, and low-risk service recovery
- New standing research-review rule (PM, 2026-04-25): all future strategy / spec / variant / prototype comparisons must include the full metrics pack, not just `PnL / Sharpe / MaxDD`. Minimum pack now includes `PnL/BP-day`, `marginal $/BP-day`, `worst trade`, `disaster window`, `max BP%`, `concurrent 2x days`, and `CVaR 5%`
- Fast runtime reference: `SERVER_RUNTIME.md`
- Reference: `doc/old_air_server_maintainer.md`

## Active APPROVED Specs

（无——当前无待实施 SPEC）

## Recently Closed Specs

- `SPEC-099` — Telegram Bot SPX Profit-Target Alert Resilience. Closed **DONE 2026-05-11**（Developer impl 566789c + Quant fast-path fix d6c4b4b）. 触发背景：live BPS 到达 profit target 0.60 但 bot silent miss（state.json 残留 stale `status=closed`，PM 未跑 `/opened`）. Layer B：broker-state reconciliation alert（Schwab 有 SPX option 仓位但 local state 无记录时推 warning）. Layer C：Schwab-direct profit-target fallback（state.json 缺失/stale 时直接用 Schwab `averagePrice` 计算 capture%）. Quant fix：`_profit_check_from_state` category-filter bug（d6c4b4b）— `See: task/SPEC-099.md`
- `SPEC-098` — Q042 Drawdown Overlay Independent Frontend Dashboard. Closed **DONE + deployed old Air** 2026-05-10（commit 20476ff；handoff 20476ff）. AC1-AC12 全 PASS：独立路由 `/q042`（C1-C5 Dashboard）+ `/q042/backtest`（C6-C11 分析页）；4 个新 API 端点；nav 全页入口；paper fail-soft；两个关键修复：scatter 对齐（54c5afd）/ MTM OPEN 行（0fe90ca）— `See: task/SPEC-098.md`, `task/SPEC-098_handoff.md`
- `SPEC-094.1` — Q042 Sleeve A 参数替换（D30/2.5%）. **DONE 2026-05-10.** Sleeve A：DTE 90→30, short offset 5%→2.5%, no-overlap 90→30 days；Sleeve B 不变。Developer 实施 5 文件 ~15 行（trigger / pricing / executor / backtest / state / web / SOP），AC-1.1/1.3/1.5-1.10 全 PASS；AC-1.4 初次回测 ann=9.03% 偏 -0.91pp，Quant 诊断 `_exp_date` 应用 entry_dt 而非 signal_dt（每笔少持 ~1 trading day，30 DTE 放大 3.3% × 35 trades = -0.91pp）；Quant fast-path fix 3 文件 12 行（backtest engine + production executor + signals trigger），post-fix AC-1.4 exact match：n=35/WR=74.3%/ann=9.94%/MaxDD=-19.0%/total=192.3%。Sleeve B 不动（5/100%/2.2%/0%）。Grandfather 2026-03-12 in-flight 仓位 expiry 由 6-10 调整为 6-11（+1 cal day 副作用）。Bootstrap p=0.09 / P(C>A)=91% PM disclosed 并接受；alert 频率 +40%（1.30→1.81/yr）PM 接受。**Quant standing obligations**：首次 SPEC-094.1 生效后 HIGH_VOL trigger F4 re-validation；2026-11-10 半年 review 重跑 bootstrap；连续 3 笔 Sleeve A 亏损暂停 — `See: task/SPEC-094.1.md`, `RESEARCH_LOG.md R-20260510-15`
- `SPEC-097` — V2f Cluster Loss Mitigation: M1 Entry Frequency Throttle. Closed **DONE + deployed old Air** 2026-05-10（commit 4586a32）. AC1-AC6 全 PASS：live M1 Ann ROE +2.38%，Sharpe 0.235，stress cluster -44.45%（Δ +3.12pp）；baseline +2.65%；`/api/es-backtest/v2f` 新 shape（baseline + m1 + delta）；V2f tab 含对比表 + M1 标注；cache 定向 purge + 旧 shape guard — `See: task/SPEC-097.md`

- `SPEC-096` — Frontend Display Governance: ROE Denominator Unification + V2f Tail Risk Warning. Closed **DONE** 2026-05-10. AC1-AC9 全部 PASS：Q041 Ann ROE 修正为 $500k account-level（AMZN 0.24% / GOOGL 0.35% / SPX 3.0%，AC3 窗口差异 Quant 核实合理）；所有 ROE label 加 "(acct.)"；V2f tail risk warning block（黄色 banner，始终可见，-17% single / -47% cluster）插入 renderV2fPanel()；cache 刷新；回归 PASS — `See: task/SPEC-096.md`

- `SPEC-093` — Q041 Strategy Matrix + Backtest Page Redesign. Closed **DONE + 部署 old Air** 2026-05-10. AC1-AC6 全 PASS；Quant 对齐验证 PASS（Tier 状态 / tail caveat / Q041↔Q042 零交叉影响）；paper fail-soft 行为合理。C1-C12 全部实现；路由 `/q041` + `/q041/backtest` + nav 入口；回归 PASS — `See: task/SPEC-093.md`

- `SPEC-094` — Q042 Drawdown Overlay: Dual-Sleeve ATM/+5% Call Spread DTE 90. Closed **DONE + DEPLOYED** 2026-05-10. **Paper trading active on old Air.** AC1-AC20/AC22-AC23 全 PASS；AC21 reproducibility 修复；F5 Telegram-only（删 AC15）；F4 ✅ PASSED retroactive。**Release-critical bug caught at dry-run**：`_fetch_nlv()` Schwab key `net_liquidation_value` → `net_liquidation`（静默失效，每次 trigger 被吞，NLV→$0→0 contracts；fix commit on old Air）。`collect_chains.py` strike_count 100→160（commit 2e4142b，覆盖 +5.4% OTM）。**Quant 监控时间线**：HIGH_VOL trigger 当日 F4 re-validation（Quant 自主）；2026-08-10 3mo 早期信号；2026-11-10 6mo 正式评审（WR vs A 64% / B 100%）；2027-05-10 12mo cap + Tier 4 — `See: task/SPEC-094.md`

- `SPEC-095` — /ES P2 V2f Upgrade. Closed **DONE** 2026-05-10. **Quant Review PASS 2026-05-10.** AC1-AC11 全部 PASS（独立复跑）：`run_phase2_v2f()` 1170 trades，Ann ROE +2.55%（baseline），worst -9.24% NLV，bootstrap sig_rate 1.0；cold start 15.7s；fail-soft；V2f tab + 对比卡 + caveat banner；`es_params.py` 未改；回归 PASS。两处 SPEC drift 均审核接受：落点 `es_backtest.html`（非 es.html）；API default mode=baseline（与 V2f 归档研究口径一致，trend filter 在 ladder 下 net negative +1.28% vs baseline +2.55%）。数字微调：-0.12pp alpha 换 +1.72pp tail（MAX_SLOTS=5 binding），设计意图忠实实现。**Caveat 文字升级（~2-3% → ~17-25%）demoted to future optional SPEC**（non-blocking，不阻碍当前 DONE 状态）— `See: task/SPEC-095.md`, `RESEARCH_LOG.md R-20260510-04`
- `SPEC-072.1` — HIGH_VOL Aggregate Scale Coverage Extension. Closed **DONE** 2026-05-09. Q029 Tier 1 scan 结论：`qty=1` hardcoding 不需要 engine 重写；SPEC-072.1 补全 reporting 覆盖漏洞：`matrix.html` regime-aware + HV dual-est、`portfolio_backtest.html` structural prevention、CSV export 3 新列、`QUANT_RESEARCHER.md` + `REVIEW_TEMPLATE.md §6.1` governance 条目。PM live smoke pending（non-blocking）— `See: task/SPEC-072.1.md`
- `SPEC-092` — Q019 VIX Flip Day Overlay. Closed **DONE** 2026-05-09. AC1-AC5 PASS。实现落点为 `web/templates/backtest.html`（SPX Price + Trade Entry/Exit 图实际位置，非 spec 中误写的 es.html）。`/api/q019/flip-days` fail-soft endpoint；toggle 默认 OFF；flip day 半透明 overlay 在 trade marker 下层；hover 详情卡含双信号 VIX + 推荐 + elapsed_min；图例更新。SPEC-091 回归 PASS — `See: task/SPEC-092.md`
- `SPEC-091` — Dual-Signal Daily Recommendation (Q019 Path E Sidecar). Closed **DONE** 2026-05-09. AC1-AC10 全部 PASS。Sidecar 形态：Signal 1 零改动，Signal 2 独立运行（`production/vix_settling.py`，`|ΔVIX| < 0.5`，hourly，180 min timeout）；只读 API `/api/recommendation/settling` + homepage 面板 + `data/q019_settling_log.jsonl` + launchd `com.spxstrat.signal_settling`。**6 个月 A/B 实证期开始**：下一个介入点 2026-06-09（第 1 月 retrospective）— `See: task/SPEC-091.md`
- `SPEC-090` — Q041 Daily Dual-Source Alignment Check. Closed **DONE** 2026-05-09. AC1–AC8 全部 PASS：`research/q041/daily_alignment_check.py` 计算 M1/M4/M6；JSONL append；Telegram 日报 + 阈值告警 same-day 去重；missing-data fail-soft；非交易日 skip；`com.spxstrat.q041align` 在 old Air 注册，manual kickstart exit code 0，周末 `status=skipped` 正确；现有三个 Q041 采集 job 不受影响 — `See: task/SPEC-090.md`
- `SPEC-089` — E-Trade PM Account Integration (Read-Only). Closed **DONE** 2026-05-09. AC1–AC8 全部 PASS：lazy `pyetrade` 加载 + fail-soft 语义；OAuth 1.0a 授权流 + token 持久化 + 有效性检查；balances / positions client 降级到 stale/unavailable payload；三条路由 + `/etrade/auth` callback；23:00 ET 续期任务接入 bot scheduler；token 过期告警 per-period 去重；portfolio_home 新增 E-Trade 只读区块 + 双账户合并维护金展示；回归覆盖 Schwab / portfolio_summary / bot / `/api/recommendation` 全路径 — `See: task/SPEC-089.md`
- `SPEC-088` — `/ES` Stressed-SPAN Visibility Surface. Closed **DONE** 2026-05-08. Delivers the minimum viable SPAN visibility layer for the 1-contract `/ES` live cell: entry-time static SPAN baseline, current estimated stressed SPAN, their ratio, and a stress-band / visibility status — all as a read-only display on the portfolio surface and a minimal `/api/es/span-visibility` endpoint. This is not a shared-BP governance engine; it is post-verification visibility only. Implemented as the narrowest-scope `Q012 Phase C` outcome. Data will accumulate toward the `hard revisit gate` (NLV ≥ $1.5M + at least one stress event observed) — `See: task/SPEC-088.md`
- `SPEC-087` — Portfolio Command Center — Phase 1. Closed **DONE** 2026-05-07. This delivered the first homepage-level portfolio shell on top of the narrow `SPEC-085` read-only surfaces: `/` now renders `portfolio_home.html`, the old SPX dashboard moved to `/spx`, nav was unified, and the new homepage aggregates Today’s Actions plus a minimal portfolio snapshot without changing any write path or recommendation API shape. Developer feasibility review confirmed the Telegram bot had no `/` web links to break; 37/37 tests pass — `See: task/SPEC-087.md`, `task/SPEC-087_handoff.md`
- `SPEC-086` — `/ES` Short Put Credit Stop Monitor. Closed **DONE** 2026-05-07. Implemented as a read-only monitor inside the existing Telegram `intraday_monitor` loop: WARNING fires when `/ES` put mark reaches ≥ 2× entry premium; TRIGGER fires at ≥ 3× (the SPEC-061 credit stop line). Uses escalation-only logic (same pattern as VIX/SPX alerts) and fail-soft when Schwab is unavailable; an `observed` flag ensures Schwab unavailability never produces false "cleared" alerts. AC1–AC8 all PASS, 15/15 tests pass, Quant Researcher independent review PASS. Known open item (non-blocking): Schwab `mark` field per-share unit not yet validated against a real `/ES` position — to be confirmed when the first live `/ES` position is opened — `See: task/SPEC-086.md`, `task/SPEC-086_handoff.md`
- `SPEC-085` — Multi-Sleeve Read-Only Recommendation & Visualization Surface. Closed **DONE** 2026-05-07. This was the first narrow platformization step from `Q048`: a strictly read-only support surface for post-`Q045` / `Q041` portfolio-research work. Delivered scope: independent Tier 1 / Tier 2 `sleeve_candidates[]`, minimal dual-rail portfolio summary (`SPX` live rail + `Q041` paper rail), and an F3 attribution carrier that now reads the accepted `Q041` Tier 3 artifact. Key guardrails held: no unified routing, no live write-path merge, no broker write, no scanner / bot expansion, no full multi-asset simulator, and no direct promotion of `Q041` into a production trading sleeve — `See: task/SPEC-085.md`, `task/SPEC-085_handoff.md`
- `SPEC-084` — Q045 joint `bp_target` lift for account-level ROE. Closed **DONE** 2026-05-07 and now **live on old Air production**. AC1–AC7 PASS: `bp_target_low_vol 0.10→0.15`, `bp_target_normal 0.10→0.15`, `bp_target_high_vol 0.07→0.14`; `_size_rule()` 文案从 `3% / 1.5%` 更新为 `4.5% / 2.25%`; old baseline override (`0.10 / 0.10 / 0.07`) 验证通过；ceiling 默认值 `35% / 50%` 不变；`tests/test_state_and_api.py:138` 已同步为 `15.0`; implementation review PASS. Production deploy verified on old Air: `/api/recommendation` 200 OK, live size rule now shows `4.5%`, `/api/position/open-draft` now returns `bp_target_pct = 15.0`, and `HIGH_VOL` helper path returns `14.0`. Three failing legacy tests in `test_spec_048_055` / `test_spec_056` were separately cleaned up; current regression status is `228/228 PASS`. Runtime note: old Air accepted the change by cherry-pick because the repo is locally ahead/behind and not currently fast-forward clean — `See: task/SPEC-084.md`, `task/SPEC-084_handoff.md`

- `SPEC-083` — Q041 Paper Trade Ledger and Review Support. Closed **DONE** 2026-05-05. AC1–AC10 全 PASS：独立 `data/q041_paper_trades.jsonl` ledger；`logs/q041_paper_trade_io.py`（schema / 原子写入 / budget / export / status_snapshot）；`scripts/q041_paper_ledger.py` CLI（add-csp / add-ic / close / update / budget / export-csp / export-ic / status）；F5 选用 Option B（CLI status）；`vix_gate_passed=false` 在 library 层强制拒绝 open record；env-var 覆盖实现测试完全隔离；回归 25/25 PASS — `See: task/SPEC-083.md`
- `SPEC-080` — BCD debit stop tightening (`bcd_stop_tightening_mode`). Closed **DONE** 2026-05-02. F1-F5 全部交付 + AC1-AC9 PASS：`StrategyParams.bcd_stop_tightening_mode="disabled"`；`strategy/bcd_stop.py` 新建（`BCD_STOP_DEFAULT=-0.50` / `BCD_STOP_TIGHTER=-0.35`）；`engine.py:921-933` debit stop 参数化（BCD active → -0.35；BCD disabled/shadow + 非 BCD → -0.50）；shadow log 写 `data/bcd_stop_shadow_engine.jsonl`；`tests/test_bcd_stop.py` 7/7 PASS。SPEC-077 documented debit-side `-0.50` 硬编码 defect 正式闭合。Tieout #3 验证：disabled 模式 byte-identical，SPEC-080 3y 窗口无触发 — `See: task/SPEC-080.md`, `RESEARCH_LOG.md` R-20260502-07
- `SPEC-079` — BCD comfortable-top entry filter (`bcd_comfort_filter_mode`). Closed **DONE** 2026-05-02. F1-F6 全部交付 + AC1-AC8 PASS：`TrendSnapshot` 加 `spx_30d_high` / `dist_30d_high_pct`；`strategy/bcd_filter.py` 新建；`StrategyParams.bcd_comfort_filter_mode="disabled"`；`selector.py:933` wiring（Gate 2 后、`_build_recommendation` 前）；`tests/test_bcd_filter.py` 6/6 PASS。Tieout #3 D_pt050 验证：2026-04-30 BCD entry 被 risk_score=3 拦截（VIX=14.x / dist_30d≤-1% / ma_gap>1.5pp），trade gap +6→+5。PT=0.60 3y 窗口无触发（低压力环境）— `See: task/SPEC-079.md`, `RESEARCH_LOG.md` R-20260502-07
- `SPEC-078` — backtest dashboard `annualized_roe` single-source-of-truth. Closed **DONE** 2026-05-02. AC1-AC7 全 PASS：Codex 脚本化 smoke 验证 server 三字段 + byte-identical vs JS 公式（|Δ|≤3.1e-07）；PM 浏览器 smoke 验证 normal path + fallback path（DevTools Local Overrides 触发 `console.warn` + 卡片仍展示）。19.32y 全样本 `annualized_roe=8.0358%` 与 SPEC-077 AC3 PT=0.60 rerun cross-check 自洽。P12 Fast Path 仍 deferred — `See: task/SPEC-078.md`, `task/SPEC-078_handoff.md`, `RESEARCH_LOG.md` R-20260502-05
- `SPEC-077` — `profit_target` default lift `0.50 → 0.60` + credit-side `stop_mult` wiring lock. Closed **DONE** 2026-05-02 under PM option (2): operational MC parity with documented AC3 shortfall. F1-F5 delivered; AC1/AC2/AC4/AC5/AC6 PASS. **AC3 documented FAIL**: full-sample HC rerun (`2007-01-01`, 19.32y) gives Δ ann_roe = `+0.0856pp`, ~10× below Q037 Phase 2A's `+0.91~+1.03pp` band and below AC3's `+0.5pp` threshold (sharpe non-degrade ✓). HC↔MC magnitude-gap investigation deferred until post-SPEC-080 attribution (debit-side `params.stop_mult` hardcoding was SPEC-080 scope). Cross-engine ROE comparisons must explicitly note the open gap — `See: task/SPEC-077.md`, `doc/baseline_2026-05-02/ac3_summary.json`, `RESEARCH_LOG.md` R-20260502-04

## Top Blockers

- `B1` — **CLOSED** (2026-05-07). `/ES` credit stop bot alerting is now live via `SPEC-086 DONE`. The prior gap — pure manual stop monitoring not acceptable, minimum acceptable next step being system monitoring plus bot alerting — is resolved. `notify/telegram_bot.py` now monitors the `/ES` put mark continuously and fires WARNING at ≥ 2× and TRIGGER at ≥ 3× entry premium inside the existing `intraday_monitor` loop.
- `B2` — `Q036` is no longer blocked on branch selection, missing MC adoption inputs, missing HC local planning structure, PM review of the local disabled implementation, or the first shadow deployment itself. `SPEC-075/076` are now live on old Air in `shadow`, and the remaining blocker is evidence accumulation: we have not yet observed a real Overlay-F would-fire event in runtime, so there is nothing substantive to review yet beyond health / no-side-effect checks — owner: PM / Planner / Quant — next action: observe `shadow` until the first meaningful would-fire sample set appears, then review against the protocol
- `B3` — dependency-bound legacy items (`Q001`, `Q002`, `Q003`) are still unresolved and can compete for attention if not kept explicitly ordered — owner: PM/Planner — next action: keep these explicitly behind `Q041`; `/ES` research line is now fully closed and no longer a queue item

## Open Questions Summary

- `Q066` — **CLOSED 2026-05-12（2nd Quant review PASS WITH CAVEAT）**. Aftermath vs Q042 co-firing 频率实证（19yr）+ 2nd Quant review 4 项修订全部落地。Day-level 重叠 0.9%，事件级 74-86% 异步；Greek 四维度符号反向。**语言修订**：不再用"fully orthogonal"，改用"low-overlap / non-redundant"（Greek hedge 在 PnL 层未量化）。**Co-loss failure mode** 正式列入 portfolio failure modes：live 中若观察到一次 co-fire 同向亏损，立即触发 Q067。**Q067** 作为 standing monitoring 不主动启动（触发条件：Q042 paper→live / B 样本扩充 / cap 上调 / live co-fire co-loss 出现）。PM 无需立即行动——维持双 addon — `See: task/q066_cofiring_2nd_quant_review_packet_2026-05-12_Review.md`, `doc/addon_greek_orthogonality_2026-05-12.md`, `RESEARCH_LOG.md R-20260512-02`
- `Q065` — **CLOSED 2026-05-12**. Aftermath `EXTREME_VIX=40` 阈值敏感性研究。触发：Q064 P1 中 2025-04-09 出现 1-day aftermath window，PM 质疑阈值是否过严。P2 sweep 三个放宽变体（loosen_42 / loosen_45 / peak×0.85）trap rate 全部超标（41% / 47% / 79%），2009 GFC 局部 72%，2020 COVID 40%，属真实尾部结构。结论：**`EXTREME_VIX=40` 维持，selector.py 不动，P3 永久 closed** — `See: research/q065/q065_memo_2026-05-12.md`, `RESEARCH_LOG.md R-20260512-01`
- `Q064` — **CLOSED 2026-05-13（Phase 9 final closure，APPROVE α）**. 全程 P1–P9（aftermath routing → spell reset sensitivity）。**P1–P6**：V3-A gate-bypass 价值确认，SPEC-064 保留。**P7–P9（spell reset mechanism）**：12 variants engine replay；2nd Quant APPROVE α — 仅采纳 P8（`max_trades_per_spell: 2→3`，+$11k ann），其余全拒：hysteresis -$20.7k（alpha 流失 20%），no-high-reset -$16.3k（alpha 流失 16%），spell_age_cap=90 仅 +1 trade evidence（延后）。**三个 Design Principles 固化**：vix_low_reset 单日触发 deliberate；vix_high_reset(≥35) 是 event-boundary reset 非冗余；age_cap=30 near-optimal。**方法论 learning**：spell gate 行为不能从 code structure 直觉推断，必须 engine replay（Quant 3/3 预测全错）。**待办**：SPEC-100 实现 P8（单参数，~30 行，~1h Developer）— `See: RESEARCH_LOG.md R-20260512-03, R-20260513-04`
- `Q063` — **CLOSED 2026-05-11**. SPX 主策略 IVP<55 gate robustness review。PM hypothesis「gate 在低 VIX 环境产生 false alarm」被四层检验精确反向 reject：Phase 1 低 VIX blocked entries WR 63%（vs allowed 83%）；Phase 2 候选 A 19y +$6k 但 OOS test 期 -$907/yr；Phase 4 decay-weighted 3y HL A 输 -$19k，last 5y A 输 -$13.7k（2024-2026 gate 挡住 5 笔 counterfactual -$13.7k loss）。结论：**Keep IVP<55 gate unchanged，no SPEC change** — `See: task/q063_phase4_closure_memo_2026-05-11.md`, `RESEARCH_LOG.md R-20260511-01`
- `Q002` — Shock active mode still needs Phase B validation — `open`
- `Q012` — **closed.** `/ES` shared-BP governance clarification is complete for the current live scale. Quant `Phase A+B+C` showed that at the current production posture (`1` contract on a `$500k` account), allocator / gating / priority logic is overdesigned relative to the sleeve’s size and moves account-level ROE by only about `±0.01pp`. The correct implementation outcome was narrowed to stressed-SPAN visibility / monitoring, which has now shipped via `SPEC-088 DONE`. No further shared-BP governance work is active at the current scale; if `/ES` later expands materially, reopen under `Q050` rather than under `Q012`.
- `Q013` — `/ES` short put runtime stop execution: bot alerting for the credit stop condition is now live via `SPEC-086 DONE`; remaining open item is post-entry management and Layer 1 / Layer 3 coverage. The `/ES` stop-semantics mismatch identified in the Quant audit is now closed at the parameter layer: backtest and monitoring both use `3.0x credit`, and `/ES` runtime / research parameters now share a single `EsShortPutParams` source — `open (bot alert gap closed; remaining work is post-entry / leverage scope)`
- `Q029` — **CLOSED 2026-05-09** via `SPEC-072.1`. Tier 1 scan confirmed: `qty=1` hardcoding does not require engine rewrite; reporting-layer mitigation is sufficient. SPEC-072.1 extended coverage to `matrix.html`（regime-aware + HIGH_VOL dual-est）、`portfolio_backtest.html`（structural prevention）、CSV export（3 new columns: `live_scale_factor / live_scaled_exit_pnl_usd / live_scaled_total_bp`），并将 HIGH_VOL aggregate scale convention 写入 `QUANT_RESEARCHER.md` governance 章节 + `REVIEW_TEMPLATE.md §6.1` — `See: task/SPEC-072.1.md`, `RESEARCH_LOG.md R-20260509-10`
- `Q020` — MC `backtest_select` simplification produced a low aftermath artifact count for `SPEC-064 AC10`; HC artifact count `49` is canonical, this entry tracks MC-side housekeeping only — `MC-side housekeeping`
- `Q037` — `profit_target = 0.60` broad-rule lift is now operationally aligned via `SPEC-077 DONE`. The remaining HC↔MC AC3 magnitude gap is now explained enough to stop treating it as a high-priority mystery: HC is not showing a metric bug, and the residual gap is best read as a mixed effect of metric/denominator semantics plus sample-window / strategy-path differences. What remains is a narrow attribution note / MC tie-out follow-up, not another broad HC rerun campaign — `open (de-escalated)`
- `Q038` — `BCD comfortable top` + `BCD stop tightening` Path C is now reproduced on HC via `SPEC-079/080 DONE`, and HC has already flipped the runtime posture to `shadow` on old Air. Quant has now fixed the monitoring-governance objective: shadow is for validating trigger semantics, auditability, false-positive risk, and no-side-effect behavior in live runtime, not for proving alpha. `SPEC-079` is the live-observable branch; `SPEC-080` should be treated as engine/backtest-observable shadow plus live posture alignment, not as a source of natural live stop events — `shadow-live / protocol fixed`
- `Q039` — **resolved (2026-05-09)**. Post-SPEC-084 refresh 确认：HC 13 / MC 6 gap attribution 不变（9/11 = high-IVP fallback/gate，1/11 valid MC-missing，1/11 low-IV bug candidate，0/11 slot blocking）；SPEC-084 bp_target lift 与 IC regular selector 正交，不影响选股逻辑。Attribution 充分，不升级为完整 parity 调查 — `See: RESEARCH_LOG.md R-20260509-08`, `doc/q039_ic_regular_mini_attribution_note_2026-05-03.md §12`
- `Q041` — `Large-Cap Equity Option Income Overlay` — **Phase 1 COMPLETE (2026-05-04); Phase 2 COMPLETE (2026-05-05); 2nd Quant PASS Routing B (2026-05-05); Tier 3 portfolio-attribution prototype accepted (2026-05-07)**. The candidate hierarchy remains: Tier 1 `SPX CSP Δ0.20 DTE30`, Tier 2 `GOOGL/AMZN CSP` tail-caveated, Tier 3 `COST/JPM` earnings IC observe-only. Overlap validation cleanup is materially closed: same-day `M4` is a **STRONG PASS**, `M3` and `M6/M7/M8` are repaired/confirmed, `M9` semantics are corrected, and all three old Air data flows are loaded. The major posture change is that `Q041` now has an accepted portfolio-value artifact: `idle_day_capture = 132 days (86.8% of J3 idle)`, `delta_avg_bp = +22.21pp`, `joint mean BP = 38.04%`, and only mild excess tail overlap (`+4.5pp` vs occupancy). PM has accepted `data/q041_portfolio_attribution_latest.json` as the formal `SPEC-085 F3` source, declined the old 12-month paper-trading path as the main route, and also declined a generic `4–6` week live signal forward-tracking gate. Current state is therefore: **overlap monitoring continues on its own admission track, while visualization / attribution becomes the primary support path for Q041’s portfolio-deployment case** — `See: doc/q041_portfolio_attribution_results_2026-05-07.md`, `data/q041_portfolio_attribution_latest.json`, `doc/q041_execution_prep_packet_2026-05-05.md`, `doc/q046_bp_utilization_external_benchmark_seed_memo_2026-05-07.md`
- `Q042` — `Directional Drawdown / Reversal Overlay` — **DEPLOYED via SPEC-094 (2026-05-10) + SPEC-094.1 (2026-05-10)**. Dual-sleeve directional overlay on dd4 lenient / dd15 + MA10 reclaim 触发；ATM call vertical spread cash-settled European SPX。Sleeve A：D30/2.5% (SPEC-094.1)。Sleeve B：5%/D90 (unchanged)。10% per-sleeve sizing, A+B ≤ 20% combined cap。Q062 完成结构/strike/DTE 全 grid 优化（84 cells）+ Tier 3 robustness + Pareto + decay-weighted 分析；Bootstrap p=0.09 marginal 但 P(C>A)=91% strong directional evidence。**Paper trading active on old Air**；首次 Sleeve A live trigger 2026-03-12（baseline，grandfather 至 2026-06-11）；首次 SPEC-094.1 trigger 待。Monitoring 节点：2026-08-10（3mo 早期）/ 2026-11-10（6mo 正式评审 + bootstrap 重测）/ 2027-05-10（12mo cap upgrade + Tier 4 TP/stop 研究）— `See: task/SPEC-094.md`, `task/SPEC-094.1.md`, `task/q062_tier1_memo_2026-05-10.md`, `RESEARCH_LOG.md R-20260509-11~15, R-20260510-15`
- `Q045` — `Account-Level ROE Optimization Across Strategy Matrix` — **Tier 3 complete; implemented via `SPEC-084 DONE`, now live on old Air**. Reframed Q036/Q044/SPEC-077 piecemeal work into a single joint optimization. Phase 1 baseline showed system uses only 11% avg BP out of 35% NORMAL ceiling. Phase 2A/B/C identified joint optimum J3 (NORMAL bp=15%, HIGH_VOL bp=14%): +6.12pp on 3y window, **+5.48pp on 19y window**, Sharpe IMPROVES (1.78→1.83), all 6 strategies contribute positive uplift, peak BP 43% within HIGH_VOL ceiling 50%. Perfect additivity (NORMAL+HIGH_VOL = sum, no interaction). Worst trade scales 1.57x (-5.6%→-8.8% account). Phase 2D shows ~19pp BP still idle after J3 — now addressed by the `Q041` deployment-efficiency lane via attribution / visualization rather than by long paper-trading as the default path. `SPEC-084` kept the landing shape intentionally narrow: three `bp_target` default lifts plus `_size_rule()` text sync and explicit risk disclosure. **Supersedes Q044 (BPS-only); partially supersedes Q036 Overlay-F's value-add (Q045 J3 captures 26x more on same 19y sample)** — `See: task/q045_pm_decision_packet_2026-05-06.md`, `task/SPEC-084.md`
- `Q046` — `External Benchmarking of BP Utilization and Account-Level Deployment Efficiency` — **benchmark + mechanism map complete (2026-05-07)**. Quant normalized the external `25%–30%` / `25%–50%` practitioner headlines against our PM defined-risk book and concluded the real post-`SPEC-084` gap is only about `~5–10pp`, not `15pp+`, relative to a conservative-to-neutral PM peer. The key planning result is not “raise live size again,” but “promote mechanism C.” Of the four candidate families — more sizing, more concurrency, broader strategy breadth, higher ceilings — only **C (`Q041`)** can directly attack the remaining `17%` fully idle days. A/B remain constrained by cliff / concentration; D is an enabler, not a standalone axis. This closes `Q046` as a framing study and elevates `Q041` into the main post-`Q045` deployment-efficiency lane — `See: doc/q046_bp_utilization_external_benchmark_seed_memo_2026-05-07.md`

- `Q048` — `Portfolio-State Architecture Transition Plan` — **open (planning / architecture item)**. Planner review, with Quant-side and Developer-side architecture input, concluded that the project is now crossing from a single-SPX execution platform toward a portfolio research platform. The current runtime and UI still assume one canonical recommendation, one current live position, and one SPX-centered backtest universe, while `Q041` already operates as a separate multi-record paper ledger rail. `Q048` is the planning lane for deciding what must be abstracted first: portfolio-state model, unified live/paper/observe-only ledger semantics, candidate-set vs selected-action recommendation split, minimal portfolio summary surface, and attribution-first research interfaces. This is explicitly **not** a rewrite Spec yet; the immediate goal is staged transition design while keeping the SPX live rail stable and letting `Q041` continue as the evidence-accumulation rail — `See: doc/q048_portfolio_state_architecture_transition_plan_seed_memo_2026-05-07.md`, `RESEARCH_LOG.md`
- `Q050` — `Portfolio-Level Shared-BP Governance Framework` — **open (research seed; not ready for DRAFT Spec)**. This is the higher-order lane created to separate “current-scale `/ES` implementation” from the broader portfolio-governance question. `Q012 Phase C` showed that at the current live size (`1` `/ES` contract), a true shared-BP governance engine is overdesigned and the correct next implementation is only stressed-SPAN visibility. `Q050` preserves the larger PM request: when the system materially evolves into a multi-sleeve book (`/ES`, SPX Credit, `Q041`, future sleeves), what should be the account-level governance philosophy for scarce PM BP, what sleeve taxonomy should exist, what scale triggers justify real allocator logic, and what belongs to research governance versus platform behavior? This lane is intentionally research-driven and should not be forced into `SPEC-088` or any current narrow implementation — `See: doc/q050_portfolio_shared_bp_governance_framework_seed_memo_2026-05-08.md`
- `Q051` — `/ES` Performance Salvage Under Honest Assumptions — **closed.** Final combined conclusion: the original `/ES` thesis is statistically valid only in its larger full-system form, but it is **scale-dependent** and not production-plausible at the current `$500k` account size. Conservative leverage-table recalibrations failed to recover significance without unacceptable SPAN concentration. The current `1`-contract `/ES` deployment should therefore remain a live-data / visibility / operational-calibration cell rather than an active ROE engine. Active research resources should redirect to `Q041`; any future `/ES` redesign work now belongs to `Q052`, not to `Q051` — `See: doc/q051_es_performance_salvage_seed_memo_2026-05-08.md`
- `Q055` — **CLOSED 2026-05-10**（竞争裁决：/ES V2f 胜出；SPX CSP T1 V1 veto 淘汰）— `See: task/q055_naked_put_competition_protocol_2026-05-10.md`
- `Q056` — `/ES P2 V2f 升级（true ladder + STOP=15）` — **SPEC-095 APPROVED 2026-05-10**。V2f Ann ROE +2.67%，worst -10.96% NLV（V1 PASS），bootstrap **100% 种子显著**；backtest 层 `run_phase2_v2f()` + `/api/es-backtest/v2f` + V2f tab；生产零改动 — `See: task/SPEC-095.md`
- `Q057` — V2f 定价 Massive sanity check — `open (research，可与 Q056 SPEC 并行)`
- `Q058` — BSH 在 V2f 框架下的角色 — `open (low priority，V2f SPEC 进入后讨论)`
- `Q059` — NLV ≥ $2M 时重做 /ES vs SPX CSP 竞争 — `open (future trigger)`
- `Q052` — `/ES` Defensive Redesign — **CLOSED.** All three PM redesign hypotheses tested and failed comprehensively: H1 (technical exits) and H3 (deep OTM / longer DTE) both produced negative or non-significant AnnROE with failing bootstrap CIs; H2 (roll-out) was deferred but reasoned to fail by extension of H1's root cause. Root cause is structural: **IV expansion reprices tail risk faster than any lagging control system can respond.** This invalidates not just the current `/ES` variants but applies broadly to any small-scale naked short option strategy relying on lagging price-based exits. The `/ES` research line is now comprehensively closed across `Q012 / Q051 / Q052` at the current `$500k` account scale. Hard revisit gate: account NLV ≥ $1.5M (preferably $2M), SPEC-088 has accumulated meaningful SPAN observations across at least one stress event, PM willing to evaluate dynamic leverage + BSH as one inseparable system — `See: RESEARCH_LOG.md R-20260509-01`, `doc/q052_es_defensive_redesign_seed_memo_2026-05-08.md`
- `Q053` — `Grinding-Decline Regime Review` — **FINAL CLOSED 2026-05-09**（Tier 1→2→3→3+ 全部完成；2nd Quant APPROVE on Tier-3+）. 信号宇宙近场已穷尽：简单 VIX/SPX 信号（Tier 3，6 candidates）全部 fail，VIX term structure / VIX-VIX3M spread（Tier-3+，8 variants）全部 fail。Tier-4 候选（HY spread / rates curve / VVIX / breadth）理论上对 grinding 更有希望，但仅在 standing trigger 激活时开启。C3（regime-conditional put-side filter）保留为 standing architectural candidate，含 4 条 revisit trigger（Planner 月度监控 Trigger 3）。永久遗留：`research/tools/iv_expansion_stress_test.py`、5 条 Short-Premium Principles、REVIEW_TEMPLATE §6.1、Trade.pnl_pct 修复、完整 8 条 RESEARCH_LOG 负面结果档案（R-20260508-09 → R-20260509-07）— `See: RESEARCH_LOG.md R-20260509-07`, `sync/open_questions.md Q053 §revisit`
- `Q054` — `Unusual Whales Alpha Research` — **open (future seed / low priority)**. PM 持有订阅。候选方向：options flow 作为 entry timing 信号；dark pool prints 作为方向性 filter；institutional positioning 辅助 Q041 sleeve 判断。前置：先确认数据格式和历史回溯范围（Tier 0），再决定研究方向。优先级低于 Q041 运营 / Q039 / Q021 — `See: sync/open_questions.md Q054`
- **Standing Principles** (governance reference, not an open question) — 5 must-absorb + 3 calibrated cautions from `/ES` closure have been codified in `sync/open_questions.md`. Key anchor: *IV expansion fires faster than any lagging control; risk management must be entry-gated and stress-capital-aware, not exit-driven.* Seven action items (corrected; original mapping missed 4 governance codification actions): A1 `iv_expansion_stress_test` tool (pending); A2 Q053 (open); A3 Q041 IV-stress appendix (pending activation); A4 QUANT_RESEARCHER.md principles (done); A5 Q036 revisit gate (done); A6 REVIEW_TEMPLATE stress-capital (done); A7 REVIEW_TEMPLATE exec-drift (done) — `See: sync/open_questions.md "Standing Principles"`, `RESEARCH_LOG.md R-20260509-02`
- `SPEC-085` — `Multi-Sleeve Read-Only Recommendation & Visualization Surface` — **DONE**. The narrow `Q048` support surface is now implemented: read-only Tier 1 / Tier 2 `sleeve_candidates[]`, minimal portfolio summary across SPX live rail + Q041 paper rail, and F3 attribution carrier consuming the accepted `Q041` artifact. It remains explicitly out of scope for unified routing, live write-path merge, broker write integration, scanner/bot expansion, full multi-asset simulation, or direct promotion of `Q041` into a production trading sleeve — `See: task/SPEC-085.md`, `task/SPEC-085_handoff.md`

- `Q044` — `BPS Spread Sizing and Account-Level ROE` — **superseded by Q045 J3**. Q044 Tier 1/Tier 2 work preserved as evidence base. Q044 A1 (BPS-only +1.51pp) is a strict subset of Q045 J3 (account-wide +5.48pp). Status: closed as superseded. A1 (bp_target 15%, same δ0.30/0.15 structure) cleared all Tier 2 checks: Sharpe unchanged (0.91), peak concurrent BP% unchanged (30%), 3-year attribution uniform, Q036 coexistence safe (peak 29% vs 50% ceiling). AnnROE +1.5pp (3.0%→4.6%), marginal decay -0.3%. Key risk: worst trade rises to -6.25% acct (from -4.17%). Axis B (wider spread) closed. Bonus finding: A2 (20%) with ceiling 40% generates $27,332 > A1 $22,823 — deferred secondary direction — `See: doc/q044_bps_tier2_results_2026-05-06.md` Current BPS single-position target is 10% account BP (NORMAL regime), leaving ~25% structural idle BP below the 35% ceiling. Question: can BPS spread width or position size be increased to improve account-level ROE without unacceptable tail-risk cost? Key dependencies: Q036 Overlay-F active decision (competing for same idle BP), and empirical confirmation of actual Schwab PM BP consumed per BPS contract. Not active queue; requires Tier 1 quick scan before any Spec discussion — `See: doc/q044_bps_sizing_roe_seed_memo_2026-05-06.md`
- `Q043` — `Q041 Scanner and Bot Support Seed` — future support branch only. This is a preserved idea for a read-only `Q041` candidate scanner plus dev-bot / reminder support after the `SPEC-085` visualization / attribution surface stabilizes. It is intentionally **not** part of `SPEC-083`, not an active implementation item, and not a license to merge `Q041` into the main SPX recommendation engine. A Developer planning prompt has been written covering Phase A (shadow scanner + audit log), Phase B (dev-bot / shadow notification), and Phase C (paper-trade reminder support). Use it later for a narrow shadow-scanner spec if PM chooses to promote it — `See: doc/q043_q041_scanner_and_bot_seed_memo_2026-05-05.md`, `doc/q043_developer_planning_prompt_2026-05-05.md`
- `Q036` — the HC↔MC direction-level disagreement is now resolved: PM has accepted the MC-side escalate / productization-stack direction for `Overlay-F`, MC has supplied the `SPEC-075/076` adoption package, Quant has clarified HC-local guardrails, and Developer has completed both the HC-local implementation and the first old Air `shadow` rollout. Current HC posture is: adopt MC’s core structure, preserve `disabled -> shadow -> active`, implement as one batch with two separate units (`SPEC-075` first, `SPEC-076` second), and treat `web/templates/index.html` as the formal HC mapping for MC’s `web/html/spx_strat.html`. Runtime is now on `overlay_f_mode = "shadow"`; `active` remains unapproved — `shadow-live / observation pending meaningful would-fire samples`
- `Q019` — **SPEC-091 deployed (sidecar, 2026-05-09)**。6 个月 A/B 实证期：Signal 1（现有开盘推荐）vs Signal 2（stable-VIX，`|ΔVIX| < 0.5`，180 min timeout）并行运行，实证数据累积到 `data/q019_settling_log.jsonl`。Quant 监控基线：stable 触发率、timeout 率、两信号差异频率、recovery rate。上线 gate（4 条）待满足后由 2nd Quant review 决定 Signal 2 是否升为 binding。下一介入：**2026-06-09** — `monitoring active`
- `Q032` — after MC selected aftermath broken-wing `V3-A` (`LC 0.04 / LP 0.08`), `V3-C` (`LC 0.03`) stays as a monitor-only revisit candidate after `5–10` live aftermath trades — `monitoring`
- `Q011` — regime decay DIAGONAL sample is still small — `monitoring`
- `Q003` — L3 Hedge v2 live implementation — `open`
- `Q004` — `vix_accel_1d` L4 fast-path — `open`
- `Q005` — multi-position trim refinement — `open`

## Next Priorities

- `P1` — formalize the new top-level objective in ongoing research work: reasonably maximize account-level ROE under explicit risk guardrails, not just improve rule-local efficiency metrics
- `P1` — monitor the first post-`SPEC-084` planning consequences rather than reopen the spec: keep Q045 closed at implementation level unless PM requests a rollout / observation layer or a new ceiling study
- `P1` — treat `Q046` as complete framing work and promote `Q041` as the next account-level ROE mechanism axis. The next real question is no longer “which family should we study?” but “how far can the accepted `Q041` attribution artifact + `SPEC-085` surface carry the deployment-efficiency case before any narrower realism follow-up is needed?”
- `P1` — keep the shipped `/ES` stack (`SPEC-061/086/088`) in its correct posture: a `1`-contract live-data / visibility / operational-calibration cell. No new active `/ES` thesis implementation is queued at the current account scale.
- `P1b` — keep `Q050` open as the explicit long-horizon research lane for full portfolio-level shared-BP governance. This is not a current implementation target; its function is to prevent future global allocation questions from being smuggled into narrow `/ES` monitoring work.
- `P1-archive` — **`/ES` research line COMPREHENSIVELY CLOSED** (Q012/Q051/Q052 all closed; R-20260509-01). Key archive constraints: (1) `SPEC-061/086/088` 1-contract cell stays live as data-collection only; (2) `strategy/es_params.py` / `EsShortPutParams` stays as the single-source layer — do not redisperse parameters; (3) hard revisit gate = NLV ≥ $1.5M + meaningful SPEC-088 SPAN data + PM accepts dynamic leverage + BSH as inseparable system; (4) structural insight for all future naked short research: *IV expansion reprices tail risk faster than any lagging control system can respond* — this applies to XSP and any similar strategy, not just `/ES`.
- `P1` — **Q055 竞争结论（2026-05-10）：/ES V2c 胜出，SPX CSP T1 正式淘汰**。三层协议执行结果：B（SPX CSP T1）在 V1 veto 失败（2020 worst -17.99% NLV > -15% 阈值）；A（/ES V2c）在所有 Tier 2 主指标全胜（Ann ROE 1.28% vs 0.44%，$/BP-year 6.61% vs 4.67%，worst % NLV -10.96% vs -17.99%）。$500k 账户 naked put 槽归 /ES V2c。**下一步：PM 决定是否授权 /ES V2c 进入 SPEC 评审**（true ladder + STOP=8 实现升级）。Q041 P2（GOOGL/AMZN CSP）独立继续 — `See: task/q055_naked_put_competition_protocol_2026-05-10.md`
- `P1` — C3 standing revisit gate 由 Planner 月度监控（Trigger 3：rolling 3-month put-side PnL < −1.5× MAD in medium-VIX grinding 条件）。Trigger 4（Q041/Q036 部署使 short-premium 暴露显著上升）现已激活——Q041 Tier 1 paper-trading 开始后 Planner 应在月度检查中跟踪 C3 Trigger 4 状态。
- `P2` — keep the `Q041` overlap-validation program running through the formal `20` trading-day window. `SPEC-081` / `SPEC-082` are closed, same-day `M4` is now a strong pass, and the three old Air data feeds are scheduled. The current hard check is not “can we collect the data?” but “does the stitched Massive historical + Schwab forward path remain stable day after day in the liquid subset and month-roll transitions?” Focus the remaining work on daily monitoring, weekly reconciliation summaries, and final PM-facing Reconciliation Report packaging rather than on collector plumbing
- `P2` — keep the `Q041` **overlap-validation / reconciliation** program running as its own admission track for the stitched dataset. Current evidence says the stitched path is converging in the economically relevant subset, and the main governance warning is now interpretive: do **not** treat raw full-chain denominator mismatch or the current non-same-day `M4` tail as a formal data blocker. Continue the clock, fix the cleanup issues, and let overlap remain separate from candidate routing
- `P2` — for `Q041`, Phase 2 research execution is now complete. The immediate next work is no longer `P0-2 / P1-1 / P1-2`; it is **attribution-backed support surface + candidate governance**, now with a stronger mandate after `Q046` and the accepted Tier 3 artifact: `(1)` preserve `SPX CSP Δ0.20 DTE30` as the leading formal candidate, `(2)` keep `GOOGL / AMZN CSP` as tail-caveated secondary candidates without assuming a broad forward-tracking gate, `(3)` keep `COST / JPM` earnings iron condors as observe-only, `(4)` carry `VIX >= 15` as the current best next-step governance filter candidate for earnings-IC work, and `(5)` let `SPEC-085` consume the accepted attribution artifact while overlap validation continues independently
- `P4` — keep `Q044` closed as **superseded by Q045**. Do not reopen the BPS-only sizing lane unless PM explicitly rejects the joint `Q045` route and asks for a narrower fallback study.
- `P5` — clear the narrow pre-existing test debt around removed Gate 1 expectations (`tests/test_spec_048_055` / `tests/test_spec_056`) in a maintenance-only cleanup. This is unrelated to `SPEC-084` and should not be treated as a regression in the new bp-target lift.
- `P4` — keep `Q042` explicitly out of the active queue. It is now indexed as a future directional-overlay seed so the idea is preserved, but it should not displace current `Q041 / Q036 / Q038 / /ES` work unless PM later promotes it
- `P4` — keep `Q043` out of the active queue for now. It is a useful next-stage support idea for `Q041`, but only after the `SPEC-085` read-only surface and overlap-validation cleanup are stable; do not let it expand `SPEC-083` retroactively or merge into the main SPX bot path too early. Developer planning prompt is now written and indexed — `See: doc/q043_developer_planning_prompt_2026-05-05.md`
- `P2` — run the first `Q036` / `SPEC-075/076` shadow observation window on old Air: keep runtime in `shadow`, watch for the first real Overlay-F would-fire samples, confirm telemetry / artifacts when those samples arrive, and evaluate no-side-effect behavior before any future `active` discussion
- `P3` — run `Q038` shadow observation under the fixed Quant protocol: review `data/bcd_filter_shadow.jsonl`, runtime health, `risk_score` distribution, and recommendation side-effects weekly; treat `SPEC-079` as the actual live-observable signal and `SPEC-080` as posture alignment unless a separate live stop-monitoring scope is opened
- `P4` — ask PM to choose whether `Q019` stays deferred or advances via one of MC’s proposed paths `A / B / C`; until then, keep the new close/open VIX evidence indexed but non-binding
- `P5` — finish the narrow post-sync attribution wrap-up: record the `SPEC-077 AC3` gap as explained enough for planning purposes, then use the MC 6-trade IC ledger to build only a compact `Q039` mini attribution table rather than widening into an IVP sweep
- `P6` — `/ES` credit stop bot alerting is now live (`SPEC-086 DONE`, B1 closed); the first real `/ES` live position should still be used to validate the Schwab `mark` field unit and calibrate stressed-SPAN visibility against real broker behavior. That runtime verification remains separate from the upcoming narrow `Q012` monitoring spec.

## Recent Meaningful Changes

- 2026-05-09 — **Q053 FINAL CLOSED**（R-20260509-07）。Tier-3+ 测试 8 个 VIX term structure / VIX-VIX3M spread variants 全部 fail；2nd Quant APPROVE。信号宇宙近场已穷尽——Tier-4（macro risk-off：HY spread / rates curve / VVIX / breadth）理论上更有针对性，但仅在 standing trigger 激活时开启。本 session 完整工作链闭合：/ES Q012/Q051/Q052（5 轮）→ absorption（5 principles + 7 actions）→ A1 stress test 工具 + A3 Q041 appendix → Q053 Tier 1-2-3-3+（全部穷尽）→ C2 engine bug 反转 + Trade.pnl_pct 修复 → 2nd Quant reviews × 2 → 7/7 governance actions 落地 — `See: RESEARCH_LOG.md R-20260509-07`
- 2026-05-09 — **Q053 CLOSED — 2nd Quant APPROVE WITH ADJUSTMENTS 全部落地**（R-20260509-06）。最终结论：主策略 grinding-decline 弱点已量化实证（2022 亏损 −$26.8k），但简单信号家族无法 cleanly 分离（Tier 3 全候选 fail）。C3（put-side suppress on grinding regime）作为正确架构方向保留，4 条 revisit trigger 已写入索引。三项 2nd Quant 校正已应用：entry_credit consumer audit（5 个 consumers 文档化，2 个 display-side naming ⚠️）、C3 revisit trigger 条件修订（4 条 OR 逻辑，含 Planner 月度自动检查）、wording 校正（Principle 1/2 范围精确化，§6.1 豁免条款）。270/270 tests PASS，6 files changed。`/ES` 相关全线（Q012/Q051/Q052 + Q053）正式闭合 — `See: RESEARCH_LOG.md R-20260509-05/06`
- 2026-05-10 — **SPEC-094 修订：SPX-only + F4 简化**（R-20260510-01）。XSP/SPX 双路径简化为 SPX-only（NLV ≥ $200k），减少 F2/F4/F5/F6/F8 实施工作量；F4 tie-out 从 5 日缩为 3 日（Mon/Wed/Fri 2026-05-11/13/15）+ Massive cross-check；单日 PASS 2.5%；XSP 路径保留为 NLV < $200k 时的未来 revisit。Quant 这一棒完成，待 Developer 实施。
- 2026-05-10 — **Q055 裁决完成：/ES V2c 胜出，SPX CSP T1 正式淘汰**。三层竞争协议执行结果：SPX CSP T1 在 V1 veto 失败（2020 COVID worst trade -17.99% NLV > -15% 阈值）；/ES V2c Tier 2 三项全胜（Ann ROE 2.9×，资本效率 +41%，worst NLV 减半）。ladder 结构（5.6× BP 周转）是 $500k 真实可部署的关键。B 的 per-contract $/year 更高但无法 ladder，诚实披露。$2M+ 账户时需重做比较。Q041 T1 paper-trading 正式取消；Q041 P2（GOOGL/AMZN）独立继续 — `See: task/q055_naked_put_competition_protocol_2026-05-10.md`
- 2026-05-10 — **SPEC-094 最终配置锁定**（R-20260509-15）。Q042 Drawdown Overlay 双 sleeve 最终方案：Sleeve A（dd4 ddATH_lenient，无 MA filter）+ Sleeve B（dd15 ddATH_lenient + MA10 reclaim），两者完全独立 armed state，组合 cap 20%（governance 公式）。结构：ATM/+5% call spread DTE 90，T+1 open，hold-to-expiry，XSP/SPX 按 NLV 切换。19y 组合回测 +140%（~7.2% Ann ROE），max combined BP 20%（109 天）。**F4 tie-out 下周 05-11-15 自动收集**（单日 PASS 2.5%，5 日中位需 < 15%）。Quant 这一棒完成；下一棒：Developer 实施 + F4 tie-out + paper trading 部署；6 个月后 Quant Tier 4 retrospective — `See: task/SPEC-094.md`
- 2026-05-09 — **Q041 T1 vs /ES 治理审查完成**（`task/q041_t1_es_governance_review_archive_2026-05-09.md`）。关键结论：(1) Q041 T1 SPX CSP 与 /ES 在数据层本质同质（共享 BS-flat 合成 SPX 数据），在 $500k 账户下 Q041 T1 单槽 alpha +0.44% << /ES V2c +1.29%，T1 paper-trading 暂缓；(2) /ES P2 当前实现是 fixed-slot，与 spec 设计的 true rolling weekly ladder 结构不同——这是最重要的方法论发现；(3) True ladder + STOP_MULT=8 (V2c) 把 /ES P2 从 +0.45% 推到 +1.29% Ann ROE，borderline 显著（75% seed stability）；(4) STOP_MULT=3.0 在 true ladder 下是 alpha 杀手（-4.93pp）；(5) Q041 P2 其他候选（GOOGL/AMZN CSP、COST/JPM IC）不受影响。**3 项 PM 决策 pending** — `See: PROJECT_STATUS.md §Open Questions Summary`, `task/q041_t1_es_governance_review_archive_2026-05-09.md`
- 2026-05-09 — **Q042 Tier 3 DONE — SPEC-094 DRAFT ready**（R-20260509-14）。6 项 2nd Quant adjustments 全部落地并实证验证。关键发现：DTE 90 完胜 DTE 30（WR 80 vs 73%，hit-rate 73 vs 51%，drift-tolerance 5×）；dd15 naive + no-overlap 与 dd12+reclaim 并列 lead（filter 后 WR 82-90%）。SPEC-094 含 F1-F9 / AC1-AC18 / F4 部署硬 gate（5 日 broker-API tie-out）。3 项 PM 决策 pending：SPEC-094 approve / revise / reject；Trigger A or B 先 paper-trade；live data tie-out 时间窗（建议下周一开市后启动）— `See: task/SPEC-094.md`, `RESEARCH_LOG.md R-20260509-14`
- 2026-05-09 — **SPEC-072.1 DONE + Q029 CLOSED**。Q029 Tier 1 scan 确认 engine `qty=1` 不需要重写；SPEC-072.1 补全 SPEC-072 三处覆盖漏洞：`matrix.html` HV dual-est、`portfolio_backtest.html` structural prevention、CSV export 3 新列。HIGH_VOL aggregate scale convention 写入 `QUANT_RESEARCHER.md` + `REVIEW_TEMPLATE.md §6.1`。PM live smoke pending（non-blocking）— `See: RESEARCH_LOG.md R-20260509-10`
- 2026-05-09 — **SPEC-091 DONE — Q019 Sidecar 部署完成，6 个月 A/B 实证期开始**。`production/vix_settling.py` 上线：Signal 1 零改动，Signal 2 独立 sidecar（hourly 轮询，`|ΔVIX| < 0.5`，180 min timeout → 12:30 ET）。每日写 `data/q019_settling_log.jsonl`；stable 时发 Telegram 确认 / 差异；Web UI homepage 双信号面板；launchd `com.spxstrat.signal_settling` 在 old Air 注册。Sidecar 架构价值：Signal 1 仍是 binding decision，Signal 2 积累 130 天真实 live 对比数据，比 paper trading 更有说服力，比直接切换 Signal 1 更安全。下一 Quant 介入：**2026-06-09**（第 1 月 retrospective）；如连续 3 天 timeout 异常提前联系 — `See: task/SPEC-091.md`, `RESEARCH_LOG.md R-20260509-09`
- 2026-05-09 — **SPEC-089 DONE**：E-Trade PM 账户只读集成上线。AC1–AC8 全部 PASS；lazy pyetrade 加载 + fail-soft；portfolio_home 新增 E-Trade 区块 + 双账户合并维护金展示；token 续期 bot 任务 + 过期 Telegram 告警。回归覆盖 Schwab / bot / recommendation 全路径 — `See: task/SPEC-089.md`
- 2026-05-09 — **A1/A2/A3 全部完成**，7/7 actions 落地。A1：`research/tools/iv_expansion_stress_test.py` 建成（支持 naked_put / CSP / BPS / IC，自验证 PASS，复现 Phase A /ES SPAN 模型）。A3：Q041 stress visibility 附件写入 `doc/q041_execution_prep_packet §11`（Tier 1 SPX CSP +40 shock = 12.3% NLV mark loss / 27.2% stress BP；Tier 2 GOOGL/AMZN pnl_ratio FAIL，符合既有 tail caveat）。A2：Q053 Tier 1 完成，**2022 主策略亏损 −$26.8k（WR 55.6%，−21.4pp）实证确认 grinding decline 是隐藏弱点**；stop rate 0.0%——pnl_ratio 止损在 grinding decline 中不触发；Tier 2 扩展（2011/2015/2018-Q1）现已合理，待 Q041 后启动 — `See: RESEARCH_LOG.md R-20260509-03`
- 2026-05-09 — **Short-Premium Risk Management Principles codified into role documents** (pending commit). `QUANT_RESEARCHER.md` now contains a new section "Short-Premium Risk Management Principles" with all 5 principles from R-20260509-02, each with evidence, application rules, and spec-review gatekeeping language. `REVIEW_TEMPLATE.md` has a new mandatory "6.1 Short-Premium Standard Checks" section (checklist for stress-capital, execution-drift, IV expansion stress test, stop methodology, scale dependence). These are the final institutional step of the `/ES` research absorption — the principles now live in documents that will be consulted in every future short-premium spec review, not just in the research log — `See: QUANT_RESEARCHER.md#short-premium-risk-management-principles`, `REVIEW_TEMPLATE.md §6.1`
- 2026-05-09 — **`/ES` research absorption into main strategy governance is complete** (R-20260509-02). 2nd Quant reviewed the knowledge-transfer summary and issued PASS with three calibration cautions. Five must-absorb principles are now codified: (1) IV expansion leads all lagging signals — exit-driven risk control is structurally insufficient; (2) main strategy's entry-gated design is the right direction and should not be retrofitted with lagging exits; (3) `pnl_ratio` stops are more robust than credit-multiple stops; (4) 2018-Q4 / 2022 grinding-decline is likely the main strategy's hidden weakness, now opened as Q053; (5) `iv_expansion_stress_test` must become a mandatory new-spec review tool (Action A1). Three calibration cautions added by 2nd Quant: Overlay-F scale-dependence is a revisit hypothesis not a conclusion; capital efficiency must be measured on stress-capital basis; PM-executed rules must test T+0/T+1/T+2 delay scenarios. Highest-value takeaway: **`/ES` closed a strategy but opened a system-level governance principle: short-premium risk is about entry discipline and stress-capital survival, not exit timing** — `See: RESEARCH_LOG.md R-20260509-02`
- 2026-05-09 — **`/ES` research line comprehensively closed** across all three identifiers (`Q012 / Q051 / Q052`). Q052 tested all three PM redesign hypotheses (H1: technical exits, H2: roll-out, H3: deep OTM / longer DTE) and all failed: H3 grid (9 configs) produced universally negative AnnROE with failing bootstrap CIs; H1 grid showed trend exits universally underperform credit stops (84% of exits at a loss); H2 deferred but reasoned to fail via H1's root cause. Structural conclusion: **IV expansion reprices tail risk faster than any lagging control system can respond** — this invalidates not just the tested `/ES` variants but applies broadly to any small-scale naked short option strategy relying on lagging exits. This insight is archived as a standing constraint for future naked-short research proposals. Planner implication: `/ES` drops off the active research / spec queue entirely; hard revisit gate = NLV ≥ $1.5M + at least one SPEC-088 stress event observed + PM accepts dynamic leverage + BSH as inseparable system — `See: RESEARCH_LOG.md R-20260509-01`
- 2026-05-08 — `/ES` original thesis line was formally **closed at the current `$500k` account scale**. Final combined conclusion from `Q012/Q051`: the thesis is statistically valid only in its larger full-system form, but it is scale-dependent and not production-plausible here; conservative recalibrations do not recover significance without unacceptable SPAN concentration. Planner implication: keep the shipped `1`-contract `/ES` stack (`SPEC-061/086/088`) as a live-data / visibility / operational-calibration cell, stop active thesis work at this scale, and redirect research resources to `Q041` — `See: RESEARCH_LOG.md` `R-20260508-13`
- 2026-05-08 — Opened `Q052` as a new low-priority future `/ES` redesign seed after closure of the original thesis line. This preserves PM’s three fresh directions — technical-risk-off exits, roll-down / roll-out management, and very-far-OTM long-dated short puts — without keeping `Q012/Q051` artificially open. Planner implication: any future `/ES` work is now a new branch, not a continuation of the closed thesis line — `See: doc/q052_es_defensive_redesign_seed_memo_2026-05-08.md`
- 2026-05-08 — Quant completed an `/ES` alignment audit and the practical outcome is narrower than a new strategy spec but important for future implementation honesty. The review passes the recent cleanup, but fixes two semantic anchors: `/ES` stop semantics should be interpreted canonically as `3.0x credit` (matching `SPEC-061` and `SPEC-086`, not any lingering `4.0x` backtest assumption), and the current production-vs-research sizing mismatch should be understood as a **fixed 1-contract vs BP%-target** issue rather than a simple “10% vs 20%” discrepancy. Planner implication: keep these constraints explicit in future `/ES` engineering follow-ups and consider a single-source `EsShortPutParams` hardening path later — `See: RESEARCH_LOG.md`
- 2026-05-08 — Developer completed the `/ES` parameter hardening pass implied by the Quant alignment audit. The backtest now uses `STOP_MULT = 3.0` (stop rate `15.9% → 27.8%`, as expected for a more honest stop), the production-comparable research path now fixes `/ES` sizing at `1` contract, and a new `strategy/es_params.py` / `EsShortPutParams` layer now feeds backtest, bot, and server-side BP-limit usage from one source of truth. Planner implication: the semantic-alignment follow-up is now implemented; future `/ES` work should build on this single-source layer instead of re-splitting assumptions across files — `See: RESEARCH_LOG.md`, `strategy/es_params.py`
- 2026-05-08 — The small pre-existing `SPEC-087` nav-label test drift is now cleared. Two tests in `tests/test_spec_087.py` were updated to match the already-intended `Port BT` label. This is maintenance-only cleanup: no UI semantics, routing, or product behavior changed — `See: RESEARCH_LOG.md`
- 2026-05-08 — PM-accurate homepage margin breakdown is now live. The old BP display overstated usage (~33.6%) by applying a Reg-T-style `(width - credit) * 100` lens to a PM account. The current portfolio surface now uses a more faithful operational proxy: SPX spread BP is approximated as `width * 100 * contracts` (max-loss proxy), equity margin is derived as `maintenance_total - spx_max_loss`, and cash / cash ETF / money-market buckets are now broken out explicitly. Practical research conclusion: Schwab’s account API is usable mainly at the **account-total** maintenance layer; per-position `maintenanceRequirement` stays zero, so PM decomposition must be inferred from account totals plus strategy-specific proxies — `See: schwab/client.py`, `web/portfolio_surface.py`, `web/templates/portfolio_home.html`
- 2026-05-08 — Portfolio read surfaces expanded materially without changing strategy write paths. `SPEC-087` put the Portfolio Command Center at `/`, moved the old SPX dashboard to `/spx`, and standardized nav. In parallel, `/ES` gained an open-position modal and a dedicated backtest page, Q041 gained a dedicated backtest page with sleeve-level summary cards and trade-log tabs, and startup cache warmup was added so the first `/ES` / `Q041` backtest requests no longer hit Cloudflare timeouts and HTML-instead-of-JSON failures. These are presentation / research-support upgrades, not new routing logic — `See: task/SPEC-087.md`, `web/templates/es.html`, `web/templates/es_backtest.html`, `web/templates/q041_backtest.html`, `web/server.py`
- 2026-05-07 — `Q046` benchmark-normalization finished. Quant concluded that our post-`SPEC-084` average BP usage (`~15.9%`) is only moderately below a conservative-to-neutral PM defined-risk benchmark once external `25%–30%` / `25%–50%` practitioner figures are normalized. The important planning result is mechanism ranking: broader strategy / underlying coverage (`Q041`) is now the primary next account-level deployment-efficiency axis because it is the only active family that can directly attack the remaining `17%` fully idle days. More sizing / more concurrency / higher ceilings are all deferred — `See: doc/q046_bp_utilization_external_benchmark_seed_memo_2026-05-07.md`
- 2026-05-07 — Planner opened `Q048` as a dedicated architecture-planning lane for the transition from a single-SPX execution platform to a portfolio research platform. Current diagnosis: the codebase still assumes one recommendation, one current position, and one SPX-centered research surface; the near-term need is staged planning for portfolio-state abstraction, unified ledger semantics, recommendation splitting, and minimal portfolio summary surfaces rather than an immediate large UI rewrite or implementation Spec — `See: doc/q048_portfolio_state_architecture_transition_plan_seed_memo_2026-05-07.md`
- 2026-05-07 — `SPEC-085` opened as the first narrow DRAFT coming out of `Q048`. Planner accepted Quant’s scope-down recommendation: replace the old long-cycle paper-trading dependency with a smaller support surface centered on read-only `sleeve_candidates[]`, minimal portfolio summary, and a minimal attribution/visualization artifact. This is explicitly a forward-tracking / research-support layer, not a unified portfolio routing or live state rewrite — `See: task/SPEC-085.md`
- 2026-05-07 — PM approved `SPEC-085`. The next step is standard Developer implementation of the narrow `Q048` support surface: independent read-only `sleeve_candidates[]`, minimal dual-rail portfolio summary, and a minimal attribution/visualization carrier, all under explicit fail-soft and no-write-path-change guardrails — `See: task/SPEC-085.md`
- 2026-05-07 — Quant completed the `Q041` Tier 3 portfolio-attribution prototype and PM accepted `data/q041_portfolio_attribution_latest.json` as the formal `SPEC-085 F3` source. This materially changes the route: the old 12-month paper-trading path is no longer the main dependency, and PM also declined a generic `4–6` week live signal forward-tracking gate. `Q041` now advances primarily through visualization / attribution support plus the still-separate overlap-validation track — `See: doc/q041_portfolio_attribution_results_2026-05-07.md`, `RESEARCH_LOG.md`
- 2026-05-07 — `SPEC-085` implementation completed and the spec is now **DONE**. The delivered surface stays within the narrow read-only boundary: dual-rail portfolio summary, independent `sleeve_candidates[]`, and an F3 attribution carrier consuming the accepted `Q041` artifact, with no unified routing and no live write-path merge — `See: task/SPEC-085.md`, `task/SPEC-085_handoff.md`
- 2026-05-07 — Opened `Q046` as a new account-level ROE research seed after `SPEC-084` went live. Trigger: PM observation that even after the J3 lift the book still averages only ~16% BP usage, which may remain below mainstream short-premium portfolio deployment norms. Scope is external benchmarking plus mechanism ranking (sizing vs overlap vs strategy breadth vs ceiling), not a new implementation spec yet — `See: doc/q046_bp_utilization_external_benchmark_seed_memo_2026-05-07.md`
- 2026-05-02 — **Tieout #3 PASS** (`doc/tieout_3_2026-05-02/`). Q-A 回归：场景 A（两 toggle `disabled`，PT=0.60）= 57/$79,933.69，与 tieout #2 Q-C byte-identical → **REGRESSION_PASS = True**，SPEC-079/080 在 `disabled` 模式对 trade flow 零影响。B/C/D preview（PT=0.60）：三个 toggle-active 场景全部等于 A — 3y 低压力窗口内 comfort filter 与 stop tightening 均未触发（SPEC-079 VIX≤15+dist+ma_gap 三条件从未同时满足于 BCD 入场日；SPEC-080 pnl_ratio [-0.50,-0.35) 区间亦从未命中）。D_pt050（PT=0.50，both active）：57/$76,450，比 tieout #2 少 1 笔 +\$880 PnL — `2026-04-30` BCD 被 SPEC-079 risk_score=3 拦截（VIX=14.x / dist_30d≤-1% / ma_gap>1.5pp）。Gap vs MC@0.50：trade delta +6→+5，PnL delta +\\$29,648→+\\$30,528。主残余 gap（IC regular HC 13 vs MC 6）不在 SPEC-079/080 范围 — `See: RESEARCH_LOG.md` R-20260502-07, `doc/tieout_3_2026-05-02/README.md`
- 2026-05-02 — PM completed the remaining post-sync direction calls: `Q036` now follows the MC-side escalate / productization-stack direction (ending the HC hold-vs-escalate divergence), `Q021` is approved closed with `SPEC-066` unchanged, `Q038` is approved to move to `shadow`, and `Q039` is explicitly kept in the research lane rather than promoted into a separate parity investigation. Planner implication: sync routing questions are now largely settled; next work is adoption / monitoring / prerequisite planning, not more branch selection — `See: sync/open_questions.md`, `sync/hc_to_mc/HC_return_2026-05-02.md`
- 2026-05-02 — `Q038` shadow rollout is now actually live on old Air. Developer flipped `bcd_comfort_filter_mode` and `bcd_stop_tightening_mode` defaults from `disabled` to `shadow`, deployed commit `3da1f5f` to old Air, and restarted `com.spxstrat.web` + `com.spxstrat.bot`. Live recommendation path is healthy (`/api/recommendation -> 200`), and Path C has now moved from “shadow-approved” into real runtime observation. Important nuance: `SPEC-079` shadow is live-observable via `data/bcd_filter_shadow.jsonl`; `SPEC-080` shadow remains mostly posture alignment because live-side debit-stop shadow logging is not yet wired — `See: sync/open_questions.md`, `sync/hc_to_mc/HC_return_2026-05-02.md`
- 2026-05-03 — Developer completed the first HC-local implementation pass for `SPEC-075/076`. `SPEC-075` core Overlay-F logic / sizing hook and `SPEC-076` monitoring / dashboard / review support are now implemented locally with `overlay_f_mode = "disabled"`, tests passing, and a 3y disabled parity spot matching the tieout #3 baseline. No old Air deploy, pull, push, or service restart has happened yet. Planner implication: the branch has moved from implementation-planning into local disabled implementation complete; the next decision is review plus PM-approved `shadow` rollout on old Air — `See: task/SPEC-075_handoff.md`, `task/SPEC-076_handoff.md`, `sync/open_questions.md`
- 2026-05-03 — PM reviewed the HC-local `SPEC-075/076` disabled implementation package and chose **Option B**: authorize the first old Air `shadow` rollout. This does not approve `active`; it only advances the branch from local disabled implementation into runtime `shadow` deployment. Planner implication: the next work is no longer branch selection or local review, but controlled deployment execution on old Air followed by shadow observation under the existing review protocol — `See: sync/open_questions.md`, `task/SPEC-075_handoff.md`, `task/SPEC-076_handoff.md`
- 2026-05-03 — Developer completed the first old Air `shadow` rollout for `SPEC-075/076`. Commit `458231e` was pushed to `origin/main`, old Air pulled latest `main`, `pip install -e .` was run, and `com.spxstrat.web` + `com.spxstrat.bot` were restarted. Runtime posture is now `overlay_f_mode = "shadow"` (not `active`), local web health is `200`, `/api/recommendation` returns normally, and no-side-effect checks show recommendation strategy parity with factor `1.0` and `would_fire = False` for the current sample. `overlay_f_shadow.jsonl` / `overlay_f_alert_latest.txt` do not yet exist on old Air because no real would-fire event has occurred yet; this is expected under the current writer semantics — `See: task/SPEC-075_handoff.md`, `task/SPEC-076_handoff.md`, `sync/open_questions.md`
- 2026-05-03 — Developer aligned old Air to `origin/main @ a112d33`, installed `pyarrow`, and deployed the `Q041` forward collector as a separate launchd job. No `web` / `bot` restart was needed because the pushed changes were research/data-collection scaffolding rather than live recommendation path changes; runtime stayed healthy and `Q036` remained on `overlay_f_mode = "shadow"`. `Q041` collector smoke validation passed manually on `AAPL`, weekend launchd gate behavior was confirmed, and the first meaningful next checkpoint is now the first weekday `16:30 ET` automatic run for all `12` whitelist symbols — `See: research/q041/collect_chains.py`, `research/q041/whitelist.py`, `sync/open_questions.md`
- 2026-05-03 — Quant completed `Q041 Phase 1 / D1 Data Sanity` and closed it **PASS**. Historical Massive parquet on `17` symbols shows effectively complete ~`1,000`-trading-day coverage, zero null / negative closes, zero OHLC-order violations, bounded stale-price behavior after penny-option filtering, acceptable BS-IV inversion quality in the target region (`92.8%` valid on the sampled AAPL slice, no `>200%` outliers), and passing structure checks for delta monotonicity and put-call parity. The report records three SPX holiday anomalies plus one isolated AMZN sparse-day anomaly, and it fixes four operational filters (`F1–F4`) for downstream work. Planner implication: `Q041` is now `D2-ready`; benchmark replication may begin on the historical parquet while dual-source overlap validation continues on its own schedule — `See: doc/q041_d1_data_sanity_report_2026-05-03.md`, `RESEARCH_LOG.md`
- 2026-05-04 — Quant completed `Q041 Phase 1 / D2 Benchmark Replication` and closed it **PASS**. The historical SPX benchmark pipeline now tracks official `BXM` closely enough for Phase 1 planning use: monthly correlation `0.9715`, MAE `0.43%` per cycle, cumulative drift only `-1.24%`. This fixes the D3 baseline frame: official `BXM` Sharpe `1.23`, replicated `BXM` Sharpe `1.22` (`1.14` at `3%` slippage), and ATM CSP Sharpe `0.81` (`0.73` at `3%` slippage). Structural interpretation is also now explicit for this window: covered-call overlays underperform SPX in absolute return during the bull-dominant sample but improve Sharpe and drawdown behavior, while ATM CSP provides the cleaner premium-harvest baseline with `76.6%` win rate and about `+4.15%` annualized BP-day ROE pre-slippage. Planner implication: `Q041` is now `D3-ready` for Module A/B work while overlap validation continues independently — `See: doc/q041_d2_benchmark_replication_2026-05-04.md`, `RESEARCH_LOG.md`
- 2026-05-04 — `task/q041_phase2_planner_context.md` is now the canonical short-form planning note for `Q041 Phase 2`. It compresses the D1/D2/D3/D4 outcomes into one routing document, fixes the current production-candidate shortlist, records the explicit “do not retest” exclusions, and grades the remaining work into `P0 / P1 / P2`. Planner implication: future Phase 2 routing should default to this document rather than re-reading the full D3/D4 analysis set unless implementation detail is needed — `See: task/q041_phase2_planner_context.md`
- 2026-05-04 — Quant completed the first formal `Q041` overlap-validation read under the `M1–M10` protocol and the verdict is **mixed but acceptable**. Both sources land cleanly at the file / symbol layer (`17/17` each), and the economically relevant comparable subset is already converging strongly: traded / price-bearing contract keys match near-perfectly, matched-price deviation is effectively zero, and volume rank correlation is very high. The remaining issues are protocol / semantic cleanup rather than collector failure: `(1)` `M4` should be limited to liquid / price-bearing contracts because deep-ITM Massive `day_close` rows can be stale, `(2)` Massive IV is decimal while Schwab IV is percentage and must be normalized before formal `M6` comparison, and `(3)` `SPX` Massive Greeks / IV nulls should be treated as a structural provider limitation rather than a blocker. Planner implication: continue the overlap clock; do not reset it or misclassify today’s read as a data-source failure — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-05-05 — Quant completed `Q041 Phase 2` and wrote the final summary note. All `P0 / P1 / P2` tasks are now closed. The project now has an explicit candidate hierarchy rather than an open validation queue: `SPX CSP Δ0.20 DTE30` remains the only clean formal candidate; `GOOGL / AMZN CSP` are promoted to borderline formal candidates; `COST / JPM` earnings iron condors are downgraded to observe-only; `SPX CC DTE45` is eliminated and `SPX CSP DTE45` remains observe-only. A new practical rule candidate also emerges from `P2-2`: `VIX >= 15` improves earnings-IC results and should be carried forward into future paper-trading / governance work rather than treated as a finished production parameter today — `See: doc/q041_phase2_summary_2026-05-05.md`, `RESEARCH_LOG.md`
- 2026-05-05 — **2nd Quant issued PASS Routing B for Q041**. All five specific questions (SPX CSP, GOOGL/AMZN CSP, COST/JPM IC, overlap separation, packaging choice) received explicit answers confirming the planner's tiering. Key verdicts: `SPX CSP DTE30` enters Tier 1 formal paper trading; `GOOGL/AMZN CSP` enter Tier 2 borderline formal / tail-caveated (lower BP budget than Tier 1, COVID tail mandatory caveat); `COST/JPM IC` remain Tier 3 observe-only (VIX≥15 is a real governance filter; JPM IMR≥33% optional only); overlap validation continues as parallel admission track and does not block paper trading — `See: doc/q041_2nd_quant_review_feedback.md`, `task/q041_2nd_quant_review_packet_2026-05-05.md`
- 2026-05-05 — **Q041 execution-prep packet written** (`doc/q041_execution_prep_packet_2026-05-05.md`). The packet is the canonical paper-trading reference document for all three tiers. It defines: entry flows (BS-delta strike selection, third-Friday roll, non-overlapping), BP budget (Tier 1 ≤20% / Tier 2 combined ≤15% / Tier 3 ≤5% / total Q041 ≤40%), per-cycle recording fields, monthly/annual monitoring checklists, upgrade/downgrade trigger rules, and mandatory risk disclosures (IV compression trap, COVID tail, single-name narrative risk, IC sample fragility). Paper trading start-up is now unblocked for all three tiers — `See: doc/q041_execution_prep_packet_2026-05-05.md`
- 2026-05-05 — Planner prepared a dedicated `Q041` next-stage execution-prep packet for **2nd Quant review**. This does not reopen the research tree; it packages the completed Phase 2 findings into a bounded governance question: whether the current tiering (`SPX CSP DTE30` formal, `GOOGL/AMZN` borderline formal, `COST/JPM` observe-only) is strong enough for next-stage execution / paper-trading routing, and whether overlap validation should remain a parallel admission track rather than a blocker for candidate governance — `See: task/q041_2nd_quant_review_packet_2026-05-05.md`
- 2026-05-05 — `SPEC-083` is now formally closed **DONE** and indexed as a recently closed support spec rather than an active implementation item. Review conclusion is full PASS: AC1–AC10 all passed, ledger independence and atomic-write semantics were explicitly confirmed, `vix_gate_passed=false` is enforced at the library layer, budget reads fail closed when config is missing, env-var overrides keep tests isolated, and the final regression result is 25/25 PASS with zero effect on the main SPX recommendation path — `See: task/SPEC-083.md`
- 2026-05-07 — `SPEC-084` opened as the narrow DRAFT implementation path for `Q045 J3`. Scope is intentionally constrained to the three `bp_target` default lifts (`normal`/`low_vol` 0.10→0.15, `high_vol` 0.07→0.14), `_size_rule()` text sync (`3%→4.5%`, `1.5%→2.25%`), and explicit risk disclosure (`Worst trade -8.82% acct`, `Peak BP 43%`). No ceiling change, no Q036 rewrite, no Q041 route expansion — `See: task/SPEC-084.md`
- 2026-05-07 — PM approved `SPEC-084`. The next step is standard Developer implementation of the narrow Q045 J3 landing shape: three `bp_target` default lifts, `_size_rule()` text sync, and explicit risk disclosure only. This is now the single active approved spec in queue — `See: task/SPEC-084.md`
- 2026-05-07 — `SPEC-084` implementation review passed and the spec is now **DONE**. Implementation stayed within scope: only the three `bp_target` defaults, `_size_rule()` text sync, `test_state_and_api` expectation update, and explicit risk disclosure handoff. Review confirmed AC1–AC7 PASS. Three failing legacy tests (`test_spec_048_055` / `test_spec_056`) were verified as pre-existing Gate-1 test debt, not a new regression — `See: task/SPEC-084.md`, `task/SPEC-084_handoff.md`
- 2026-05-06 — **Q044 Tier 2 completed**: A1 (bp_target 15%) passes all checks — Sharpe 0.91→0.91, peak BP% 30→30%, year-by-year attribution uniform (2024/2025/2026 all proportional), Q036 coexistence peak 29% vs 50% ceiling. Bonus: A2+ceiling40% generates $27,332 (deferred). Recommend DRAFT Spec. Key change: `bp_target_normal` 0.10→0.15; live size rule 3%→4.5%; worst trade risk disclosure (-6.25% acct) — `See: doc/q044_bps_tier2_results_2026-05-06.md`
- 2026-05-06 — **Q044 Tier 1 completed**: BPS sizing scan (6 variants, 2 axes, 3y window). A1 (bp_target 15%, same δ0.30/0.15 structure) shows near-linear scaling with -0.3% marginal decay, AnnROE +1.5pp — advances to Tier 2. A2 (20%) hits BP ceiling adverse-selection cliff (N drops 15→9, PnL turns negative) — not viable. Axis B (wider spread δ0.25/0.20) uniformly -14% to -37% $/BP-day — direction closed. Q036 dependency revised: BPS and Overlay-F are regime-separated, Tier 2 can proceed without Q036 active decision — `See: doc/q044_bps_tier1_results_2026-05-06.md`
- 2026-05-06 — **Q044 opened**: BPS spread sizing and account-level ROE seed memo. Core question: current BPS 10% BP per trade leaves ~25% structural idle BP (ceiling 35%); can spread width or contracts scale up to improve ROE? Key constraints: depends on Q036 Overlay-F active decision (compete for same idle BP); needs Tier 1 backtest scan before any Spec — `See: doc/q044_bps_sizing_roe_seed_memo_2026-05-06.md`
- 2026-05-06 — Runtime access documentation received a facts-only housekeeping update: stale fixed-LAN-IP assumptions were removed from `SERVER_RUNTIME.md` and `doc/old_air_server_maintainer.md`; Tailscale (`100.114.226.33`) is now the explicitly preferred stable access path for old Air, `oldair-lan` is documented as a same-LAN convenience only, and the 502 triage wording now more clearly distinguishes local Flask / local tunnel health from Cloudflare Access / public ingress faults. No runtime behavior changed — `See: SERVER_RUNTIME.md`, `doc/old_air_server_maintainer.md`
- 2026-05-06 — **Q041 old Air data infrastructure complete**. Three launchd jobs now operational: Schwab chain (16:30 ET) ✓, Massive REST snapshot (16:35 ET) ✓, Massive S3 historical OHLC (08:15 ET) ✓. Fixed: `boto3` missing from old Air venv. Bootstrap: full history 2022-05-06→2026-05-05, 17 parquets, 28.9M rows. Daily incremental thereafter. Known: exit code 1 on 403 for today's S3 flatfile (T+1 lag, expected) — cosmetic, not blocking — `See: task/q041_massive_runtime_handoff_2026-05-04.md`
- 2026-05-06 — **Q041 overlap-validation: first same-day comparison completed**. Ran `download_massive.py` to pull May 4–5 data (80K rows). M4 same-day (Schwab `last` vs Massive `close`, May 4): all matched n=29,733, **median 0.000%, >2%=2.3%** → STRONG PASS. SPX ATM 14% >2% tail is structural (timing difference for low-volume strikes, not data error). M3 corrected to merge-based comparison: SPX 99.8%, GOOGL 83.6%, AMZN 85.8% (PASS); overall 80.3% (PASS). M6 IV completeness 100% all symbols; M7/M8 100%. M9 protocol text corrected: Schwab `S` = standard monthly (not `M`). Three cleanup items closed. Overlap validation status: **converging as expected**, confirmed with same-day data — `See: RESEARCH_LOG.md` R-20260506-01, `doc/q041_overlap_validation_protocol_2026-05-03.md`
- 2026-05-05 — Quant issued a stronger `Q041` overlap-validation checkpoint: **converging as expected**. The stitched dual-source path now clears the important protocol gates on the economically relevant subset (`M1` traded-key match `99.2%`, `M2` expiry `97.5%`, `M5` volume rank-corr `0.879`, near-money `M6` IV completeness `100%`, `M7/M8` `100%`). No blocker-grade mismatch is currently present. Remaining work is narrow cleanup only: explicit null handling for Schwab `iv=-999`, a repair of the `M3` strike-match script, wording updates around `S/W/M` expiry semantics, and the first true same-day `M4` read once Massive `2026-05-04` data lands — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-05-05 — **Q043 Developer planning prompt written** (`doc/q043_developer_planning_prompt_2026-05-05.md`). The prompt formalizes the three-phase structure: Phase A (read-only shadow scanner writing structured JSONL audit log, no bot push), Phase B (dev-bot / shadow notifications after ≥2 weeks Phase A observation), Phase C (paper-trade reminder support for CSP expiry + earnings T-3 windows). Key guardrails: q043 modules must not be imported by `engine.py` / `selector.py`; no writes to broker; no merge into main SPX recommendation path — `See: doc/q043_developer_planning_prompt_2026-05-05.md`
- 2026-05-05 — Planner opened `Q043` as a future support-branch memo for `Q041`: preserve the idea of a read-only candidate scanner, dev-bot / shadow recommendations, and reminder support, but keep it separate from `SPEC-083` and out of the active queue until paper-trading operations are stable. The intended future posture is scanner-first, shadow-first, notification-first, with no automatic execution and no early merge into the main SPX recommendation path — `See: doc/q043_q041_scanner_and_bot_seed_memo_2026-05-05.md`
- 2026-05-02 — **HC reproduction sprint batch 2 全部闭合**（SPEC-079 + SPEC-080 均 DONE）。`bcd_comfort_filter_mode` / `bcd_stop_tightening_mode` 均默认 `"disabled"`；PM 目标 4-8 周观察后 flip shadow。SPEC-077 documented debit-side 硬编码 defect 通过 SPEC-080 正式闭合。Path C（PM 选定）已落地 — `See: task/SPEC-079.md`, `task/SPEC-080.md`, `RESEARCH_LOG.md` R-20260502-07
- 2026-05-02 — **Tieout #2 PASS** (`doc/tieout_2_2026-05-02/`). Q-A 自洽性：HC@PT=0.50 今天 vs tieout #1 CSV 98.28% entry match，仅新增 `2026-04-30`（date-boundary artifact，当时 CSV 生成于 2026-04-29），57 原始 entry 100% 保留 → **batch 1 未引入 trade flow 变化**。Q-B gap：HC 58/\\$75,570 vs MC 52/\\$45,922 Δ = +6/+\\$29.6k（与 tieout #1 Δ+5/+\\$28k 相差仅 date expansion，gap 不收敛符合预期 — batch 1 未改 selector/IVP gate/persistence）。Q-C 新基线 HC@PT=0.60：57 trades (2 open), \\$79,934（BCD 20/+\\$38.6k, IC 13/+\\$19.3k, BPS 15/+\\$13.5k, IC_HV 8/+\\$6.8k）。Gap 收敛等 **batch 2 (SPEC-079/080)** 后做 tieout #3 — `See: RESEARCH_LOG.md` R-20260502-06, `doc/tieout_2_2026-05-02/README.md`
- 2026-05-02 — `SPEC-078` closed **DONE**. Codex 脚本化 smoke ([task/SPEC-078_handoff.md](task/SPEC-078_handoff.md)) PASS：AC1 三字段（`annualized_roe` / `annualized_roe_basis="final_equity_compound"` / `period_years`）在 `start=2023-01-01` 与 `start=2007-01-01` 两窗口都返回；AC4 byte-identical vs JS 公式 |Δ| 分别 `3.1e-07` / `1.2e-07`，远 < `1e-6`；19.32y 全样本 `ann_roe=8.0358%` 与 SPEC-077 AC3 PT=0.60 数据 cross-check 一致。PM 浏览器 smoke PASS：normal path 直接读 server 字段，fallback path 经 DevTools Local Overrides 删字段后触发 `[SPEC-078] ... JS fallback` console.warn 且 ROE 卡片仍以 JS 兜底数值展示。AC1-AC7 全部 PASS。P12 Fast Path 仍 deferred。Server-side metrics SoT 现可被 tieout #2 直接消费 — `See: RESEARCH_LOG.md` R-20260502-05, `task/SPEC-078.md`, `task/SPEC-078_handoff.md`
- 2026-05-02 — HC reproduction sprint **batch 1 全部闭合**（SPEC-074 / SPEC-077 / SPEC-078 三 spec 均 DONE）。tieout #2（`2023-04-29 → 2026-04-29` rerun）现可启动；HC↔MC magnitude gap 调查 deferred 至 SPEC-080 wire debit-side `params.stop_mult` 之后（候选索引 Q040）— `See: RESEARCH_LOG.md` R-20260502-04 / R-20260502-05`
- 2026-05-02 — `SPEC-077` closed **DONE** under PM option (2): operational MC parity with documented AC3 full-sample shortfall. F1-F5 delivered; AC3 full-sample HC rerun (`2007-01-01`, 19.32y) gave Δ ann_roe = `+0.0856pp` (vs Q037 Phase 2A `+0.91~+1.03pp` band, vs AC3 `≥+0.5pp` threshold), Δ sharpe `0.00`, Δ max_dd `-$850`. The ~10× HC↔MC magnitude gap is deferred for post-SPEC-080 attribution (debit-side `params.stop_mult` wiring is the most likely confound after compounding-baseline口径). Closure unblocks tieout #2; cross-engine ROE comparisons must flag the open gap until resolved — `See: RESEARCH_LOG.md` R-20260502-04, `task/SPEC-077.md`, `doc/baseline_2026-05-02/ac3_summary.json`
- 2026-05-02 — `SPEC-078` code-level F1-F4 landed: server `compute_metrics` now emits `annualized_roe` / `annualized_roe_basis` / `period_years` ([backtest/engine.py](backtest/engine.py)); frontend reads from `metrics.annualized_roe` with deprecated `console.warn` fallback ([web/templates/backtest.html:2028](web/templates/backtest.html#L2028)); JSDoc on `impliedAnnualizedRoe` marks it `@deprecated SPEC-078`; new unit test `tests/test_metrics_annualized_roe.py` (5/5 PASS) locks server-vs-JS byte-identical parity within 1e-6. `computeSubsetMetrics` untouched per AC7. P12 Fast Path remains deferred. PM browser smoke pending before DONE — `See: RESEARCH_LOG.md`, `task/SPEC-078.md`
- 2026-05-02 — `SPEC-077` code-level F1/F2 landed: `StrategyParams.profit_target` default raised to `0.60` ([strategy/selector.py:68](strategy/selector.py#L68)); two `web/server.py` fallback overrides synced; `tests/test_engine_stop_wiring.py` locks credit-side `params.stop_mult` wiring at engine.py:880 and explicitly punts the debit-side hardcoded `-0.50` (line 882) to SPEC-080. Baseline rerun under HC at `doc/baseline_2026-05-02/` (3.3y window, 2023-01-01 → today) shows: trades 59 → 58 closed +2 open, win rate +1.3pp, max DD improved $958, but realized total PnL -$13,276 and Sharpe -0.29. The two missing `50pct_profit` exits are now `open_at_end` (unrealized winners excluded from `total_pnl`), so the 3.3y diff is partly a window-truncation artifact rather than a structural regression. SPEC-077 AC3 remains anchored on `Q037 Phase 2A` full-sample evidence (+0.91~+1.03pp ann ROE); PM AC3 sign-off pending before closing SPEC-077 DONE — `See: RESEARCH_LOG.md`, `task/SPEC-077.md`, `doc/baseline_2026-05-02/README.md`
- 2026-05-02 — `SPEC-074` (HC `backtest_select` parity vs MC) closed DONE. F2 7-gate × 6-component cross-check confirmed all MC gates present in HC `select_strategy` except DIAG both-high (HC removed via SPEC-056c). F5 PM裁定 = Option A: HC keeps SPEC-056c removal as canonical posture; MC's retention is a documented permanent HC↔MC divergence. F4 parity test `tests/test_backtest_select_parity.py` added (22/22 = 100% PASS, ≥95% threshold). HC `engine.py:835/1252` calls live `select_strategy` directly so parity test reduces to snapshot-construction regression guard (5 testcases) — `See: RESEARCH_LOG.md`, `task/SPEC-074.md`
- 2026-04-26 — Quant completed `Q021 Phase 4` (`doc/q021_phase4_sizing_curve_2026-04-26.md`), the 6-variant aftermath first-entry sizing-curve study. The decisive result is that every sizing-up variant underperformed the `SPEC-066` baseline on marginal `$ / BP-day`: `V_G 3.83`, `V_D 3.37`, `V_J 2.98`, `V_E 2.70` versus baseline `V_A 4.85`. This means the apparent `V_D` uplift is leverage drag rather than a smarter rule, `V_J` confirms most of `V_D`’s extra dollars came from distinct-cluster overlapping leverage, `V_G` is the cleanest doubler but still below baseline, and `V_H` is effectively `V_A - 1 trade`. Planner-level implication: `Q021` is now ready to close pending PM approval, with current recommendation “keep `SPEC-066`, do not open `SPEC-067`” — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-26 — PM’s new permanent review convention is now in force: all future strategy / spec / variant / prototype comparisons must include the full metrics pack (`PnL/BP-day`, `marginal $/BP-day`, `worst trade`, `disaster window`, `max BP%`, `concurrent 2x days`, `CVaR 5%`), not just `PnL / Sharpe / MaxDD`. This rule came out of `Q021 Phase 4` and should be treated as a standing research-governance constraint rather than a question-specific preference — `See: RESEARCH_LOG.md`
- 2026-04-26 — PM reset the project’s top-level objective from strategy-local improvement toward **reasonable account-level ROE maximization**. This changes the framing of the open sizing discussion: `Q021` remains a rule-layer conclusion and evidence base, but future work on aftermath sizing should no longer ask “should `V_D` replace `V_A`?” Instead, the new question is whether controlled idle-BP deployment can improve account-level ROE without unacceptable drawdown, margin, or concentration costs. Planner implication: open a new capital-allocation research branch (`Q036`) and keep `IC_HV aftermath` as the first pilot use case rather than as the universal answer — `See: RESEARCH_LOG.md`, `sync/open_questions.md`, `task/IDLE_BP_OVERLAY_RESEARCH_NOTE.md`
- 2026-04-26 — Quant completed the first formal `Q036` feasibility framing pass. The key result is conceptual but important: `Q036` should be evaluated against the **idle-BP baseline** (roughly `$0 / BP-day`), not against `V_A`’s rule-layer `+$4.85 / BP-day`. Quant also reports that baseline BP use appears structurally low (~`12.5%` average, `14%` max), so idle BP is likely persistent enough to matter. However, the actual account-level ROE answer is still open: tail cost, margin stress, forced-liquidation proxy, and regime-conditional idle-BP availability have not yet been measured. Planner implication: move `Q036` into Phase 1 feasibility prototype, but do not open a Spec or reinterpret `Q021` — `See: RESEARCH_LOG.md`, `sync/open_questions.md`, `task/IDLE_BP_OVERLAY_RESEARCH_NOTE.md`
- 2026-04-26 — PM approved `Q036 Phase 2`. The branch now advances from feasibility-only into a narrow controlled overlay study. Scope remains intentionally small: test only the three conditional pilot candidates (`Overlay-A 1.5x conditional`, `Overlay-B 2x + disaster cap`, `Overlay-C 2x + no-overlap`), keep idle-BP threshold gating mandatory, and require full account-level ROE plus tail-cost reporting before any Spec discussion — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-26 — Quant completed `Q036 Phase 2`. All three conditional overlay pilots produced positive account-level incremental return, but the magnitude is modest: about `+0.05` to `+0.09` annualized ROE points. None justifies DRAFT-spec escalation yet because each still pays an incremental tail-cost or crowding tradeoff. `Overlay-A` looks weakest and can largely be dropped. If PM wants to continue, the branch should narrow to `Overlay-B` (best raw uplift, cleanest disaster net, but highest peak BP) and `Overlay-C` (best stacking guardrail, slightly weaker return) — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-26 — Quant completed `Q036 Phase 4` short-gamma-guard refinement and found the first real compromise candidate: `Overlay-F_sglt2`. It keeps the key guardrails clean (`SG>=2 = 0%`, disaster-window net `+301`, peak BP `34%`) while materially improving on the overly conservative hybrid’s return (`+0.074pp` annualized ROE uplift vs `+0.046pp`). This is still not enough to justify DRAFT-spec escalation, but it changes the research branch from “positive but diffuse” to “continue only if confirming one lead candidate” — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-26 — PM approved the final narrow `Q036` confirmation pass focused only on `Overlay-F_sglt2`. The branch should now stay tightly constrained: no new overlay family, no return to `Q021`, and no new spec discussion until `Overlay-F` has been checked on yearly attribution, fire distribution, and recent-era robustness — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-26 — Quant completed the final narrow `Q036` confirmation pass on `Overlay-F_sglt2`. The uplift is now confirmed as “sparse but distributed” rather than single-year-dependent: `+9,005` total PnL, `+0.074pp` annualized ROE uplift, `11/27` positive delta years, no `SG>=2` fires, and recent-era (`2018+`) results remain positive though thinner (`+0.040pp`). Planner implication: stop expanding the research tree and move the branch to **PM decision packet** stage rather than continuing blind variant search — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-26 — 2nd Quant reviewed the `Q036` packet and issued a targeted **CHALLENGE** rather than a substantive rejection of the branch. Framing, lead-candidate choice, disaster posture, and yearly attribution all passed, but one methodological inconsistency must be fixed before PM packet use: the `Overlay-F` gate counts pre-existing short-gamma exposure in family-deduplicated terms, while the framing / cleanliness metrics are reported in position-count terms. Planner implication: do not widen the branch, do not drop it, and do not advance to PM decision packet yet; first align gate and metric semantics, then rerun the narrow confirmation and refresh the packet — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-26 — Quant completed the post-review reconciliation step after receiving both 2nd Quant `CHALLENGE` and 3rd Quant `PASS`. Final integrated verdict is **PASS WITH CAVEAT**: the short-gamma-count mismatch is real, but the packet’s cleanliness claim is already measured on the stricter position-count metric, so this is now treated as a disclosure / governance caveat rather than a numerical blocker. Minimal remediation was to add a methodology note to the packet and record that any future productization path must align the gate to position-count semantics. Planner implication: `Q036` may now proceed to PM decision packet, but still should not enter DRAFT overlay spec discussion — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-26 — PM reviewed the completed `Q036` packet and chose **Option B**. This is an important governance change, but not a production one: `Overlay-F_sglt2` is now authorized to move from pure research into a more formal overlay-discussion / next-stage planning track, while remaining explicitly outside both DRAFT-spec and implementation status. Planner implication: stop treating `Q036` as merely “packet-ready”; the next work should define governance, monitoring, and productization prerequisites under the disclosed methodology caveat — `See: RESEARCH_LOG.md`, `sync/open_questions.md`, `doc/q036_pm_decision_packet_2026-04-26.md`
- 2026-04-26 — Quant completed the PM-facing productization decision packet for `Q036` (`task/q036_pm_decision_packet_2026-04-26.md`). The packet’s recommendation is now explicit: **hold as research candidate, do not productize now**. The reasoning is that `Overlay-F_sglt2` is economically real and governance-clean enough to avoid a `drop`, but still too thin (`+0.074pp` full-sample annualized ROE, `+0.040pp` recent) to justify productization complexity today. Planner implication: `Q036` is no longer waiting on more research packaging; it is waiting on PM’s final promote-vs-hold decision, with Quant currently recommending `hold` rather than `escalate` — `See: RESEARCH_LOG.md`, `sync/open_questions.md`, `task/q036_pm_decision_packet_2026-04-26.md`
- 2026-04-25 — `SPEC-072` is now `DONE` on HC (`main` / `3fca17a`). The frontend-only dual-scale / broken-wing visual layer has been implemented, Quant code-level review passed, and PM smoke completed enough to close the spec. Scope stayed strictly in frontend files: shared helpers in `web/static/spec072_helpers.js`, recommendation-card updates in `web/templates/index.html`, research-view / trade-log / legend work in `web/templates/backtest.html`, and margin BP dual-text in `web/templates/margin.html`. This closes the last outstanding implementation item in the HC reproduction sprint from `MC_Handoff_2026-04-24_v3` — `See: task/SPEC-072.md`, `doc/quant_review_spec072_2026-04-25.md`
- 2026-04-24 — `MC_Handoff_2026-04-24_v3.md` has now been accepted as the authoritative MC sync package. It materially extends the aftermath stack beyond HC’s current indexed state: MC reports `SPEC-068 DONE` (per-strategy spell throttle), `SPEC-069 DONE` (`open_at_end` artifact/UI support), `SPEC-070 v2 DONE` (delta-based long-leg alignment), `SPEC-071 DONE` (aftermath broken-wing IC, `LC 0.04 / LP 0.08`, `DTE 45` unchanged), `SPEC-072 MC-side DONE` (frontend dual-scale display, still pending HC deploy), and `SPEC-073 DONE` (dead-code cleanup). These should be treated as HC reproduction targets, not yet HC-canonical facts — `See: sync/mc_to_hc/MC_Handoff_2026-04-24_v3.md`
- 2026-04-24 — MC also surfaced two planning-relevant research governance findings that HC had not yet indexed. First, `Q029` found one material parity issue: the engine hardcodes `qty = 1` and therefore overstates live aftermath notional relative to `SizeTier`; MC’s chosen interim resolution is reporting-layer dual columns (`research_1spx` + `live_scaled_est`), not immediate engine rewrite. Second, `Q019 Phase 1` measured a non-trivial close/open VIX mismatch (`aftermath 4.63%`, `regime 9.71%`, `trend 31.54%` flips), but PM has not yet chosen whether to close, reproduce, or codify that evidence — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-20 — PM surfaced a new follow-up concern on the `Q018 / SPEC-066` line: the intended behavior in a double-spike pattern is not “allow two back-to-back `IC_HV` entries,” but “allow a second `IC_HV` only after a distinct second VIX peak begins to mean-revert.” This raises the possibility that part of the `cap=2 + B` alpha was earned under the wrong semantic, so a new research item `Q020` has been opened to measure how much of the result depends on back-to-back re-entry versus genuine second-peak capture — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-20 — `SPEC-066` review is now closed with **PASS with spec adjustment**, and the spec has been moved to `DONE`. No code changes were needed after Developer handoff: Quant concluded `AC4` was over-constrained at the trade-set level and correctly rewrote it as a logic-level invariant, and `AC10`’s original `[33,40]` artifact-count expectation was simply miscalculated and has been corrected to `[45,55]`. Core implementation stands: `IC_HV_MAX_CONCURRENT = 2`, `AFTERMATH_OFF_PEAK_PCT = 0.10`, the `2026-03-09 / 2026-03-10` double-spike pair is captured, and `2008-09` remains filtered — `See: task/SPEC-066.md`
- 2026-04-20 — Developer implemented `SPEC-066`, regenerated local artifacts, and wrote handoff. Core intent is working: `IC_HV_MAX_CONCURRENT = 2`, `AFTERMATH_OFF_PEAK_PCT = 0.10`, the `2026-03-09 / 2026-03-10` double-spike pair is captured, `2008-09` remains filtered, and system-level PnL / Sharpe / MaxDD all land near target. However, two acceptance criteria remain open: `AC4` (non-`IC_HV` trade-set drift) and `AC10` (research-view count now `49`, above the old expected range). This means `SPEC-066` is implemented but not yet closed — `See: task/SPEC-066_handoff.md`
- 2026-04-20 — `SPEC-066` has been moved to `APPROVED`. The selected `Q018` landing shape is now fixed as `cap=2 + B` (`IC_HV` max concurrent = 2 plus `AFTERMATH_OFF_PEAK_PCT 0.10`), and the next step is standard Developer implementation rather than further Planner-side branch selection — `See: task/SPEC-066.md`, `RESEARCH_LOG.md`
- 2026-04-20 — Quant completed `Q018 Phase 2` and PM selected the final research outcome: `cap=2 + B` (`IC_HV` max concurrent = 2 plus `AFTERMATH_OFF_PEAK_PCT 0.10`). The combined shape materially outperformed either component alone, with expected system improvement around `+$47K`, Sharpe about `+0.02`, and max drawdown nearly flat. This is now strong enough to justify a narrow DRAFT candidate, though it still sits behind `/ES` runtime safeguards in queue priority — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-19 — Quant completed `Q018 Phase 1`: the single-slot aftermath question now has two credible but unresolved directions. Variant A (aftermath multi-slot replay) produced about `+$47,735` across `36` replayed trades with concentrated tail risk in `2008-09`, while Variant B (`AFTERMATH_OFF_PEAK_PCT 0.05 -> 0.10`) improved max drawdown by about `36%` at very low implementation cost. This is still research, not Spec, because the key realism gaps (BP ceiling, shock / overlay interaction, and significance hardening) remain open — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-19 — PM opened a new research track `Q019` around time-basis mismatch: backtests use end-of-day VIX, while live recommendation uses opening / early-session VIX context. This may affect HIGH_VOL / NORMAL routing, `VIX_RISING`, `ivp63`-style gates, and aftermath detection, so it should be studied before any production reinterpretation of recent live behavior — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-19 — `SPEC-064` (`HIGH_VOL Aftermath IC_HV Bypass`) shipped to production and passed review, and `SPEC-065` (`Research View Pill for SPEC-064`) also shipped and passed review. PM review of the real 2026-03 double-VIX-spike case surfaced a new research track `Q018`: the single-slot engine constraint appears to have blocked the second peak’s aftermath opportunity, but that is still a research question rather than a new Spec candidate — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-19 — Quant completed `Q017 Phase 2` and closed the ex-ante recognition question: the live-usable signal is simply the aftermath condition itself, while `peak_drop_pct` and `vix_3d_roc` add no value. Evidence is now strong enough to support a narrow DRAFT candidate focused only on `HIGH_VOL aftermath IC_HV bypass`, with `EXTREME_VOL` preserved as the hard protection layer — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-19 — Quant completed `Q017 Phase 1` and materially upgraded the evidence level: replacing SPX proxies with real strategy PnL produced significantly positive aftermath-window results, and removing the recent `2020-03 / 2025-04 / 2026-04` V-shaped events barely changed the conclusion. The alpha appears concentrated in `IC_HV`, and `Q017` now deserves Phase 2 ex-ante recognition work rather than continued `hold` status — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-19 — Planner formalized `Q017` sequencing: Tier 1 is mandatory first (`real strategy PnL` + `remove 2020/2025/2026 V-shaped events`), Tier 2 only runs if Tier 1 still shows positive evidence, and Tier 3 gate-specific work is explicitly last — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-19 — Quant opened a new research track `Q017` around early post-peak VIX-reversal windows: the phenomenon is structurally real and concentrated in HIGH_VOL filters, but current evidence still relies on SPX forward-return proxies and is dominated by a few modern V-shaped reversals, so the result is `hold`, not Spec — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-19 — Quant shipped the backtest research view (`SPEC-062`) and SPX-chart linkage (`SPEC-063`): historical Q015 marginal trades and Q016 Dead Zone A trades are now persistently viewable on the backtest page. The rollout also exposed and fixed a generator semantic trap where “baseline == current production” collapses marginal-trade diffs once a Fast Path change is already live — `See: RESEARCH_LOG.md`
- 2026-04-19 — Runtime/compute split was clarified as a project rule: old Air remains the canonical live runtime host, but heavy backtests, research view generation, and similar long-running artifact generation should default back to the main machine, with generated artifacts published to old Air only when needed by live web — `See: SERVER_RUNTIME.md`
- 2026-04-19 — Quant completed a Fast Path implementation for `Q015`: `BPS_NNB_IVP_UPPER` in `strategy/selector.py` was raised from `50` to `55` with a code comment pointing back to the OOS evidence. This closes the narrow BPS gate-relaxation candidate and leaves broader IVP / IC redesign questions for future research — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-19 — Quant completed the OOS validation for the narrow BPS gate relaxation from `IVP < 50` to `IVP < 55`: full-history, IS, and OOS slices all showed non-degrading system Sharpe and positive PnL deltas, so `Q015` is no longer just exploratory research and now qualifies as a near-spec / Fast Path candidate — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-18 — Quant completed the Dead Zone B follow-up study inside VIX recovery windows: recovery itself is not a useful conditioning variable, VIX-only / joint filters are not ready, and the only near-spec candidate is a narrow BPS gate relaxation from `IVP < 50` to `IVP < 55` pending out-of-sample confirmation — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-18 — Quant completed the follow-up test on Dead Zone A inside VIX recovery windows and found no significant conditional alpha: `NORMAL + HIGH + BULLISH` recovery-window BPS remains non-significant, so `SPEC-060 Change 3` should stay `REDUCE_WAIT`; the “dead zone” investigation now collapses back into `Q015` / Dead Zone B only — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-18 — Quant confirmed a systemic “VIX recovery window dead zone”: across 66 HIGH_VOL→NORMAL transitions with elevated IVP, 64% of candidate days were blocked by two independent mechanisms (`NORMAL + HIGH + BULLISH` route hole plus IVP gates). Conclusion is `hold`, not Spec: the next required proof is whether Dead Zone A has real conditional alpha without reducing Sharpe — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-17 — `SPEC-060` (`Recommendation Event Log`) completed review and is now `DONE`; structured live recommendation logging is available for bot-triggered recommendation events, and there are still no active approved specs waiting for Developer implementation — `See: task/SPEC-060.md`
- 2026-04-18 — Live runtime was migrated from the main machine to old Air. `bot`, `web`, and `cloudflared` are now `launchd`-managed on old Air, and a dedicated Codex `Server Maintainer` role was defined for runtime operations. All agents should treat old Air as the canonical live host — `See: doc/old_air_server_maintainer.md`
- 2026-04-16 — Quant completed the first full BPS gate study (`Q015`): unlike DIAGONAL Gate 1, the `NORMAL + BULLISH` `IVP >= 50` gate shows a real Sharpe cliff and should be retained for now; the open research question has shifted from “remove or keep” to “how to redesign the filter’s concept basis beyond IVP alone” — `See: RESEARCH_LOG.md`, `sync/open_questions.md`, `doc/strategy_status_2026-04-16.md`
- 2026-04-15 — Quant completed a Fast Path removal of DIAGONAL Gate 1 in `strategy/selector.py`; the former `SPEC-049` `ivp252` marginal-zone gate is no longer active production logic, and the next threshold study has shifted to the `NORMAL + BULLISH` `IVP >= 50` entry gate — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-15 — Quant research on DIAGONAL Gate 1 (`SPEC-049`) concluded the gate is net harmful rather than protective; PM agreed to move toward a narrow follow-up Spec to remove it — `See: RESEARCH_LOG.md`, `sync/open_questions.md`
- 2026-04-12 — `SPEC-044` has reached `DONE`; there are currently no remaining active approved Specs waiting for Developer implementation — `See: task/SPEC-044.md`
- 2026-04-11 — ES short put phased research was indexed as a research-track idea; current recommendation is `hold` until scope is narrowed and proxy assumptions are revisited — `See: research/strategies/ES_puts/spec.md`
- 2026-04-12 — ES short put production-path research was materially updated: `/ES` permissions and live BP were confirmed (`$20,529` per contract), XSP is no longer the preferred path, and the main remaining design question is shared-BP management versus SPX Credit rather than lot-size feasibility — `See: sync/open_questions.md`, `RESEARCH_LOG.md`
- 2026-04-12 — `/ES` minimal production cell (`SPEC-061`) reached `DONE`; latest QR review confirms the MVP entry path works, but also surfaces that runtime stop execution remains manual and Layer 1 / Layer 3 are still intentionally out of scope. PM has clarified that pure manual stop monitoring is not acceptable; minimum follow-up scope is system monitoring plus bot alerting — `See: task/SPEC-061.md`, `sync/open_questions.md`
- 2026-04-12 — HC-side planning status was clarified: Schwab Developer Portal access is no longer a top HC blocker; index-layer summaries were reconciled and the current front-of-queue decision is now the `/ES` minimal production cell — `See: sync/open_questions.md`, `RESEARCH_LOG.md`
- 2026-04-10 to 2026-04-11 — `SPEC-048` to `SPEC-055` verified as `DONE`; latest IVP / DIAGONAL research batch is implemented and no longer belongs in active approved work — `See: doc/strategy_status_2026-04-10.md`
- 2026-03-28 to 2026-03-29 — several stop-loss and entry-filter studies were either implemented or rejected based on full-history backtests — `See: doc/research_notes.md`

## Notes

- This file is the rolling index layer.
- Detailed evidence, tables, and long-form reasoning remain in `doc/` and sync artifacts.
- External architecture-review findings have been absorbed into a **future-review / medium-low-priority backlog** at [doc/architecture_backlog_2026-05-04.md](/Users/lienchen/Documents/workspace/SPX_strat/doc/architecture_backlog_2026-05-04.md:1). It is intentionally not a current blocker list or active refactor program; treat it as a revisit document for later runtime-engineering / reconciliation / observability work.
