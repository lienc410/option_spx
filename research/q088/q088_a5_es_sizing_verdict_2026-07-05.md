# Q088 A5 — ES sizing 三层口径对账 verdict（2026-07-05）

**触发**: T1 考古发现 5/8 对齐审计注记"live trades at 2× the backtested sizing"搁置至今。
**事实对账**（全部代码引用）:

| 层 | 槽位 | 张数 | 入场 DTE | 权益基准 | 出处 |
|---|---|---|---|---|---|
| P1 回测（过时相位） | 1 | BP-target 10% | 45 | $500k | ES_puts/backtest.py:61 |
| **V2f+G6 晋升回测（Q071 验证口径）** | **5** | **3/槽（至多 15 张并发）** | 49 | $500k | :70-75 |
| **Live 生产（SPEC-061）** | **1** | **1（固定）** | 45 | 实际 NLV + 20% BP 上限门 | strategy/es_params.py:21 |

## Verdict

1. **5/8 注记的"live 2× 回测"警报方向错误，正式撤回**：它拿 P1（过时相位）的 10% target 比 live 的 20% ceiling——两个不同语义的量。真实图景：**live 是有意的保守单张规则（SPEC-061，代码注释明示），并发暴露约为已验证 V2f 配置的 1/15**。无安全问题。
2. **真实的失配是叙事级**：Q071 晋升的 "HV Ladder" 经济学（$180.9k/26y @5 槽×3 张）**从未被 live 实施**——live 跑的是 SPEC-061 单张版，期望收益量级约为广告数字的 1/15。策略卡/回测页引用晋升数字时读者会误解 live 在赚那个数。→ 修复：展示层标注"live=SPEC-061 单张；ladder 为已验证未部署配置"（dev 小项）。
3. **给 PM 注册一个真实决策选项（不催）**：已验证的 ladder 配置（26y 全流程验证 + SPEC-121 止损收敛后）是现成的规模化路径——若想提升 /ES sleeve 贡献，阶梯化是"已验证但未部署"状态，启用需走：BP/现金占用重估（15 张并发 ≈ $308k BP）+ 自适应四件套 + 分级 paper-first。在你主动提起前不再重提。
4. **housekeeping（dev）**：`_ES_BP_PER_CONTRACT = 20_529` 硬编码（server.py:87）——ES 保证金随 SPX 水平漂移，需新鲜度机制或从 Schwab 实时读；连同展示层修复入下一 dev 批。

## 处置

A5 CLOSED（对账完成，无 live 风险项）；5/8 注记撤回入档；两个 dev 小项排队；ladder 规模化选项注册（PM-initiated only）。T1 振荡表更新：ES sizing 行由"活失配"改"已对账——叙事级修复排队"。
