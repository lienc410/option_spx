# Q095 P6 Findings — Q042 触发日的表达替换：BPS vs Call Spread

**Date**: 2026-07-12
**Status**: P6 CLOSED（事实层 + 方向判定）——"无条件替代"否决；"现金不足时的条件化 fallback"获数据支持，规则候选已成形
**Files**: `q095_p6_bps_substitution.py` / `q095_p6_bps_sub.csv`
**口径**: 35 次 Sleeve A 触发日，同预算 $78.6k（today-scale 恒定预算历史重放——绝对额为比较用途，同 Q093 惯例）、同 entry/expiry/hold-to-expiry；BPS = 主策略惯例 Δ0.30/Δ0.15；BS FLAT + short 腿 credit −2vp HAIRCUT bracket（`research_bs_flat_vix_pricing_bias` 强制）；**Q062 网格只测过 call 侧，BPS 表达首测**。

---

## 1. 头对头（26y，35 触发）

| 分层 | Call Spread（吃现金） | BPS FLAT（吃 BP） | BPS HAIRCUT（realistic） |
|---|---|---|---|
| **全部 35** | **Σ$1,848k**，WR 74%，worst −$78k | Σ$493k，WR 89%，worst −$76k | Σ$249k |
| episode 型 31 | Σ$1,873k，WR 77% | Σ$594k，WR 94% | Σ$353k |
| **突发型 4** | **−$25k**（capped debit） | **−$101k** | −$104k |
| $/资源-月 | **0.67** | 0.18 | 0.09 |

## 2. PM 假设的分项裁定

- **"vol 高时 credit 更肥" — 对，但不够**：BPS 的 WR 确实更高（89% vs 74%，横盘/小跌也赢），credit 也确实肥。但 dip 触发的本质是反弹捕获：call spread 的凸性吃到反弹全幅，BPS 收入封顶在 credit——**同预算下 call spread 赚 3.7×（FLAT）到 7.4×（realistic）**。作为收益最大化的替代，BPS 完败。
- **"现金不足时用 BPS 替代" — 成立，但只在条件化形态下**：若备选项是"跳过这次 dip-buy"，episode 型触发的 BPS（Σ$353-594k > $0）远好于空仓。fallback 有真实激活场景（Q093：live 1.7x BCD sizing 下池不够）。
- **突发型分层（PM 第 3 点"要重视"）在此变成规则的关键构件**：4 次无 episode 前导的触发（2018-02/2020-02/2020-09/2025-02，崩盘延续型）里 **BPS 亏 4 倍于 call spread**（−$101k vs −$25k，capped debit vs 近 max loss）——短 vol 结构在崩盘延续中挨打是 Principle 1/整条 HV 研究线的老机制，n=4 但机制支撑强。**且该分层 ex-ante 可判**（episode/chop 状态是 trailing 信息，触发当天可知）。

## 3. 规则候选（弹药 fallback，待 PM ratify 后进 spec 通道）

> **Q042 触发时：** 首选 call spread（现有结构不动）。若 liquid cash < 需求（$78.6k today）且触发为 **episode 型**（前 7 日内有 chop episode）→ **BPS fallback**（Δ0.30/Δ0.15，同 expiry，预算按 BP 计）；触发为**突发型**（无 episode 前导）→ **不 fallback，宁可空仓**（BPS 在该型亏更多，且 4 例中 call spread 自己 3 例也亏——空仓是合法答案）。

三个诚实边界：①BPS 侧对定价假设敏感（2vp haircut 砍半利润）——**adoption 前须过 CALIB+成本验证**（memory 强制，本结论 research-grade）；②突发型判定基于 n=4 + 机制——规则上线后每次触发积累 paper 证据（~1.8 次/年，验证周期长，预先接受）；③本模拟无 stop（双方 hold-to-expiry 对称），生产 BPS 带 stop 会改变两侧尾部——CALIB 验证时一并处理。

## 4. 衔接

- 规则候选 + SPEC-094.2 F5b 告警扩展（触发告警加一行：现金充足→call spread / 不足+episode 型→BPS fallback / 不足+突发型→建议空仓）= 一个小 spec（SPEC-094.4 候选），**待 PM ratify 方向**。
- "4 次突发型"的深挖（触发质量分层：episode 型 avg 大赢 vs 突发型 3/4 亏）**不建 entry filter**（n=4 + 循环风险，P2b 附录已登记）——但作为 **fallback 开关**使用时自由度只有 1 个 bit 且有独立机制支撑，与建 filter 不同类。未来样本扩到 n≥8 时复检。
- P4 表达层优化的第一个具体实例落地：同一 delta 观点按资源状态选结构——正是两段式架构"表达层"的原型。

---

## 5. 外审吸收（ChatGPT via PM，2026-07-13）+ 逐笔分带检验（P6b）

外审六点，三收两回一修正（脚本 `q095_p6b_external_review_bands.py`，数据 `q095_p6b_bands.csv`）：

**收下**：①结论是系统内结论（本套触发/预算/hold-to-expiry 下的表达选择），不外推为"暴跌后普适买 vertical"——crash 后卖高 IV 是独立合法策略，本账本由 aftermath 通道（V3-A broken-wing IC）承担，与 dip 通道分工互补（Q066/Q093）；②"call vertical 依赖均值（反弹幅度），BPS 依赖尾部（左尾是否走完）"的分解——**逐带验证为真**：

| 30d 前向收益带 | n | BPS 赢 | CS Σ | BPS Σ |
|---|---|---|---|---|
| <0% | 5 | 5 | −$384.6k | −$211.3k |
| 0~2% | 6 | 4 | −$56.9k | +$90.6k |
| 2~4% | 11 | 1 | +$972.9k | +$169.5k |
| >4% | 13 | 0 | +$1,316.4k | +$200.4k |

③VRP/skew/theta 是真实收入源（已含在 BPS 逐笔 PnL 里；且 crisis 期 flat-VIX credit 的真实性是本比较最弱的定价腿——CALIB 验证 standing 项覆盖）。

**回应**：④条件表是正确的地图、没有可用的罗盘——条件变量是 30d 前向收益本身（不可观测）；入场日可观测特征零分离（VIX 中位 23.7 vs 20.9；BPS 赢组 8/10 是铺垫型——铺垫/突发分层**不能**识别 BPS 赢组，它只路由尾部危害）。预测型 gate 家族在本账本已全谱系阵亡（Q082 P9 / Q089 E2 / Q090 / Q097）——把不可观测条件写进规则 = 拿感觉交易；⑤hold-to-expiry 对称性批评成立且早已登记（§3 边界③），动态管理版 BPS 必须随 CALIB 验证一起做完才准 fallback 首用；但既有证据不支持乐观（rung-stop 压力档 FAIL、MA-stop whipsaw 全灭、日内 stop 0%、roll-delay E4 kill；gap 型崩盘里 EOD 止损买回的正是刚卖出的 skew）。

**修正自己**：⑥P6 原文"突发型 BPS 亏 4×"的机制表述不精确——等预算 maxloss-sizing 下两结构单笔最大亏损相同（皆满预算）；4× 差来自**赢的一侧**：credit 封顶的赢补不回尾部满损，凸性的赢补得回（2018-02 一笔大赢把 CS 突发型合计拉回 −$25k）。不对称在恢复力，不在单笔损失。
