# Q087 P0 C-1 — 代码普查（2026-07-05, Dev）

采集方式:静态 grep/wc + git ls-files,全部命令可复跑。生产代码合计 **~29.9k LOC**(不含 prototype/research)。

## 1. 模块清册

| 包 | 文件数 | LOC | 公共入口 | 依赖方向(出) |
|---|---:|---:|---|---|
| `web/` | 4 | 7,838 | Flask app(`main.py --web`,waitress 8 线程);`server.py` 6,470 行 / **118 个 route** | → strategy(52), schwab(27), backtest(13), etrade(12), signals(9), notify(4), production(2) |
| `strategy/` | 22 | 6,426 | `selector.get_recommendation`(bot+web+backtest 共用);`sleeve_governance.evaluate_candidate`(开仓 gate);ladder/cash/q041/q085 各 sleeve 模块 | → signals(7), schwab(4), etrade(3), notify(3), **web(1)⚠反向** |
| `scripts/` | 24 | 4,236 | 各自 CLI(launchd 调 `-m` 或直接路径) | → etrade(10), schwab(6), strategy(6) |
| `backtest/` | 21 | 3,773 | `engine.run_backtest`(三套缓存的生产者);`q042_engine`;`pricer` | → strategy(20), signals(4) |
| `notify/` | 6 | 2,604 | `telegram_bot.main`(bot 进程);`event_push._send`(进程外推送);q041_t3/q085 每日 job | → strategy(16), signals(7), schwab(5) |
| `signals/` | 7 | 1,558 | `vix_regime.get_current_snapshot` / `iv_rank` / `trend.fetch_spx_history` | 无出向(底层)✓ |
| `schwab/` | 5 | 1,420 | `client.get_option_chain/get_account_*`、`auth` | **web(1)⚠**(实为 `import webbrowser` 误报,见下) |
| `etrade/` | 3 | 1,044 | `client.get_account_*`、`auth`(OAuth 三腿) | 无出向 ✓ |
| `production/` | 4 | 1,037 | `vix_settling`(Signal-2 研究轨) | → strategy(5), signals(2) |

**依赖健康度**:分层大体成立(signals→strategy→web)。反向依赖 **1 处真实**:
- `strategy/sleeve_governance.py:685` → `from web.portfolio_surface import ...`(governance 取 NLV 走了 web 层)。影响:strategy 单测必须能 import web;分拆 server.py 时这条会牵动。**建议 Track C 把 portfolio_surface 的账户聚合下沉到独立层**(如 `accounts/`)。风险:低(行为不变的搬移)。
- `schwab/auth.py:7 import webbrowser` 是标准库,矩阵误报,无问题。
- strategy→notify(3 处 `event_push._send` 告警)属可接受的侧向调用,但统一心跳方案(D-1)落地后可归一。

## 2. 测试覆盖地图

74 个测试文件。按 import 映射(完整表见采集命令输出,此处列结论):

**覆盖良好**:strategy 核心(selector/catalog/governance/ladder/cash_budget,SPEC-103~116 全链)、backtest/engine(十余个 spec 回归+bit-identical 向量)、signals(经 strategy 测试间接)、etrade/client T3 函数(mock)、schwab(auth/scanner/quotes 3 件)。

**零覆盖的生产路径**(按风险排序):

| 路径 | LOC | 风险初判 |
|---|---:|---|
| `web/server.py` 118 routes 中 ~95+ 无任何测试(有测试的集中在 backtest/q041/governance/snapshot 一小撮) | 6,470 | **高**——手动 `json.dumps` 路径曾出 NaN 事故(C-2 #5);position open/close/roll 等资金操作端点无回归网 |
| `scripts/compute_greek_attribution.py` | 725 | 中——日任务,错了污染 attribution 历史 |
| `web/partnership_book.py` | 599 | 中——含 Google Drive IO 与 PII xlsx |
| `scripts/daily_snapshot.py` | 418 | 中——月度聚合(SPEC-110 T2)的上游,schema 变更无守护 |
| `notify/telegram_bot.py` 大部分 handler(仅 SPEC-018/099 触及局部) | 1,691 | 中 |
| `etrade/auth.py` 401-renew 路径(6/11 新逻辑) | 358 | 中——token 生命线,建议补 |
| `production/vix_settling.py` | 516 | 低(research 轨) |

## 3. 重复实现清点

### BS 定价 —— 生产侧 5 份 + 研究侧 5 份(多于 handoff 已知 4 份)

| 位置 | 形态 | 备注 |
|---|---|---|
| `backtest/pricer.py` | 引擎 canonical | Track B 统一目标锚点 |
| `strategy/q042_pricing.py` | scipy norm,含 skew/term 乘子 | Q042 canonical,文档声明与研究脚本一致 |
| `strategy/sleeve_governance.py:_bs_put` | 内嵌,scipy + A&S 双 fallback | SPEC-108.1 压力闸;去重时注意 fallback 语义 |
| `backtest/q042_engine.py` | 内嵌 | |
| `scripts/compute_greek_attribution.py`(+`_reconcile_*`) | 内嵌 | |
| 研究侧 | `q082_p6`、`q082_p9`、`q085_p2d`、`q085_p3`、prototype/ 内约 12 份 | prototype 冻结不动;q082/q085 是 Track B 校准基线,统一库落地后回灌 |

**影响初判**:各份在 r、除息、CDF 近似上不保证一致 → 跨研究结论可比性风险。Track B 单一库 + 冻结向量回归是正解。工作量:中。

### 其他重复

| 主题 | 份数 | 位置 | 初判 |
|---|---:|---|---|
| SPX 历史加载 | 3 条路径 | `signals/trend.fetch_spx_history`(canonical)、`sleeve_governance._latest_market_stress` 直读 pickle、`web/server._get_spx_by_date` 直读 pickle | 直读绕过缓存新鲜度逻辑;低风险高收益的归一 |
| Telegram 发送 | 4 份 | `telegram_bot`(bot 进程)、`event_push._send`(canonical 进程外)、`daily_chain_sanity._send_telegram`、`collect_chains._send_collector_alert_telegram` | 后两份是复制品;D-1 心跳方案落地时并入 event_push |
| JSONL 读写 | 7 份 `_read_jsonl/_append_jsonl` | strategy 5 处 + notify/q085 + intraday_governance | 语义有差(fcntl 锁 vs 无锁;strict-JSON 断言仅 q085 有)——**锁与 NaN 纪律不统一是隐性风险**,建议抽 `common/jsonl.py` |
| 节假日表 | ≥5 份硬编码 `_US_HOLIDAYS` | q078_ladder、daily_chain_sanity、q041_t3_earnings_check、daily_snapshot、journal.html(JS) | 2027 起需逐份维护;抽单一来源(pandas_market_calendars 已在依赖里!) |

## 4. TODO/FIXME/XXX

生产代码 grep 结果:**0 个真实 TODO**(唯一命中是 `backtest/registry.py` 的 `EXP-...-XXXX` 格式字符串)。技术债不在注释里,在上表的结构重复里。
