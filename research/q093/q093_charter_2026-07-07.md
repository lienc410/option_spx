# Q093 Charter — Q042 组合耦合复核：crash-day 现金叠加 + co-fire co-loss 欠账

**Date**: 2026-07-07
**Owner**: Quant Researcher
**Status**: P1 CLOSED（2026-07-07 当日，见 `q093_p1_findings_2026-07-07.md`——verdict 与 charter 预设反向：**不纳入** SPEC-111 闸门，改两条零挡单运营规则）；P2 OPEN；P3 可选
**Provenance**:
- Q066 closure（2026-05-12）立的 standing trigger——「Q042 paper→live / cap 上调 → 立即启动 co-fire 深研」——已于 2026-05-17 被 SPEC-104 同时满足两条件（10%→12.5% staged production），至今未执行。原 hook 指向的编号 Q067 已被 IVP jitter 研究占用并关闭，故本研究以 Q093 立项。
- 2026-07-07 协同审计（`doc/q042_aftermath_synergy_audit_2026-07-07.md`）发现 Q042 现金占用零治理覆盖（P1 #4），Q092 reaudit ④「合成栈自洽」未覆盖此角。

---

## 研究问题

**RQ1（P1，现金侧）**：Sleeve A trigger 日的 Q042 debit 需求叠加到 SPEC-111 现金池上，历史上会产生多少次 cap/floor 冲突？今日规模下的绝对值是多少？
**RQ2（P2，co-fire 侧）**：post-2020 时代，aftermath 与 Q042 co-active 窗口的同向亏损（co-loss）在 PnL 层有多大？Q066 的 "low-overlap / non-redundant" 结论在 cap 12.5% 生产口径下是否仍然成立？

## P1 — crash-day 现金叠加重放

**方法**：复用 Q092 P0 重放框架（`research/q092/q092_p0_composed_stack_replay.py`）。在既有 debit 流（BCD + T2/T3）之上叠加 Q042 Sleeve A 触发流：

- trigger 日与持有期：`data/q042_backtest_trades.csv`（SPEC-094.1 后 n=35，D30 结构，持有 entry+30）
- 每笔现金占用：`12.5% × basis`；basis 双口径跑——(a) canonical $500k（与 Q092 主表可比）、(b) **today-scale 绝对值**：Schwab NLV $629k（2026-07-07 executor 实测）→ 单笔 ≈ $78.6k vs 今日池 $152k（`feedback_absolute_at_today_scale_not_historical_ratio` 强制项）
- 现金池网格：沿用 Q092 {37k, 90k, 152k, 250k}
- 输出：Q092 §1 表追加「+Q042」行 ×4 池；trigger 日 cash-stack 案例清单（BCD 在场 × Q042 需求 × 池余量逐日）

**判定**：若在今日池 + 今日 sizing 下任一历史 trigger 日出现「Q042 需求 + 在场 debit > 60% cap 或触 $30k floor」，则 SPEC-111 universe 修订（Q042 纳入 `CASH_OCCUPYING_STRATEGIES`，或 executor sizing 对 liquid cash 取 min）从「建议」升「必需」，出 SPEC-111 修订设计稿。

**注意**：此为 execution-constraint 类决策（对照 `feedback_decision_type_governs_significance_standard`）——尺子是「vs 现状是否产生真实冲突」，不要求统计显著性。

## P2 — co-fire co-loss（Q066 欠账）

**方法**（METHODOLOGY.md v1.1 约束下）：

1. **事实先于反事实**：先重建 19y aftermath window × Q042 position window 的 co-active 日历（day-level 与 event-level），分时代（pre/post-2020）呈现频率——更新 Q066 的 0.9% 数字到 SPEC-094.1 D30 结构（30 天持有期 vs 原 90 天，co-active 天数应显著下降，需实测）。
2. co-active 日的两 sleeve **日 PnL 联合分布**：同向亏损频率、co-loss 日合计 PnL 分位（p95/p99/CVaR）、worst co-window。生产口径 sizing（aftermath 按 SPEC-066 baseline、Q042 按 12.5%）。today-scale 绝对值强制。
3. 时代分层呈现（`feedback_adaptive_posture_no_allweather_gate`）：post-2020 与全样本分列，不合并结论。
4. **不做** gate/cutpoint 优化（防 `feedback_stratum_cutpoint_overfit`）；本研究是 risk-audit，输出是「co-loss 暴露账本」+ 是否需要联合 cap 的 go/no-go，不是新信号。

**判定**：若 post-2020 口径下 worst co-window 合计亏损 > 单独 sleeve worst 的 1.5×（Q072 P3.3 lift 2-6x 的方向在 PnL 层复现），则提「aftermath + Q042 联合暴露 cap」设计稿供 PM 决策；否则记录并 close（cash 侧结论不受影响）。

## P3（可选尾巴）— aftermath 分层 fact-check

审计 #8/#9 的两个事实问题（BEARISH aftermath 触发日 backwardation 占比；触发日按 vix_trend RISING/FALLING 分层的频率），**只出分布，不出反事实 PnL**。若分布显示暴露可忽略（<5% 触发日）则就地 close。

## 数据与工具

全部本地：`q042_backtest_trades.csv` / Q092 重放脚本与 CSV / 主引擎回测交易流（aftermath 标记）/ `research/q072/q072_p1_daily_flags.csv`。无需新数据采购。

## 失败模式

- Q042 回测流是 D30 新结构 n=35，co-active 样本可能个位数 → 只报事实频率与逐笔账，不做分布推断（`feedback_bootstrap` 位数不足时不硬拟合）。
- 现金池平稳假设沿袭 Q092 局限（PnL 不回池），方向保守，结论措辞对齐。

## 交付

1. `q093_p1_cash_stack.csv` + memo §（Q092 表追加行 + 案例清单 + SPEC-111 修订 go/no-go）
2. `q093_p2_cofire_ledger.csv` + memo §（co-active 日历 + co-loss 账本 + 联合 cap go/no-go）
3. 若 P1 = go：SPEC-111 修订设计稿（Quant 设计内容 → Planner packaging）
