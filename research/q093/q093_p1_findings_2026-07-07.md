# Q093 P1 Findings — Q042 crash-day 现金叠加重放

**Date**: 2026-07-07
**Status**: P1 CLOSED — 主问题已回答；verdict 与 charter 预设方向相反（见 §0 reframing）
**Files**: `q093_p1_cash_stack.py` / `q093_p1_cash_stack.csv`（48 rows：2 窗口 × 4 池 × 2 sizing × 3 情景）
**口径**: 主引擎 debit 流 = canonical $500k 回测（复现 Q092 基线 ✓：$152k 池 107/107 pass）；Q042 = Sleeve A D30 流（n=35, 19y），生产 sizing 双口径 canonical $62.5k / **today-scale $78.6k**（12.5% × Schwab NLV $629k，2026-07-07 executor 实测）；Sleeve B 生产 cap 0% 不入流。

---

## 0. Reframing（charter 预设被数据推翻）

Charter 预设方向是「若冲突则把 Q042 纳入 `CASH_OCCUPYING_STRATEGIES`」。重放结果：**naive 纳入是净大负；现状（完全隔离）在今日池 + 引擎 sizing 下历史零透支、零挤出，是三个方案里 PnL 最优**。P1 verdict 因此不是「如何治理 Q042」而是「何时才需要治理 Q042」——答案：池 < ~$100k 或 live sizing 维持 1.7x 时。

## 1. 三情景 × 今日池（$152k）× today sizing（$78.6k）， 19y

| 情景 | 语义 | Q042 通过 | Q042 PnL foregone | 主 debit 挤出 Δ | 主 PnL foregone Δ | 池抽穿 |
|---|---|---|---|---|---|---|
| **ISO（现状）** | Q042 免闸、对主闸不可见 | 35/35 | $0 | +0 | $0 | **0 次**（max combined committed $97.5k < $152k） |
| VIS（单向可见） | Q042 免闸但 committed 计入主闸 | 35/35 | $0 | **+8** | **+$36,016** | 0 次 |
| GOV（naive 纳入） | Q042 同过 60% cap/$30k floor | **32/35** | **+$243,759** | +7 | +$37,338 | 0 次 |

**机制**（案例明细）：GOV 挡掉的 3 笔——2021-05-13（+$74.5k）、2021-12-02（+$78.2k）、2024-07-25（+$91.0k）——全部是**普通 BCD 在场时的 -4% dip 触发**，不是危机期。60% cap 语义天生装不下「1 笔 BCD $22k + 1 笔 Q042 $78.6k」共存（需求 $97.5k > 60% × $130k = $78k allowance）。D30 结构 74% WR 下，被挡的 dip-buy 是系统性大赢家。VIS 则反向挤出 8 笔 $22k BCD（净 +$36k），同样是为一个在今日池不存在的透支风险付费。

## 2. 风险真正出现的两个条件

**条件 A — 池水位 < ~$100k**（重放实证）：

| 池 | ISO 抽穿事件（19y, today sizing） | ISO overdraft（Q042 需卖资产入场） |
|---|---|---|
| $152k | 0 | 0 |
| $90k | **8** | 1 |
| $37k | **42** | 35（全部） |

**条件 B — live BCD sizing 1.7x**（算术，非重放）：live 实况 2 笔 BCD × $38.3k = $76.6k；叠加一次 Q042 触发 $78.6k → **$155.2k > $152k 今日池**——今日实盘 sizing 下 Q042 触发即抽穿，历史重放看不到它只因引擎流 BCD 从不并发（Q092 发现 2：live sizing 把账户推进历史上不存在的并发状态）。**Q092 留给 PM 的 (a)/(b) 选择（接受单笔 BCD vs 回 $22k 换并发）现在多了 Q042 维度的权重：选 (a) 则 Q042 触发日必须卖资产，选 (b) 则三方共存无虞。**

## 3. Verdict

1. **不把 Q042 纳入 SPEC-111 闸门**（gate 形式否决）。GOV 相对现状（ISO）的净成本 = Q042 侧 −$243,759 **加** 主侧多挡 7 笔 −$37,338 = **−$281k / 19y**——主侧 foregone 是 GOV 的代价而非"保护"（ISO 下主侧 Δ=0，且今日池 overdraft=0，闸门根本没有可保护对象）。*（2026-07-08 更正：初稿误写「-$244k vs 保护 +$37k，净 -$207k」，系聚合符号错误，2nd Quant review N4 抓出；方向不变、否决更强。）* charter 判定条件「今日池+今日 sizing 冲突」在引擎 sizing 口径未触发。
2. **不加 VIS 单向可见**（纯代价 -$36k，无透支可防）。
3. **改为两条零挡单的运营规则**：
   - **R-a（并入 SPEC-094.2 F5 alert 增强）**：Q042 触发告警正文附当前 liquid cash 读数与在场 debit 合计，PM 人工执行时可见池余量——顺序风险由「PM 看得见」覆盖，不由闸门覆盖；
   - **R-b（池水位规则，Q092 操作含义加强版）**：liquid pool 维持 ≥ $150k 时三方（主 debit × Q042 × SPEC-111）自洽；池落入 <$100k 区间时，Q042 触发日预期需要卖 QQQ/SGOV 入场（crash-day 卖资产成本自担）或 PM 主动跳过该次 overlay——这是 sizing/水位的事实约束，先记账，不设自动规则。
4. live BCD sizing 1.7x 问题升权重（§2 条件 B），归 Q092 已开的 PM 决策项，本研究不重复立项。

## 4. 局限

- 现金池平稳假设沿袭 Q092（PnL 不回池、无调度），方向保守。
- Q042 流为 D30 回测流 n=35，engine BCD 流为 canonical sizing——live 1.7x 情形只做了算术核对（§2B），未做全重放（若 PM 选择维持 1.7x，可补一轮 live-sizing 重放，工作量小）。
- 被挡笔 PnL foregone 含全额赢家（GOV 3 笔恰全为赢家），无对称亏损抵扣——数字按实际发生呈现，非期望值。
- Sleeve B 若未来升产（>0% cap），本结论需重跑。

## 5. 队列衔接

- P1 → **CLOSED**；SPEC-111 universe 修订 **不立项**（gate 形式否决）；R-a 并入 SPEC-094.2（F5 alert 增强一行）；R-b 写入池水位操作含义（与 Q092 §1 操作含义合并读）。
- P2（co-fire co-loss，Q066 欠账）：未开工，按 charter 继续。
- P3（aftermath 分层 fact-check）：未开工，可选。
