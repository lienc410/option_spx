# HC Return — 2026-04-16

**角色**：HC Quant Researcher / Planner  
**周期**：2026-04-08 ~ 2026-04-16  
**协作方**：PM（lienchen）/ Developer  
**用途**：本包为给 `MC` 的整合版 return。由于 `MC` 尚未同步 `HC_return_2026-04-12.md`，本文件已吸收 2026-04-12 之后的重要变化，`MC` 可直接从这里接手，不必先拼接旧包与增量。

---

## 本轮最重要变化

### 1. `/ES` minimal production cell 仍为既成事实，但后续重点没有改变

| SPEC | 标题 | 当前结论 | Status |
|------|------|----------|--------|
| SPEC-061 | `/ES` Short Put Minimal Production Cell | 已完成；MVP 入场路径成立，但 runtime safeguard 仍缺 | DONE |

当前 HC 侧结论仍是：
- `SPEC-061` 已把 `/ES` short put 最小 Layer 2 production cell 落地到：
  - 单槽
  - `1` 张
  - `45 DTE`
  - `20 delta`
  - `trend filter`
  - `BP <= NLV 20%`
- 当前最大剩余缺口不是入场逻辑，而是运行时风控：
  - `-300%` stop 仍未系统化自动执行
  - 生产侧最低要求不是人工盯仓，而是 **系统监控 + bot alert**

这仍对应：
- `Q012` — `/ES` 路径与 shared-BP 管理
- `Q013` — `/ES` runtime stop / post-entry 管理 follow-up

### 2. `SPEC-044` 已 DONE，当前没有 active approved spec 等待 Developer

和 2026-04-12 相比，一个重要状态变化是：
- `SPEC-044` 已 `DONE`
- 当前 `Active APPROVED Specs = None`

对 MC 的含义：
- 目前没有“直接交给 AMP Developer 开工”的 approved spec
- 当前前排工作重新回到 research / planning

### 3. DIAGONAL Gate 1 已被 Fast Path 删除

这是 2026-04-12 之后最重要的生产逻辑变更。

对象：
- `SPEC-049` 的 DIAGONAL Gate 1
- 旧规则：`ivp252 ∈ [30, 50] -> REDUCE_WAIT`

研究结论：
- 敏感性平坦：`gate_hi` 在 `40~65` 上无 cliff
- 被拦交易显著为正：`47` 笔，avg `+$987`，bootstrap CI `[+$574, +$1,751]`
- 系统净成本：约 `-$11,146`
- 属于已确认的负向选择偏差案例

执行结果：
- Quant 已在 `strategy/selector.py` 直接删除 Gate 1 分支（Fast Path）
- 当前生产逻辑只保留 Gate 2（`IV=HIGH`）及其余 LOW_VOL + BULLISH 有效逻辑

当前索引层状态：
- `Q014` 已归档为 done / resolved

### 4. BPS `NORMAL + BULLISH` `IVP >= 50` gate 已完成首轮完整研究，结论是“保留 gate，但转入 redesign 研究”

这是 2026-04-16 新增的主要研究结论。

研究对象：
- `NORMAL + BULLISH` 下的 BPS entry gate：`IVP >= 50 -> REDUCE_WAIT`

结论与 Gate 1 不同：
- 这里 **存在真实 cliff**
  - `IVP 55 -> 60` 时 Sharpe 从 `0.53 -> 0.23`
- blocked trades **不显著为正**
  - avg `+$7`
  - bootstrap CI `[-$601, +$1,062]`
- 若直接删 gate：
  - BPS Sharpe `0.49 -> 0.22`
  - MaxDD 恶化约 `70%`

因此：
- 当前 gate=50 **应保留**
- 但研究同时发现一个更深层问题：
  - 在 NORMAL regime 中，IVP 与 VIX 绝对水位弱负相关（`r = -0.154`）
  - `68%` 的 `IVP >= 50` 拦截发生在 `VIX < 18`
  - 也就是说，这个 gate 保护了真实 cliff，但“stressed vol”这套概念解释并不成立

所以 `Q015` 的最新理解不是：
- “是否删 gate？”

而是：
- “如何 redesign 这个 filter 的概念基础，使其不只是靠单维 IVP 碰巧挡住 cliff？”

当前后续研究方向：
1. `IVP + VIX` 联合 filter
2. VIX 趋势条件化
3. BPS dual-slot / slot-occupancy 架构优化

---

## 文档与索引层更新

### 索引层当前状态

| 文件 | 当前状态 |
|------|----------|
| `PROJECT_STATUS.md` | 已更新到 `2026-04-16`；最新 full strategy status 指向 `doc/strategy_status_2026-04-16.md` |
| `RESEARCH_LOG.md` | 已包含 Gate 1 删除结论与 BPS gate 首轮完整研究结论 |
| `sync/open_questions.md` | 已将 `Q014` 归档；`Q015` 改写为“初轮完成，进入 redesign research” |

### 详细层当前关键入口

| 文件 | 用途 |
|------|------|
| `doc/research_notes.md` | 新增 §54、§55，记录 Gate 1 删除和 BPS gate 研究完整过程 |
| `doc/strategy_status_2026-04-16.md` | 当前最新策略状态快照；已吸收 Gate 1 删除、BPS gate 保留、方法论总结 |

---

## 系统当前状态快照（截至 2026-04-16）

### 当前项目阶段

- `stable`
- `research-driven`

### 当前生产姿态

- 保持现有主策略矩阵
- 只接受研究证据充分、低 regret 的小步变化
- `/ES` MVP 已存在，但未达到最终生产安全要求

### 当前 active approved spec

- None at the moment

### 当前前排事项

1. `Q013`
   - 为 `/ES` 补 runtime safeguard follow-up
   - 最小范围仍是：stop-condition monitoring + bot alert
2. `Q015`
   - BPS gate redesign research
   - 不是删 gate，不是立刻进 Spec
3. `Q002 / Q003`
   - dependency-bound legacy priorities remain open

### 当前高相关 open questions

| 编号 | 状态 | 含义 |
|------|------|------|
| Q012 | open | `/ES` 路径已成立；剩余问题是 shared-BP 与 MVP 边界 |
| Q013 | open | `/ES` runtime stop 监控与 bot alert 尚未定义 |
| Q015 | research | BPS gate 初轮研究已完成；下一步是 redesign，不是删除 |
| Q002 | open | Shock active mode 仍待 Phase B |
| Q003 | open | L3 Hedge v2 仍待独立 SPEC |
| Q011 | open | regime decay DIAGONAL live sample 仍小 |

### 已解决但 MC 需要知道的事项

| 编号 | 结论 |
|------|------|
| Q014 | DIAGONAL Gate 1 已删除（Fast Path） |
| SPEC-044 | DONE |
| SPEC-061 | DONE，但后续 `Q013` 仍 open |

---

## 对 MC 最重要的理解更新

### A. 不要再把 Gate 1 当成 open question

对 `MC` 来说，`Q014` 已经结束：
- 不是候选方向
- 不是待拍板问题
- 是已生效事实

### B. `Q015` 不是“下一个 Fast Path 删除对象”

尽管 `Q015` 起点看起来与 Gate 1 类似，但结论完全不同：
- `Gate 1`：平坦 + blocked 显著正 -> 删除
- `BPS gate`：有 cliff + blocked 非显著 -> 保留

因此不要把两者混成同一类问题。

### C. `/ES` 当前仍卡在 runtime safeguard，不是卡在 entry logic

如果 MC 明天继续 `/ES`，重点仍然是：
- `Q013`
- 不要回头重做 `SPEC-061` entry logic

---

## MC 接手建议读取顺序

MC 环境仍按：
- `Claude Quant`
- `AMP Developer`

模式工作，不需要先完整读取 HC 的全部角色拆分入口。

### 若明天做项目全局接手

按顺序建议读取：
- `sync/hc_to_mc/MC_CLAUDE_INSTRUCTIONS.md`
- `sync/hc_to_mc/HC_return_2026-04-16.md`
- `PROJECT_STATUS.md`
- `RESEARCH_LOG.md`
- `sync/open_questions.md`

### 若明天重点是 `/ES`

再补读：
- `task/SPEC-061.md`
- `doc/strategy_status_2026-04-16.md`

### 若明天重点是 threshold / route 研究

再补读：
- `doc/research_notes.md` §54–55
- `doc/strategy_status_2026-04-16.md`

---

## 需 MC / PM 拍板的事项

### 1. 是否立刻为 `Q013` 起新的 DRAFT Spec

HC 当前建议仍是：
- **建议起**
- scope 保持很窄，只补：
  - stop 监控
  - bot alert
  - post-entry 管理定义

不建议带入：
- Layer 1
- Layer 3
- leverage
- Strategy #3

### 2. `Q015` 是否需要被提升为正式专题研究主线

HC 当前建议：
- 可以继续研究
- 但不要直接进入 Spec
- 除非后续出现足够强、可解释、且不明显 overfit 的 redesign 方案

---

## 不要推断的项目

- 不要把 `SPEC-061 DONE` 误解为 SoMuchRanch 三层体系整体已落地
- 不要把 `Q015` 误解为“gate 50 应删”
- 不要把 Gate 1 删除外推成“所有 IVP 整数阈值都应删除”
- 不要把人工止损当作可接受默认前提；PM 已明确否定
- 不要把 `Q012`、`Q013`、`Q015` 混成一个问题：
  - `Q012` 偏 `/ES` shared-BP / 路径边界
  - `Q013` 偏 `/ES` runtime risk management
  - `Q015` 偏 BPS route filter redesign research

---

## MC 明日建议起手动作

### 若明天继续 `/ES`

1. 先确认是否接受 HC 对 `Q013` 的收缩建议
2. 若接受，直接起一个新的 DRAFT Spec
3. 不要把 Layer 1 / Layer 3 / Strategy #3 一起带入

### 若明天继续阈值 / 路由研究

1. 先把 `Q015` 视为 redesign research，不是 gate-removal candidate
2. 读取 §54–55 与 `strategy_status_2026-04-16`
3. 若继续推进，优先验证：
   - `IVP x VIX` 风险口袋是否 OOS 稳定
   - VIX-trend conditioning 是否有真实可解释增量
   - slot-occupancy 改造是否比 filter 改造更本质

### 若明天只做状态整理

1. 使用 `PROJECT_STATUS.md` / `RESEARCH_LOG.md` / `sync/open_questions.md`
2. 只在需要详细证据时再回到 `doc/research_notes.md` 或 `doc/strategy_status_2026-04-16.md`

---

*Generated on HC — 2026-04-16*
