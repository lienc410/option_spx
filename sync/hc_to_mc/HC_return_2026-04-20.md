# HC Return — 2026-04-20

**角色**：HC Planner / Quant Researcher  
**周期**：2026-04-19 ~ 2026-04-20  
**协作方**：PM（lienchen）/ Developer  
**用途**：本包是给 `MC` 的增量 return。`MC` 已经同步到 `HC_return_2026-04-19.md`，因此本文件只覆盖 4/19 之后的新变化。

---

## 本轮最重要变化

### 1. `SPEC-066` 已正式收口为 DONE

对象：
- `SPEC-066` — `HIGH_VOL Aftermath IC_HV Multi-slot + Tightened Off-peak Threshold`

最终状态：
- Quant review 结论：**PASS with spec adjustment**
- `Status: DONE`

关键点：
- 不需要 Developer 补改代码
- 真正成立的实现事实保持不变：
  - `IC_HV_MAX_CONCURRENT = 2`
  - `AFTERMATH_OFF_PEAK_PCT = 0.10`
  - `2026-03-09 / 2026-03-10` 双峰案例被捕捉
  - `2008-09` 仍被过滤

需要注意的地方：
- 两个看似失败的 AC（AC4 / AC10）最后被认定为 **spec 表述问题**，不是实现错误
- AC4：原来要求非 `IC_HV` trade-set 恒等，约束过严；已改成逻辑级 invariant
- AC10：原 artifact count 区间算错，已从 `[33,40]` 改到 `[45,55]`，实测 `49` 正确

对 MC 的意义：
- `Q018` 不再是 open research
- `SPEC-066` 是已落地事实
- 但它的 alpha 含义出现了新的后续研究问题（见 `Q020`）

### 2. 新开 `Q020`：`SPEC-066` 可能答对了容量问题，但答错了“第二次机会”的语义

PM 在复盘 `2026-03` double-spike 真实案例时，提出了一个新的、更细的研究问题：

当前 `SPEC-066` 抓到的是：
- `2026-03-09` — `IC_HV`
- `2026-03-10` — `IC_HV`

但这两笔是 **back-to-back 连续开仓**。

PM 真正想要的可能是：
- 第一峰后的机会没有完整吃到
- 等第二个峰值形成后
- 再抓第二峰回落后的第二次 `IC_HV`

所以新问题不是“能不能开两笔”，而是：

> `SPEC-066` 的增量 alpha 中，有多少来自语义正确的 distinct second peak，又有多少只是同峰后的紧邻 back-to-back re-entry？

当前状态：
- `Q020` 已登记为新 open research
- **不会自动推翻 `SPEC-066`**
- 但会限制我们如何解释 `SPEC-066` 的收益来源

### 3. `Q019` 仍保持 open

`Q019` 没有新研究结果，但仍然有效：
- backtest 使用收盘口径 VIX
- live recommendation 使用开盘 / 早盘 VIX
- 这个时间基准差异仍可能影响：
  - `HIGH_VOL / NORMAL`
  - `VIX_RISING`
  - aftermath 条件
  - `SPEC-064` 一类 HIGH_VOL cells 的解释

当前 HC 侧判断：
- 如果继续 HIGH_VOL 研究线，`Q020` 的优先级已高于 `Q019`

### 4. 详细层快照已升级到 2026-04-20

HC 新增了两份详细层状态文档：

- [doc/strategy_status_2026-04-20.md](/Users/lienchen/Documents/workspace/SPX_strat/doc/strategy_status_2026-04-20.md:1)
- [doc/system_status_2026-04-20.md](/Users/lienchen/Documents/workspace/SPX_strat/doc/system_status_2026-04-20.md:1)

它们分别补充了：

**strategy status**
- `SPEC-064` 已成为当前 HIGH_VOL 生产逻辑的一部分
- `SPEC-066` 已成为当前 `IC_HV` 多槽位 / tightened aftermath 的生产事实
- `Q020` 是对 `SPEC-066` 语义的后续研究，而不是对其实现状态的否认
- `Q019` 作为 VIX 时间基准研究正式挂入当前策略语义上下文

**system status**
- old Air runtime split 已成为当前正式系统架构
- `SPEC-060` recommendation event log 已成为审计链路一部分
- `SPEC-062 / 063 / 065` 已把 research views 固化到 web / artifact 层
- `SPEC-064 / 066` 的实现状态和系统边界已被纳入当前系统快照

---

## 当前索引层变化

### `PROJECT_STATUS.md`

新增 / 更新要点：
- `SPEC-066` 已 DONE
- 新增 `Q020`
- `Next Priorities` 现在变成：
  - `P1` `/ES` runtime safeguard (`Q013`)
  - `P2` `Q020`
  - `P3` `Q019`

### `RESEARCH_LOG.md`

新增：
- `R-20260420-02` — `SPEC-066` review closeout
- `R-20260420-03` — `Q020` opened

### `sync/open_questions.md`

更新：
- `Q018` → `resolved into SPEC-066 (DONE)`
- 新增 `Q020`

---

## 当前项目状态（给 MC 的最短理解）

### 已经是生产事实的

| 项目 | 当前状态 |
|------|----------|
| `SPEC-061` `/ES` minimal cell | DONE |
| `SPEC-064` HIGH_VOL aftermath IC_HV bypass | DONE |
| `SPEC-066` IC_HV max concurrent 2 + off-peak 0.10 | DONE |
| `Q015` BPS gate `50 -> 55` | Fast Path 已落地 |

### 仍然是 open / research 的

| 编号 | 当前状态 |
|------|----------|
| `Q013` | `/ES` runtime safeguard，仍是前排 |
| `Q019` | VIX close/open 时间基准差异 |
| `Q020` | `SPEC-066` 的第二笔语义是否设错 |

---

## 对 MC 最重要的提醒

1. **不要把 `Q018` 当成还没拍板的问题**
   - 它已经通过 `SPEC-066` 落地并 DONE

2. **也不要把 `Q020` 理解成“SPEC-066 被推翻了”**
   - 当前代码仍然是 canonical 生产事实
   - `Q020` 只是在研究其 alpha 归因是否混入了错误语义

3. **如果明天要继续 HIGH_VOL 研究线**
   - 优先顺序已从 `Q019` 改成 `Q020 -> Q019`

4. **如果明天要继续 `/ES`**
   - 仍然不要回头重做 `SPEC-061` entry logic
   - 当前真正前排还是 `Q013`

---

## MC 建议读取顺序

1. `sync/hc_to_mc/MC_CLAUDE_INSTRUCTIONS.md`
2. `sync/hc_to_mc/HC_return_2026-04-20.md`
3. `PROJECT_STATUS.md`
4. `RESEARCH_LOG.md`
5. `sync/open_questions.md`

若要补详细层：
6. `doc/strategy_status_2026-04-20.md`
7. `doc/system_status_2026-04-20.md`

