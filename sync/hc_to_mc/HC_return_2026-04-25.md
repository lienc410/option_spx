# HC Return — 2026-04-25

**角色**：HC Planner / Quant Researcher
**周期**：2026-04-24 ~ 2026-04-25
**协作方**：PM（lienchen）/ Developer (Codex)
**用途**：MC v3 handoff 复现回程包。HC 已完成 4 个可实施 SPEC，第 5 个（SPEC-072）需要 MC 补 handoff 文件后才能继续。

---

## 1. MC v3 handoff 复现进度

HC 在 branch `hc-reproduction-2026-04`（anchor tag `pre-spec070-baseline-2026-04-24`，commit `a7f938e`）上完成了五个 SPEC 中的四个：

| SPEC | 描述 | Commit | 状态 |
|---|---|---|---|
| SPEC-073 | `BEAR_CALL_DIAGONAL` dead-code 清理（HC 自查发现）| `4cbaeae` | DONE |
| SPEC-070 v2 | IC long legs → delta-based δ0.08 | `f69b840` | DONE |
| SPEC-068 | per-strategy HV spell budget（scalar → dict）| `127e741` | DONE |
| SPEC-069 | `open_at_end` reporting + UI badge | `6b68e68` | DONE |
| SPEC-071 | aftermath IC_HV → broken-wing V3-A | `8b79a47` | DONE |
| SPEC-072 | frontend dual-scale + broken-wing visual | — | **BLOCKED** |

完整 Quant 验证报告：`doc/quant_validation_2026-04-25.md`
全量 baseline 工件：`doc/baseline_post_spec0{70,68,69,71}/`

### 关键 cascade（anchor → 当前）

| 阶段 | closed | open_at_end | total PnL | Sharpe | MaxDD |
|---|---|---|---|---|---|
| pre-070 baseline | 59 | n/a | 93,890.04 | 2.36 | -9,807.63 |
| post-SPEC-070 | 59 | n/a | 79,736.85 | 2.09 | -9,391.92 |
| post-SPEC-068 | 60 | n/a | 81,464.82 | 2.13 | -9,391.92 |
| post-SPEC-069 | 58 | 2 | 80,765.71 | 2.14 | -9,391.92 |
| post-SPEC-071 | 58 | 2 | 73,748.04 | 1.97 | -9,391.92 |

注：
- 070 → 068：per-strategy budget 修复释放 1 笔 `BPS_HV` (2025-05-02 → 2025-05-13, +$1,727.97)
- 068 → 069：closed 数量 -2 是 bookkeeping，不是经济变化；2 笔尾仓位被显式标为 `open_at_end`
- 069 → 071：-$7,017.67 全部集中在 `IC_HV` (avg 1620 → 919)；10 个 IC_HV entry 日期未变；说明 aftermath classifier 在运行时覆盖了 5 个 vol-spike 窗口，不只是文档中提到的 2026-03 双峰

---

## 2. 需要 MC 这边回应的两件事

### 2.1 SPEC-072 handoff 文件缺失（BLOCKED_BY_HANDOFF）

MC v3 handoff 引用的 `task/SPEC-072_deploy_handoff.md` 在 HC repo **不存在**。HC 自查后确认：

- HC 当前 frontend 主入口在 `web/templates/`（如 `index.html` / `backtest.html` / `margin.html`），**没有** `web/html/spx_strat.html`
- 因此 MC handoff 中提到的"对 `web/html/spx_strat.html` 的具体 HTML / JS diff"无法直接套到 HC

请 MC 补传以下三项内容（HC 拿到之后再开 Developer 通道实施 SPEC-072）：

1. `task/SPEC-072_deploy_handoff.md` 全文（或其等价内容）
2. **目标文件名 / 路径在 HC 的映射**：MC 端的 `web/html/spx_strat.html` 在 HC 应改写到哪个文件（HC 推测是 `web/templates/backtest.html`，需 MC 确认或给出新路径）
3. 5 个 smoke test 场景的具体定义（场景输入 / 预期 visual / 通过条件）
4. live-scaled 换算因子确认：HC 当前理解是 `XSP = SPX / 10`，仅做 PnL / BP / credit 数值缩放、strike 不缩放。请 MC 确认是否还有其他 scaling factor 或边界规则

在 1–4 全部到齐之前，HC 这边不会基于猜测开始 SPEC-072 实施。

### 2.2 Aftermath IC_HV short delta 0.16 → 0.12 治理溯源

HC 在审 SPEC-071 草稿时发现：MC v3 handoff 在落 V3-A 的同时，把 aftermath 路径的 short delta 从 `0.16` 调到了 `0.12`。这个调整：

- **不在原 SPEC-071 V3-A 描述范围内**（V3-A 名义上只动 long legs：LC 0.04 / LP 0.08）
- **在 HC 端没有对应的 SPEC 起草记录或 PM 批准链**
- 但 HC 这一轮已经在 SPEC-071 草稿与实施中接受了 `0.12`，并量化了影响（IC_HV avg PnL `1620 → 919`，全 -$7,018 PnL hit 来自这两个改动联合作用）

需要 MC 这边补：

1. **谁在 MC 端发起了 0.16 → 0.12 的调整**（PM？Quant？Developer 自行？）
2. **触发依据**：是基于 backtest result、live PnL、还是 V3-A 设计本身要求 short 收紧来配合 LC 0.04 的 risk profile？
3. **是否应该独立写成 SPEC**（HC 倾向：是，建议 MC 端补一份 governance trace SPEC，标注事后追认）

HC 当前不会回滚 0.12 实施 — 它已经是 HC `hc-reproduction-2026-04` 分支的事实。但下次 sync 之前 MC 应给出溯源。

---

## 3. 顺手修订的 errata

`doc/hc_vs_mc_v3_semantic_audit.md` §3.3 在 SPEC-070 v2 实施前估计 wing=100 长腿"实际 delta ≈ 0.04"。SPEC-070 v2 实测后这个估计方向写反了：真实 δ0.08 strikes 比 wing=100 baseline **更远 OTM**（call 7772 → 8017），所以 baseline 的隐含 delta 大约是 `0.10 ~ 0.12`，不是 0.04。已加 2026-04-25 postscript 修正。

这影响的只是当时对错配量级的描述，不影响：
- 错配真实存在的判断（成立）
- 修复方向（成立，已落地 SPEC-070 v2）

---

## 4. 当前项目状态（给 MC 的最短理解）

### 已经是生产事实的

| 项目 | 状态 |
|---|---|
| `SPEC-061` `/ES` minimal cell | DONE |
| `SPEC-064` HIGH_VOL aftermath IC_HV bypass | DONE |
| `SPEC-066` IC_HV max concurrent 2 + off-peak 0.10 | DONE |
| `SPEC-070 v2` IC long legs delta-based | DONE (本轮) |
| `SPEC-068` per-strategy HV spell budget | DONE (本轮) |
| `SPEC-069` open_at_end reporting | DONE (本轮) |
| `SPEC-071` aftermath broken-wing V3-A | DONE (本轮) |
| `SPEC-073` BCD dead-code 清理 | DONE (本轮，HC 自查) |

### 仍然 open

| 编号 | 状态 |
|---|---|
| `SPEC-072` | BLOCKED_BY_HANDOFF — 等 MC 补 §2.1 四项 |
| Aftermath short 0.16 → 0.12 governance | OPEN — 等 MC 补 §2.2 溯源 |
| `Q013` `/ES` runtime safeguard | unchanged |
| `Q019` VIX close/open 时间基准 | unchanged |
| `Q020` SPEC-066 第二笔语义归因 | unchanged |

---

## 5. MC 建议读取顺序

1. 本文件
2. `doc/quant_validation_2026-04-25.md`（完整 AC 核对 + cascade 报告）
3. `doc/baseline_post_spec0{70,68,69,71}/README.md`（每个 SPEC 的对照 README）
4. `task/SPEC-070.md` / `SPEC-068.md` / `SPEC-069.md` / `SPEC-071.md` 的 Review 段
5. `task/SPEC-072.md`（BLOCKED 现状 + HC 列出的待补条目）
