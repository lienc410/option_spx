# HC Return — 2026-04-12

**角色**：HC Quant Researcher / Planner
**周期**：2026-04-08 ~ 2026-04-12
**协作方**：PM（lienchen）/ Developer

---

## 本轮工作总结

### 1. `/ES` minimal production cell 已完成并 review 通过

| SPEC | 标题 | Review 结论 | Status |
|------|------|-------------|--------|
| SPEC-061 | `/ES` Short Put Minimal Production Cell | PASS；AC1–AC9 通过，AC6 已从 handoff 误判纠正为 PASS | DONE |

关键结论：
- `SPEC-061` 已把 `/ES` short put 最小 Layer 2 production cell 落地到：
  - 单槽
  - `1` 张
  - `45 DTE`
  - `20 delta`
  - `trend filter`
  - `BP <= NLV 20%`
- 当前最大剩余缺口不是入场逻辑，而是运行时风控：
  - `-300%` stop 仍是文档化规则
  - 生产中没有自动执行
  - 趋势转负后的持仓行为也未定义

### 2. PM 已明确新的生产安全底线

HC 侧已记录并同步：
- **不能接受纯人工盯仓止损**
- 最低可接受要求：
  - 系统监控 stop 条件
  - bot 触发提醒

这已沉淀为新的 open question：
- `Q013` — `/ES` short put 运行时止损与持仓管理定义

### 3. `/ES` 三层体系的当前覆盖状态已被系统梳理

当前判断：
- `SPEC-061` 只覆盖 SoMuchRanch 三层体系中的最小 **Layer 2**
- 以下内容仍未进入生产系统：
  - Layer 1：Long SPY / VTI 核心持仓
  - Layer 3：BSH 持仓管理
  - Strategy #3：Long `/ES` calls

这不是实现遗漏，而是**有意缩 scope**

### 4. 正式角色文档入口已重组

当前正式入口已从旧的 `AGENTS.md + CLAUDE.md` 混合结构，调整为：

- `AGENTS.md`：共享规则
- `DEVELOPER.md`：Developer 专属
- `PLANNER.md`：Planner 专属
- `QUANT_RESEARCHER.md`：Quant Researcher 专属
- `CLAUDE.md`：兼容说明，指向 `QUANT_RESEARCHER.md`
- `PROMPTS.md`：PM 常用 prompt 索引

对 MC 的意义：
- 若明天从头接手，优先读新的角色入口，不要再把旧 `CLAUDE.md` 当成唯一正式协议

---

## 文档与索引层更新

### 已更新索引层

| 文件 | 更新内容 |
|------|----------|
| `PROJECT_STATUS.md` | 前排 blocker 改为 `/ES` runtime safeguard follow-up；`SPEC-061` 已记为 DONE |
| `RESEARCH_LOG.md` | 新增 `/ES` 三层体系覆盖盘点条目（R-20260412-03） |
| `sync/open_questions.md` | 新增 `Q013`；更新 `Q012` 为 post-`SPEC-061` 状态 |

### 已更新正式角色文档

| 文件 | 更新内容 |
|------|----------|
| `AGENTS.md` | 仅保留共享规则 |
| `DEVELOPER.md` | 新建，承载 Developer 规则 |
| `QUANT_RESEARCHER.md` | 新建，承载 Quant Researcher 规则 |
| `PLANNER.md` | 术语与详细层职责说明已更新 |
| `CLAUDE.md` | 改为兼容说明 |
| `PROMPTS.md` | 新建，集中收录 PM 可直接复制的 prompt |
| `doc/agent_docs_migration_2026-04-12.md` | 记录这次角色文档迁移 |

---

## 系统当前状态快照（截至 2026-04-12）

### 当前项目阶段

- `stable`
- `research-driven`

### 当前生产姿态

- 保持现有主策略矩阵不轻易扩张
- `/ES` 已完成最小 MVP cell，但未满足最终生产安全要求

### 当前前排事项

1. `Q013`：为 `/ES` 起一个狭窄 follow-up Spec
   - 最小范围：stop-condition monitoring + bot alert
2. `Q012`：shared-BP 口径后续是否需要进一步细化
3. `SPEC-044`：仍为当前唯一 active approved implementation item

### 当前 active approved spec

- `SPEC-044` — Delta deviation display in open modal

### 当前 open questions（高相关）

| 编号 | 状态 | 含义 |
|------|------|------|
| Q012 | open | `/ES` 生产路径已成立，剩余问题是 shared-BP 与 MVP 边界 |
| Q013 | open | `/ES` runtime stop 监控与 bot alert 尚未定义 |
| Q002 | open | Shock active mode 仍待 Phase B |
| Q003 | open | L3 Hedge v2 仍待独立 SPEC |

---

## MC 接手时建议先读什么

说明：
- MC 环境仍按 `Claude Quant + AMP Developer` 模式工作
- 不需要先完整读取 HC 侧拆分后的全部角色文档
- MC 的主入口仍应是 `MC_CLAUDE_INSTRUCTIONS.md + 最新 HC return + 索引层`

### 如果明天重点是 `/ES`

按顺序建议读取：
- `sync/hc_to_mc/MC_CLAUDE_INSTRUCTIONS.md`
- `sync/hc_to_mc/HC_return_2026-04-12.md`
- `PROJECT_STATUS.md`
- `RESEARCH_LOG.md`
- `sync/open_questions.md`
- `task/SPEC-061.md`
- `research/strategies/ES_puts/spec.md`

### 如果明天重点是项目全局状态

按顺序建议读取：
- `sync/hc_to_mc/MC_CLAUDE_INSTRUCTIONS.md`
- `sync/hc_to_mc/HC_return_2026-04-12.md`
- `PROJECT_STATUS.md`
- `RESEARCH_LOG.md`
- `sync/open_questions.md`

如需进一步查看 HC 侧新文档结构，再按需补读：
- `AGENTS.md`
- `QUANT_RESEARCHER.md`
- `PLANNER.md`
- `PROMPTS.md`

---

## 需 MC / PM 拍板的事项

### 1. 是否立刻为 `Q013` 起新的 DRAFT Spec

HC 建议：
- **建议起**
- 但 scope 必须很窄，只补：
  - stop 监控
  - bot alert
  - post-entry 管理定义

不建议一起带入：
- Layer 1
- Layer 3
- leverage table
- Strategy #3

### 2. `/ES` MVP 是否仍被视为“可研究推进”，还是“必须先补 runtime safeguard 才允许继续”

HC 当前理解：
- 由于 PM 已明确“不能接受纯人工盯仓止损”，所以 `/ES` 路径若要继续向生产靠近，`Q013` 应优先于更大体系扩张

---

## 不要推断的项目

- 不要把 `SPEC-061 DONE` 误解为 SoMuchRanch 三层体系整体已落地
- 不要把 Layer 1 / Layer 3 的缺失写成“实现遗漏”；当前是有意缩 scope
- 不要把人工止损当作可接受默认前提；PM 已明确否定
- 不要把 `Q012` 与 `Q013` 混成一个问题：
  - `Q012` 偏 shared-BP / 路径边界
  - `Q013` 偏运行时风控

---

## MC 明日建议起手动作

### 若明天继续 `/ES`

1. 先确认是否接受 HC 对 `Q013` 的收缩建议
2. 若接受，直接起一个新的 DRAFT Spec
3. 不要把 Layer 1 / Layer 3 / Strategy #3 一起带入

### 若明天做全局整理

1. 使用 `PROMPTS.md` 中的 Planner prompt 重建状态
2. 先看 `PROJECT_STATUS.md` 与 `sync/open_questions.md`
3. 仅在需要详细背景时再回到 `strategy_status` / `research_notes`

---

*Generated on HC — 2026-04-12*
