# HC Return — 2026-04-19

**角色**：HC Quant Researcher / Planner  
**周期**：2026-04-12 ~ 2026-04-19  
**协作方**：PM（lienchen）/ Developer  
**用途**：本包为给 `MC` 的整合版 return。由于 `MC` 尚未同步 `HC_return_2026-04-12.md` 及其后的内容，本文件已吸收 2026-04-12 之后的关键变化，`MC` 可直接从这里接手，不必再拼接旧包。

---

## 本轮最重要变化

### 1. `/ES` minimal production cell 之后，真正的前排问题仍然是 runtime safeguard

| SPEC | 标题 | 当前结论 | Status |
|------|------|----------|--------|
| SPEC-061 | `/ES` Short Put Minimal Production Cell | 已完成；MVP entry path 成立，但 runtime safeguard 仍缺 | DONE |

当前 HC 侧结论没有变：
- `SPEC-061` 已把 `/ES` short put 最小 Layer 2 production cell 落地到：
  - 单槽
  - `1` 张
  - `45 DTE`
  - `20 delta`
  - `trend filter`
  - `BP <= NLV 20%`
- 当前最大剩余缺口不是 entry logic，而是运行时风控：
  - `-300%` stop 尚未系统化自动执行
  - 生产侧最低要求不是人工盯仓，而是 **系统监控 + bot alert**

当前对应 open questions：
- `Q012` — `/ES` 路径与 shared-BP 管理
- `Q013` — `/ES` runtime stop / post-entry 管理 follow-up

### 2. `SPEC-044` 已 DONE；当前没有 active approved spec 等待 Developer

与 4/12 相比，一个重要状态变化是：
- `SPEC-044` 已 `DONE`
- 当前 `Active APPROVED Specs = None`

对 MC 的含义：
- 目前没有“直接交给 AMP Developer 开工”的 approved spec
- 当前前排工作重新回到 research / planning

### 3. `Q015` 已走完完整闭环，并通过 Fast Path 落地

对象：
- `NORMAL + BULLISH` 下 BPS entry gate：`IVP >= 50 -> REDUCE_WAIT`

研究结论分两步：

第一步：
- 该 gate **不是** DIAGONAL Gate 1 那种“纯有害 gate”
- 删除整个 gate 会伤 Sharpe、恶化 MaxDD
- 所以不能直接删

第二步：
- 对更窄的放宽方案做 OOS 验证后，`IVP < 55` 通过了关键检验：
  - Full / IS / OOS 三个切片里 system Sharpe 都不退化
  - PnL 增量为正
- Quant 已通过 Fast Path 把：
  - `BPS_NNB_IVP_UPPER`
  - 从 `50`
  - 提到 `55`

对 MC 的意义：
- `Q015` 不再是 open question
- 当前生产逻辑里，BPS `NORMAL + BULLISH` upper gate 已是 `55`
- 若未来继续研究更广的 IVP / IC redesign，要作为新问题处理，不能再把 `Q015` 当成“待删 gate”

### 4. `Q017` 已研究完成并落地为 `SPEC-064`

`Q017` 研究主题：
- 在 `HIGH_VOL` 分支、VIX 顶峰后早期回落窗口里，系统是否结构性错过机会

研究结论：
- 真实策略 PnL 层面，漏机会现象成立
- alpha 主要集中在 `IC_HV`
- ex-ante 可识别条件就是 aftermath 本身
- 不需要额外 `peak_drop_pct` / `vix_3d_roc`
- 真正的灾难保护来自现有 `EXTREME_VOL (VIX >= 40)`

结果：
- `SPEC-064` 已起草、实施、review 通过并 `DONE`
- 当前生产已经包含：
  - `HIGH_VOL aftermath IC_HV bypass`
  - 仅限 `IC_HV`
  - 不涉及 `BPS_HV / BCS_HV`
  - 保留 `EXTREME_VOL`

### 5. `SPEC-065` 已完成：SPEC-064 research view pill 已上线

对象：
- `SPEC-065` — Research View Pill for `SPEC-064`

当前结果：
- 已实施
- 已 review 通过
- 已 `DONE`

对 MC 的意义：
- `SPEC-064` 相关 historical aftermath trades 现在能在 backtest 页面持续查看
- `SPEC-062 / SPEC-063 / SPEC-065` 这类可视化工作已经形成可复查入口，不再只是一次性 HTML 报告

### 6. `Q018` 新开：double-spike 场景下，单槽位是否错过第二峰

触发源：
- `SPEC-064` 上线后第一次真实 `double VIX spike`（2026-03）

现象：
- 第一峰触发了 `2026-03-09` 的 `IC_HV aftermath` 开仓
- 第二峰回落期间的：
  - `2026-03-31`
  - `2026-04-01`
  - `2026-04-02`
  离线重跑会再次路由到 `IC_HV aftermath`
- 但实际被 engine `_already_open` 的单槽位约束挡住

Phase 1 prototype 已完成，出现两条都值得继续的方向：

**变体 A：多槽位**
- ex-post replay 约 `+$47,735`
- `36` 笔 blocked clusters
- 胜率约 `86.1%`
- 尾部风险集中在 `2008-09 -$7,968`

**变体 B：收紧 aftermath 阈值**
- `AFTERMATH_OFF_PEAK_PCT 0.05 -> 0.10`
- 工程成本极低
- MaxDD 约改善 `36%`
- `IC_HV` Sharpe 提升

当前结论：
- `Q018` 已不再是“单一样本触发的想法”
- 但仍是 **research only**
- 还不能进 Spec

原因：
- 变体 A 还没补：
  - BP ceiling
  - shock engine
  - overlay
  - 更真实的 cluster 内路径
- 变体 B 也还没做足 sensitivity / significance 硬化

当前最推荐的下一步：
- 若 PM 要继续，优先做 `Q018 Phase 2-A`
- 即：先把多槽位方案做真实化，而不是直接拍板

### 7. `Q019` 新开：backtest close-based VIX vs live open-based VIX 的时间基准差异

问题来源：
- 当前很多研究 / 回测默认使用按交易日收盘口径构建的 VIX 时间序列
- 但 live recommendation 的真实决策发生在开盘附近
- VIX 往往在开盘前或刚开盘时达到当日高点，随后回落

重点不是泛泛地说“开盘 VIX 常高于收盘”，而是：
- 如果把历史 selector 输入改成 open-based / earliest-live-time VIX
- route / gate / aftermath 结果会变多少次
- 这些变化会不会集中影响：
  - `SPEC-064`
  - 相邻 HIGH_VOL cells
  - `VIX_RISING`
  - `HIGH_VOL / NORMAL`

当前结论：
- `Q019` 已登记为 **research only**
- 还没有研究结果，不应外推成现有结论失效

---

## 文档与索引层更新

### 索引层当前状态

| 文件 | 当前状态 |
|------|----------|
| `PROJECT_STATUS.md` | 已更新到 `2026-04-19`；前排事项已切到 `Q013 / Q018 / Q019` |
| `RESEARCH_LOG.md` | 已包含 `Q015` OOS + Fast Path、`Q017` Phase 1/2、`Q018` Phase 1、`Q019` 新立项 |
| `sync/open_questions.md` | `Q015` 已归档，`Q017` 已 implemented via `SPEC-064`，`Q018` / `Q019` 为当前研究项 |

### 详细层当前关键入口

| 文件 | 用途 |
|------|------|
| `doc/research_notes.md` | 包含 `Q015/Q017/Q018` 详细研究过程 |
| `doc/strategy_status_2026-04-16.md` | 仍是当前最新策略状态快照 |
| `task/SPEC-064.md` | `HIGH_VOL aftermath IC_HV bypass` 正式规格与 review 记录 |

---

## 系统当前状态快照（截至 2026-04-19）

### 当前项目阶段

- `stable`
- `research-driven`

### 当前生产姿态

- 保持现有主策略矩阵
- 接受研究证据充分、低 regret 的窄变更
- `/ES` MVP 已存在，但未达到最终生产安全要求
- `HIGH_VOL aftermath IC_HV bypass` 已在生产中

### 当前 active approved spec

- None at the moment

### 当前前排事项

1. `Q013`
   - `/ES` runtime safeguard follow-up
   - 最小范围仍是：stop-condition monitoring + bot alert
2. `Q018`
   - 是否继续研究 double-spike 单槽位问题
   - 若继续，优先 Phase 2-A
3. `Q019`
   - VIX 时间基准差异是否会影响 `SPEC-064` 及相邻 HIGH_VOL 格子的解释

### 当前高相关 open questions

| 编号 | 状态 | 含义 |
|------|------|------|
| Q012 | open | `/ES` 路径已成立；剩余问题是 shared-BP 与 MVP 边界 |
| Q013 | open | `/ES` runtime stop 监控与 bot alert 尚未定义 |
| Q018 | research | aftermath 单槽位 vs 多槽位 / 收紧阈值 的 tradeoff |
| Q019 | research | close-based vs open-based VIX 时基差异 |
| Q002 | open | Shock active mode 仍待 Phase B |
| Q003 | open | L3 Hedge v2 仍待独立 SPEC |
| Q011 | open | regime decay DIAGONAL live sample 仍小 |

### 已解决但 MC 需要知道的事项

| 编号 | 结论 |
|------|------|
| Q014 | DIAGONAL Gate 1 已删除（Fast Path） |
| Q015 | BPS upper gate 已从 `50` 调到 `55`（Fast Path） |
| Q017 | 已通过 `SPEC-064` 落地 |
| SPEC-044 | DONE |
| SPEC-061 | DONE，但后续 `Q013` 仍 open |
| SPEC-064 | DONE |
| SPEC-065 | DONE |

---

## 对 MC 最重要的理解更新

### A. `/ES` 仍然卡在 runtime safeguard，不是卡在 entry logic

如果 MC 明天继续 `/ES`，重点仍然是：
- `Q013`
- 不要回头重做 `SPEC-061` entry logic

### B. `Q015` 与 `Q017` 都已经是“已生效事实”，不是待拍板问题

- `Q015`：BPS gate 已从 `50 -> 55`
- `Q017`：已通过 `SPEC-064` 进入生产

所以不要再把它们当成“明天要不要推进”的 open question。

### C. 当前 HIGH_VOL 研究前沿已经换成 `Q018 + Q019`

- `Q018`：single-slot 是否错过 second spike
- `Q019`：VIX close vs open 时基差异是否扭曲 HIGH_VOL 研究与 live 行为解释

这两条都还是 research，不应误读成可直接交给 AMP Developer 的任务。

---

## MC 接手建议读取顺序

MC 环境仍按：
- `Claude Quant`
- `AMP Developer`

模式工作，不需要先完整读取 HC 的全部角色拆分入口。

### 若明天做项目全局接手

按顺序建议读取：
- `sync/hc_to_mc/MC_CLAUDE_INSTRUCTIONS.md`
- `sync/hc_to_mc/HC_return_2026-04-19.md`
- `PROJECT_STATUS.md`
- `RESEARCH_LOG.md`
- `sync/open_questions.md`

### 若明天重点是 `/ES`

再补读：
- `task/SPEC-061.md`
- `task/SPEC-064.md`
- `doc/strategy_status_2026-04-16.md`

### 若明天重点是 HIGH_VOL / aftermath 研究

再补读：
- `task/SPEC-064.md`
- `doc/research_notes.md`
- backtest 研究视图里 `SPEC-064` 对应 pill

---

## 需 MC / PM 拍板的事项

### 1. 是否立刻为 `Q013` 起新的 DRAFT Spec

HC 当前建议仍是：
- **建议起**
- scope 保持很窄，只补：
  - stop 监控
  - bot alert
  - post-entry 管理定义

不建议带入：
- Layer 1
- Layer 3
- leverage
- Strategy #3

### 2. `Q018` 是否进入 Phase 2，以及优先哪条子路径

HC 当前建议：
- 如果继续，优先 `Phase 2-A`
- 即：先把多槽位方案的现实约束补上
  - BP ceiling
  - shock engine
  - overlay

原因：
- 它最直接回答 `+$47,735` 到底可不可信

### 3. `Q019` 是否应尽快开跑

HC 当前建议：
- 可以开，但优先级低于 `Q013`
- 且研究范围应先收窄到：
  - `SPEC-064`
  - 相邻 HIGH_VOL cells
- 不要一上来做全系统 VIX 口径重跑

---

## 不要推断的项目

- 不要把 `SPEC-061 DONE` 误解为 SoMuchRanch 三层体系整体已落地
- 不要把 `Q018` 误读成“应该直接开多槽位”
- 不要把 `Q019` 误读成“现有 HIGH_VOL 研究都失效了”
- 不要把 `Q015` 与 `Q017` 当成还待拍板的问题；它们都已落地
- 不要把人工止损当作可接受默认前提；PM 已明确否定

---

## MC 明日建议起手动作

### 若明天继续 `/ES`

1. 先判断是否接受 HC 对 `Q013` 的窄范围建议
2. 若接受，起一个新的 DRAFT Spec
3. 不要把 Layer 1 / Layer 3 / Strategy #3 一起带入

### 若明天继续 HIGH_VOL 研究

1. 不要重做 `Q017`
2. 在 `Q018` 和 `Q019` 中选一条：
   - 若要判断“第二峰值是否值得开第二笔”，做 `Q018 Phase 2-A`
   - 若要判断“live 与 backtest 口径是否不一致”，做 `Q019` 窄研究

### 若明天做全局整理

1. 先看 `PROJECT_STATUS.md`
2. 再看 `sync/open_questions.md`
3. 需要研究背景时再回到 `RESEARCH_LOG.md` 和 `research_notes`

---

*Generated on HC — 2026-04-19*
