# HC Return — 2026-04-07

**角色**：HC Claude（Quant Researcher）
**周期**：2026-04-06 ~ 2026-04-07
**协作方**：PM（lienchen）/ Codex（Developer）

---

## 本轮工作总结

### SPEC Review 完成情况

| SPEC | 标题 | Review 结论 | Status |
|------|------|-------------|--------|
| SPEC-039 | Option Chain Liquidity Scanner for Open Draft | PASS，AC1–AC5 全部通过 | DONE |
| SPEC-040 | Strike-Centered Option Chain Scan | PASS，AC1–AC5 全部通过，strikeCount 动态放大正确 | DONE |
| SPEC-041 | EOD Signal Snapshot Push (4:03pm ET) | PASS，F1–F6 全部通过 | DONE |
| SPEC-042 | Index Option Liquidity Filter Relaxation | PASS，F1–F4 全部通过 | DONE |
| SPEC-045 | Delta-Seeking Strike Scan via Interpolation | PASS，AC1–AC6 全部通过 | DONE |
| SPEC-046 | Schwab Intraday Alert Source for VIX Spike and SPX Stop | PASS，F1–F8 全部通过 | DONE |
| SPEC-047 | Adaptive Multi-Pass Delta-Seeking Scan | PASS，AC1–AC9 全部通过 | DONE |

---

### SPEC 起草与设计

| SPEC | 标题 | 来源 | 最终状态 |
|------|------|------|---------|
| SPEC-041 | EOD Signal Snapshot Push | PM 提案 → HC 起草 | DONE |
| SPEC-042 | Index Option Liquidity Filter Relaxation | PM 草稿（doc/ → task/）→ HC Review + 修正 | DONE |
| SPEC-043 | Wider Delta-Seeking Strike Scan | PM 草稿 → HC Review | CANCELLED（被 SPEC-045 取代）|
| SPEC-044 | Delta Deviation Display in Open Modal | PM 草稿（doc/ → task/）→ HC Review + 修正 | APPROVED（待 Codex）|
| SPEC-045 | Delta-Seeking Strike Scan via Interpolation | PM 草稿（迭代扩窗方向）→ HC 重新设计（插值方案）| DONE |
| SPEC-046 | Schwab Intraday Alert Source | PM 草稿（doc/ → task/）→ HC Review + 修正 | DONE |
| SPEC-047 | Adaptive Multi-Pass Delta-Seeking Scan | PM 草稿（doc/ → task/）→ HC Review + 修正 | DONE |

**SPEC-042 修正项（HC 补全后 PM APPROVE）：**
- P1：score 公式含 `oi_penalty` 但未展示完整表达式 → 在 F3 补充完整 score = ... 公式，明确"oi_penalty 替换而非叠加"原有 OI 项

**SPEC-044 修正项（HC 补全后 PM APPROVE）：**
- P1：`live_delta` 为 PUT 时原始值为负（如 −0.016）→ 明确所有三个字段（`target_delta`、`live_delta`、`delta_gap`）均使用绝对值，示例从 −0.016 修正为 +0.016

**SPEC-046 修正项（HC 补全后 PM APPROVE）：**
- P1：Design 说"add quote fetch helpers"但未给出 API 路径 → 补充 `GET /marketdata/v1/quotes?symbols={symbol}`
- P2：AC7 stale 判断提到"超过阈值"但未给出数字 → 补充"10 minutes"并定义常量 `_STALE_QUOTE_MINUTES = 10`
- P3：`realtime=False`（非实时账户）有 15–20 分钟结构性延迟，`quote_time` 不可靠 → 新增 F8/T8，非实时账户无条件标注 `[delayed — non-realtime quote]`，不做时差计算

**SPEC-047 修正项（HC 补全后 PM APPROVE）：**
- P1：伪代码引用 `_slice_chain_around_center()` 但从未定义 → §5 补充注明"即 SPEC-045 内联切片逻辑，不是新函数"
- P2：`delta_gap` / `interpolated_center` 字段（来自 SPEC-045）在 SPEC-047 伪代码中未提及，有被遗漏风险 → 新增 §6 明确"SPEC-047 实现必须保留这两个字段"

**SPEC-045 设计演化（重要）：**
初版草稿为迭代扩窗（类 binary search）。PM 提出"有没有比 binary search 更好的方法？"，HC 识别 delta 对 strike 严格单调，可单次扫描穿越点并线性插值，零额外 API 请求。完整方案重写后 PM APPROVE。设计细节见 research_notes.md §42。

---

### 文档更新

| 文件 | 内容 |
|------|------|
| `sync/hc_to_mc/HC_return_2026-04-07.md` | 本文件 |
| `doc/system_status_2026-04-07.md` | 更新文件树（schwab/scanner.py / 新测试文件）；补充 §4.25–4.28 新模块详解；SPEC 状态表补全至 SPEC-047 |
| `doc/research_notes.md` | 新增 §42：Delta 单调性插值设计决策 |

---

## 系统当前状态快照（截至 2026-04-07）

### Schwab 期权链扫描器全链路（SPEC-039/040/042/045/047）

```
web/server.py  GET /api/position/open-draft
  ↓ get_option_chain(symbol, type, dte, center_strike, strike_window)
  ↓ [schwab/client.py — 带缓存，cache key 含 strike_window]
  ↓
build_strike_scan(symbol, type, target_delta, dte, center_strike)
  [schwab/scanner.py]
  ↓
  如 center_strike=None：
    单次宽泛链 → scan_strikes() → rows

  如 center_strike 指定：
    for window in (80, 140, 220):                 # SPEC-047 自适应三轮
      chain = get_option_chain(... strike_window=window)
      sought = _seek_target_delta_strike(chain, target_delta)
                                                  # SPEC-045 插值穿越
      best_center = round(sought / 5.0) * 5.0
      if not _is_boundary_hit(chain, sought): break  # 内部穿越即停止

    slice ±10 档 around best_center
    → scan_strikes(candidate_chain, ...) → rows   # SPEC-042 宽松 OI
    → rows 附加 delta_gap + interpolated_center   # SPEC-045 字段

  return {"rows": rows, "scan_fallback": bool}
```

### Telegram Bot 实时告警链路（SPEC-041/046）

```
APScheduler 9:35am ET  scheduled_push()
  → select_strategy(use_intraday=True)
  → 推送并写 _morning_snapshot

APScheduler 4:03pm ET  scheduled_eod_push()     # SPEC-041 新增
  → select_strategy(use_intraday=False)          # 强制 EOD 收盘数据
  → 与 _morning_snapshot 对比
  → 推送 EOD 信号快照

APScheduler 每 5min    intraday_monitor()        # SPEC-046 更新
  Primary: get_vix_quote() / get_spx_quote()     # Schwab 实时报价
    → get_vix_spike_from_quote() / get_spx_stop_from_quote()
    → 告警附时间标注（bar时间 | sent时间 | delayed Xm）
  Fallback: Yahoo intraday history（原有路径）
```

---

## 待处理事项

| 优先级 | 事项 | 前置条件 |
|--------|------|---------|
| 高 | SPEC-044 实现：Delta Deviation Display in Open Modal | Codex 执行 APPROVED SPEC |
| 高 | Schwab Developer Portal 批准后完成 SPEC-035 AC1/3/4/5 联调 | Schwab 批准账户 |
| 中 | Iron Condor put-side 4-leg schema 扩展 | 独立 SPEC（从 SPEC-039 deferred）|
| 中 | 真实交易录入开始后，performance 页面数据积累 | 开始真实交易 |
| 低 | Shock active mode 上线：需 Phase B A/B 验证 | 验证通过 |

---

## 技术债 / 已知限制（无变化）

- Precision B 系统性偏差（IV Bias + 无 bid-ask）→ 综合 haircut ~51%，调整后 Sharpe ~0.99
- `DailyPortfolioRow.bp_headroom` 存储为 USD 而非 NAV 比例（已记录，不影响下游）
- True Range 近似（close.diff().abs()）而非真实 TR（可接受）
- Shock active mode 上线待 Phase B A/B 验证（目前为 shadow mode）
- L3 overlay hedge（long put spread）v2 实现待独立 SPEC

---

*Generated by HC Claude (Quant Researcher) — 2026-04-07*
