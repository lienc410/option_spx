# HC Return — 2026-05-02

**类型**：MC→HC reproduction sprint 收口 handoff  
**对应 MC 包**：`sync/mc_to_hc/MC_Handoff_2026-05-01_v5.md`（MC 工作期 2026-04-27 → 2026-05-01）  
**作者**：HC Planner  
**用途**：让 MC 在不重读 HC 全部聊天记录的前提下，重构自 `MC_Handoff_2026-05-01_v5` 之后，HC 端完成的 reproduction sprint 全貌，包括：完成了什么、deferred 了什么、两端当前的对齐状态与残余分歧。

---

## 0. 本包一句话摘要

HC 完成了 MC handoff v5 中**可复现的 5 个 SPEC**（SPEC-074 / SPEC-077 / SPEC-078 / SPEC-079 / SPEC-080），跑了 tieout #2 和 tieout #3 验证无回归，生产配置现与 MC PARAM_MASTER v4 对齐。`SPEC-075 / SPEC-076（Overlay-F）` **在本轮 sprint 中仍未复现**，但这已不再是 canonical 分歧问题：HC PM 现已接受 MC 侧更接近 `escalate / productization stack` 的方向。与此同时，`Q021` 已在 HC 端批准关闭，`Q038` 不仅获准切到 `shadow`，而且已实际部署到 old Air runtime，`Q039` 保持在研究位置。

---

## 1. HC 完成的工作（batch 1 + batch 2）

### 1.1 Batch 1 — SPEC-074 / SPEC-077 / SPEC-078

#### SPEC-074：`backtest_select` parity vs MC

- 状态：**DONE**
- HC 的 `run_backtest` 已 delegate 到 live `select_strategy`，gate 覆盖 MC SPEC-074 所有 7 gates × 6 components
- 永久分歧一条：HC 通过 SPEC-056c 移除了 DIAGONAL Gate 1（both-high 过滤），MC 保留；HC PM 裁定 = Option A（HC 保留移除作为 canonical）。此分歧已在 SPEC-074 变更记录中显式标注
- Parity test `tests/test_backtest_select_parity.py`：22/22 PASS

#### SPEC-077：`profit_target` default 0.50 → 0.60 + `stop_mult` wiring 锁定

- 状态：**DONE（AC3 documented FAIL）**
- F1: `StrategyParams.profit_target = 0.60` 已落地；两处 `web/server.py` fallback override 同步
- F2: `tests/test_engine_stop_wiring.py`（5 case）锁定 credit-side `params.stop_mult` wiring 并显式 punt debit-side 硬编码到 SPEC-080
- AC3 full-sample HC rerun（2007-01-01，19.32y）：Δ ann_roe = `+0.0856pp`，远低于 MC 的 Q037 Phase 2A `+0.91~+1.03pp` 区间和 AC3 `≥+0.5pp` 阈值；Δ sharpe = `0.00`（不退化 ✓）
- PM 选 option (2)：操作性 MC 对齐 + 显式 acknowledge AC3 全样本未达，闭 DONE
- HC↔MC ~10× 量级差距已归档三候选原因：(a) compounding-baseline 口径、(b) 原 debit-side `-0.50` 硬编（现由 SPEC-080 闭合）、(c) SPEC-054/056c 永久分歧。待 SPEC-080 落地后做 post-hoc attribution（候选索引 Q040）

#### SPEC-078：Backtest dashboard metrics single-source-of-truth

- 状态：**DONE（AC1-AC7 全 PASS）**
- Server `compute_metrics` 新增 `annualized_roe` / `annualized_roe_basis` / `period_years`
- Frontend 直接读 server 字段；`impliedAnnualizedRoe` 降级为 `@deprecated` fallback + `console.warn`
- Codex 脚本化 smoke PASS：`|Δ|≤3.1e-07`，远 < 1e-6 阈值；19.32y `ann_roe=8.0358%` 与 SPEC-077 AC3 数据 cross-check 自洽
- PM 浏览器 smoke PASS：normal path + fallback path（DevTools Local Overrides）均 PASS
- P12 Fast Path 仍 deferred，与 MC 一致

---

### 1.2 Batch 2 — SPEC-079 / SPEC-080（Path C）

#### SPEC-079：BCD comfortable-top entry filter（`bcd_comfort_filter_mode`）

- 状态：**DONE（F1-F6 + AC1-AC8 全 PASS）**
- `TrendSnapshot` 新增 `spx_30d_high` / `dist_30d_high_pct` 字段
- `strategy/bcd_filter.py` 新建：`should_block_bcd(mode, vix, dist_30d_high_pct, ma_gap_pct, date)`
  - 三条件阈值：VIX ≤ 15.0 / dist_30d_high ≤ -1% / ma_gap > 1.5pp，来自 Q038 walk-forward，locked
- `StrategyParams.bcd_comfort_filter_mode = "disabled"`（toggle 默认值）
- `selector.py:933` wiring：Gate 2 之后、`_build_recommendation` 之前插入；shadow log 写 `data/bcd_filter_shadow.jsonl`
- `tests/test_bcd_filter.py`：6/6 PASS；`scripts/export_backtest_trade_detail.py` 同步更新

#### SPEC-080：BCD debit stop tightening（`bcd_stop_tightening_mode`）

- 状态：**DONE（F1-F5 + AC1-AC9 全 PASS）**
- `strategy/bcd_stop.py` 新建：`BCD_STOP_DEFAULT=-0.50` / `BCD_STOP_TIGHTER=-0.35`；`bcd_debit_stop(mode)`
- `StrategyParams.bcd_stop_tightening_mode = "disabled"`
- `engine.py:921-933` debit stop 参数化：BCD + `active` mode → -0.35；其余（BCD disabled/shadow + 非 BCD）→ -0.50
- shadow log 写 `data/bcd_stop_shadow_engine.jsonl`
- SPEC-077 中 documented 的 engine.py debit-side `-0.50` 硬编码 defect **正式闭合**
- `tests/test_bcd_stop.py`：7/7 PASS；`tests/test_engine_stop_wiring.py` 仍 PASS（无回归）

---

## 2. Tieout 验证结果

### Tieout #2（batch 1 regression）

- 窗口：`2023-04-29 → 2026-05-02`，对比基准：tieout #1 CSV（2026-04-29 生成）
- Q-A 自洽：98.28% entry match，date-boundary false alarm（新增 `2026-04-30` trade 因当时 CSV 未含该日），57 原始 entry 100% 保留 → **batch 1 零 trade flow 变化**
- Q-B HC↔MC gap：HC 58/$75,570 vs MC 52/$45,922（Δ +6/+$29,648），与 tieout #1 基本持平 — batch 1 未动 selector/IVP gate，gap 不收敛符合预期
- Q-C 新基线 PT=0.60：57 trades（2 open_at_end），$79,934

### Tieout #3（batch 2 regression + SPEC-079/080 preview）

- 场景 A（both disabled，PT=0.60）= 57/$79,933.69，与 tieout #2 Q-C byte-identical → **REGRESSION_PASS = True**
- B/C/D（任意 toggle active，PT=0.60）= 全部等于 A：3y 低压力窗口内 SPEC-079/080 **零触发**（comfort filter 三条件从未在 BCD 入场日同时满足；stop tightening pnl_ratio [-0.50,-0.35) 区间亦未命中）
- D_pt050（both active，PT=0.50）= 57/$76,450（vs tieout #2 58/$75,570，Δ = -1 trade / +$880）：2026-04-30 BCD 被 SPEC-079 risk_score=3 拦截（VIX=14.x / dist_30d ≤ -1% / ma_gap > 1.5pp）
- Gap vs MC@PT=0.50：trade delta +6 → **+5**；PnL delta +$29,648 → **+$30,528**（被拦的是盈利单）
- 主残余 gap 未变：IC regular HC 13 vs MC 6 仍是最大单项（IVP gate / persistence filter 差异），不在 SPEC-079/080 范围

---

## 3. HC deferred 项目

### SPEC-075 / SPEC-076（Overlay-F productization）

HC **在本轮 reproduction sprint 中未复现** SPEC-075 / SPEC-076。原因在执行窗口内一度是 canonical 分歧；但该分歧现已在 HC 端被 PM 收口。

| 维度 | MC canonical | HC canonical |
|---|---|---|
| Q036 最终 PM 裁定 | **Option 2 = ESCALATE**；productization stack 已推进，SPEC-075/076 DONE | **现已接受 MC 侧 escalate / productization-stack 方向**；但 `SPEC-075/076` 未包含在本轮 HC reproduction sprint 中 |
| Overlay-F 当前状态 | 已实现，production toggle `overlay_f_mode = disabled` | 仍在 research / governance 阶段，无 spec |
| PARAM_MASTER | v4，含 `overlay_f_mode` | 不含该字段 |

当前更准确的说法是：`SPEC-075/076` 在**本轮** HC sprint 中没有被实现，但这已不再是方向分歧导致的 deferred。HC PM 现在接受 MC 侧更接近 `escalate / productization stack` 的方向，因此下一步如果推进，应把 `SPEC-075/076` 视为 **下一批 HC adoption / prerequisite / implementation-planning 工作**，而不是继续卡在 `hold vs escalate` 的争论里。

---

## 4. 当前 HC ↔ MC 对齐状态总表

| SPEC | MC | HC | 分歧/说明 |
|---|---|---|---|
| SPEC-074 | DONE | DONE | 永久分歧：HC 移除 DIAG Gate 1（SPEC-056c），MC 保留 |
| SPEC-075 | DONE | **未实现（本轮未纳入）** | 不再是 canonical 分歧；转入下一批 HC adoption planning |
| SPEC-076 | DONE | **未实现（本轮未纳入）** | 同上 |
| SPEC-077 | DONE | DONE（AC3 shortfall） | HC AC3 +0.086pp vs MC +0.91~1.03pp；~10× 差距待 post-SPEC-080 attribution |
| SPEC-078 | DONE | DONE | 全对齐 |
| SPEC-079 | DONE | DONE | 全对齐；阈值 locked per Q038 |
| SPEC-080 | DONE | DONE | 全对齐；SPEC-077 debit-side defect 正式闭合 |

### 生产 config 对齐状态

| 参数 | MC PARAM_MASTER v4 | HC `StrategyParams` 当前默认 | 状态 |
|---|---|---|---|
| `profit_target` | `0.60` | `0.60` | ✓ 对齐 |
| `bcd_comfort_filter_mode` | `disabled` | `disabled` | ✓ 对齐 |
| `bcd_stop_tightening_mode` | `disabled` | `disabled` | ✓ 对齐 |
| `stop_mult` | `2.0`（显式） | `2.0`（显式） | ✓ 对齐 |
| `overlay_f_mode` | `disabled` | **不存在** | ✗ 本轮未复现；分歧已消除，后续属 adoption scope |
| `AFTERMATH_OFF_PEAK_PCT` | `0.10` | `0.10`（SPEC-066 落地） | ✓ 对齐 |
| `IC_HV_MAX_CONCURRENT` | `2` | `2`（SPEC-066 落地） | ✓ 对齐 |

---

## 5. 残余 HC↔MC gap（tieout #3 后）

| 分项 | trade delta | 根因 | 是否在当前 sprint 范围 |
|---|---|---|---|
| IC regular | HC 13 vs MC 6（Δ+7） | IVP gate 敏感度 / persistence filter 差异 | 否（Q039 候选） |
| BCD | HC 20 vs MC 15（Δ+5） | 部分来自 SPEC-056c Gate 1 永久分歧 | 否（永久分歧） |
| BPS | HC 15 vs MC 21（Δ-6） | 策略组合 / comfort filter 应用面差异 | 否 |
| **总计** | **Δ+5 trades（PT=0.50 mode）** | 以上三项叠加 | 主 gap 未变；SPEC-079 缩减 1 trade |

---

## 6. 待 MC 知悉 / 待对齐的事项

### 6.1 Q036 方向分歧已收口；后续问题转为 adoption 规划

HC PM 现已接受 MC 侧更接近 `escalate / productization stack` 的方向，因此 `Q036` 已不再是双端 canonical 分歧问题。当前剩余工作不是再讨论 `hold vs escalate`，而是决定 HC 何时、按什么 prerequisite / shadow-safe 姿态，把 `SPEC-075/076` 带入下一批 adoption / implementation planning。

### 6.2 SPEC-079/080 shadow 已在 HC old Air runtime 执行

HC PM 现已明确：`Q038` / Path C **可以现在切到 `shadow`**，且 Developer 已在 old Air 实际执行完成：

- 本地提交：`3da1f5f` — `Q038: flip BCD toggles to shadow for runtime rollout`
- old Air 已从 `a7cb9ec` 更新到 `3da1f5f`
- `com.spxstrat.web` / `com.spxstrat.bot` 已重启
- old Air 上默认值现为：
  - `bcd_comfort_filter_mode = "shadow"`
  - `bcd_stop_tightening_mode = "shadow"`
- `GET http://127.0.0.1:5050/api/recommendation -> 200`

需要 MC 知悉的运行细节：

- `SPEC-079` 的 shadow 现在已在 HC live recommendation path 生效，后续若命中 BCD comfortable-top 条件，会写：
  - `data/bcd_filter_shadow.jsonl`
- `SPEC-080` 的 shadow 在 HC old Air live path 上目前主要仍是 posture 对齐：
  - 当前 live bot/web 不会自然产出等价的 live-side debit-stop shadow 事件日志
  - 因此它的 shadow 观测值暂时主要存在于 engine/backtest 语义，而非 live-side event stream

这意味着 HC 端不再把 shadow flip timing 视为待拍板 blocker；后续关注点转为 shadow 观察与 monitoring，而不是是否 flip。

### 6.3 Q021 已在 HC 端批准关闭

HC PM 现已批准 `Q021` 关闭：保留 `SPEC-066` 不变，不开 `SPEC-067`。这意味着 `Q021` 在 HC 侧不再是 pending close 的研究问题，而是已完成的 rule-layer evidence base。

### 6.4 SPEC-077 AC3 量级差距（~10× gap）

HC AC3 full-sample 结果与 MC 的 Q037 数字相差约 10×。HC 最新 Quant 判断是：

- 这不是 HC dashboard / `SPEC-078` 的 metric bug
- `SPEC-080` 已基本排除 debit-side hardcode 作为当前主因
- metric / denominator 口径最多只能解释一部分，不能单独把 HC `+0.0856pp` 自然推到 MC 的 `+0.91~+1.03pp`
- 因此完整解释需要：
  - MC 侧不同 ROE definition / denominator
  - 和/或不同 trade path

HC 当前建议：下一步不要继续让 HC 盲跑，而是请 MC 补最小对账字段：

- MC 两份 full-sample ledger 的 total PnL delta
- MC 的 ROE formula
- baseline denominator
- trade count by strategy
- exit reason split

拿到这些后，HC 就能更明确判断这条 gap 是以 accounting / metric 解释为主，还是以 true path difference 为主。

### 6.5 Q039 继续留在研究位置

HC PM 现已明确：`Q039` 保持在研究位置，不提升成更强的 parity-investigation 主线。当前它的含义是 post-tieout residual attribution open item，后续是否升级，留待未来 evidence 再看。

HC 最新 Quant 判断也支持这一点：

- `Q039` 的最小下一步不是 `IVP` sweep
- 而是先拿到 MC 那 6 笔 `IC regular` 的日期级 ledger
- 然后做一个窄的 `IC regular trade-level divergence pack`

在 HC 当前 13 笔 `IC regular` 里，`9/13` 已落在 `ivp252 >= 55` 且 `9/13` 落在 `ivp252 >= 70`，并不表现为“轻微 IVP 临界区误差”；因此如果没有 MC 端 6 笔 ledger，HC 现在先扫阈值的收益不高。

### 6.6 pre-existing Gate 1 test failures（HC 特有）

HC test suite 中有 3 条 pre-existing failures：
- `test_spec_048_055.test_t16_gate_order_gate1_blocks_before_others`
- `test_spec_048_055.test_t5_low_vol_bullish_ivp252_35_waits_gate1`
- `test_spec_056.test_t9_gates_still_active_when_not_disabled`

这 3 条因 SPEC-056c 永久移除 Gate 1 而失效，**不是 SPEC-079/080 引入的回归**。HC 端 188 PASS / 3 pre-existing FAIL 是当前 canonical 状态。

### 6.7 HC 请求下一批 `SPEC-075 / SPEC-076` adoption 输入包

既然 HC 端现在已经接受 MC 侧更接近 `escalate / productization stack` 的方向，下一步不再是讨论 `hold vs escalate`，而是准备 `SPEC-075 / SPEC-076` 的 **HC adoption**。为避免 HC 端把 adoption 变成“重新发明一遍”，HC 希望 MC / PM 在下一轮同步里一并提供最小输入包，至少包括：

- `task/SPEC-075.md`
- `task/SPEC-076.md`
- 对应 MC commit / patch / file list
- `overlay_f_mode` 的默认 posture 说明（`disabled / shadow / active` 各自语义）
- old runtime 影响面说明（bot / web / dashboard / logs）
- 最小 regression / tieout 验证口径

HC 当前理解是：
- 这两项更适合 **作为同一批 adoption work 一起进入**
- 但实现 / review 仍可分开做
- 默认 rollout 姿态应优先考虑 `disabled -> shadow -> active` 的 staged path，而不是直接 active

---

## 7. HC 端当前不要求 MC 立即做的事

- 不要求 MC 针对 SPEC-077 AC3 量级差距立即做调查（建议先做上述 attribution run）
- 不要求 MC 对 SPEC-079/080 做任何额外工作（已双端 DONE）
- 不要求 MC 重新审视 Q036 决策（HC 端现已接受 MC 侧方向；后续只需按各自节奏处理 adoption / prerequisite 工作）

---

## 8. 若 MC 需要快速重建，建议阅读顺序

1. 本文件
2. `PROJECT_STATUS.md`（Last Updated: 2026-05-02 tieout #3 PASS）
3. `RESEARCH_LOG.md` 中 R-20260502-04 → R-20260502-07
4. `doc/tieout_2_2026-05-02/README.md` + `doc/tieout_3_2026-05-02/README.md`
5. `task/SPEC-079.md` + `task/SPEC-080.md`（如需查阅实现细节）
6. `task/SPEC-077.md` 最后一条变更记录（AC3 shortfall + PM option 2）
